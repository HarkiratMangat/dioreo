// scripts/lib/portalSeedRealms.mjs
//
// 🔴 THE REALMS WHOSE TWO SIDES HOLD DIFFERENT DATA UNLESS THE MOCKUP IS SEEDED. The staged-ops store is `sessionStorage` and every instrument clears it on load, while the portal harness synthesises four changesets — so any page carrying a staged surface compares an EMPTY mockup against a POPULATED one and returns well-formed numbers for a comparison nobody meant to make. Review was measured that way for its whole life (4.7% unseeded against 0.5% seeded) and Home for the first nine runs of Part 6b (78px apart unseeded, identical heights seeded).
//
// ⚠️ IT IS ONE LIST BECAUSE IT WAS FIVE. Part 6b added `home` by hand-editing the same guard in portalAudit, portalDiff, portalConverge, portalInventory and portalProbe — five copies of one fact, with nothing making them agree, so a sixth realm growing a staged surface needs five edits and a drifted copy fails SILENTLY by measuring one realm unseeded. Add a realm here and every instrument learns it at once. 🔴 ALL SEVEN AS OF 2026-09-04 20:43 EDT, AND THE CHANGE THAT CAUSED IT IS THE ONE THIS FILE EXISTS FOR. The rail's staged badge and the header's commit chip are drawn by the SHELL, on every realm — they simply never rendered on five of them, because only Home and Season passed `badges`. The moment all seven passed it, five realms grew a staged surface, and a staged surface on an UNSEEDED mockup is the empty-versus-populated comparison this list was written to stop: the portal harness synthesises four changesets on every page and the mockup seeds only under `?demo=1`. Caught on Armory's create overlay, where `a.hdr-commit` came back ONLY IN PORTAL. ⚠️ THE LIST IS NOW EVERY REALM, which reads like it could be deleted. It cannot: it is what makes the instruments REFUSE rather than quietly measure the wrong thing, and a realm added later starts unseeded unless it is named here.
export const SEED_REALMS = ['season', 'armory', 'broadcast', 'access', 'analytics', 'review', 'home'];
