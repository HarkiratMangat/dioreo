---
kind: record
status: live
---

# 🔮 Dioreo — Roadmap (Next Planned Work)

*The authoritative roadmap for the project — moved here from CLAUDE.md's "Next planned work" section on 2026-07-22 13:27 EDT as part of the CLAUDE.md modularization (see `docs/superpowers/specs/2026-07-22-claude-md-modularization-design.md`). The `🔮 Planned & Upcoming` section in `docs/CHANGELOG.md` and the `🔜 Coming soon` section in `docs/CHANGELOG-SUMMARY.md` are synced VIEWS of THIS file — update all three together, or they drift.*

*Implemented subsystems that used to live under this section now live in path-scoped rules: the built `/autobuild` PoC + Vertex migration → `.claude/rules/autobuild.md`; the Cloudinary structured-metadata system → `.claude/rules/loadout-images-and-metadata.md`. The `/autobuild` FOLLOW-UP items (for the next autobuild session) are documented at the end of `.claude/rules/autobuild.md`.*

*Items carry the `[Priority · Effort]` tag system (full spec: `reference_priority_tier_system` memory; quick legend atop `/Applications/Claude Code/meta-deferred-list.md`, mirrored in `docs/db-deferred-list.md`). **Priority** P0 now → P3 someday; **Effort** XS→L (scope ONLY — ⚠️ the model is a SEPARATE axis, picked from the premise-risk × deliberation-load grid, not implied by the tier); flags 🔗bundle-with 🧩needs-design ⛓️blocked-by. The near-term **v2** items below are tagged individually. **Version horizon already implies priority — v3 ≈ P2, v4/v5 ≈ P3** unless explicitly promoted — so those aren't per-item tagged. The deferred maintenance/tech-debt long-tail (after v5) is priority-tagged in **this project's own** `docs/db-deferred-list.md` (split out of the cross-project tracker 2026-07-25 15:56 EDT, renamed 2026-07-25 21:43 EDT), not duplicated here — as are this project's confirmed bugs and reminders.*

> **⇄ How this file and `db-deferred-list.md` actually relate (measured 2026-07-27 19:45 EDT).** The "not duplicated here" line above was aspirational: a cross-check found **7 items present in both files**, several with identical tags, and in the two richest cases (pagination perf, View Colors colour variety) the *deferred list* holds the fuller design detail while this file holds a stub. Rather than delete either copy, every one of those 7 now carries a `⇄` cross-reference naming the other location. **The rule going forward: this file answers "which version does it belong to," `db-deferred-list.md` answers "how big is it, what's decided, when does it get a session."** An item may legitimately appear in both — but it must carry the `⇄` marker, or the two copies will drift apart silently, which is exactly what happened here.

### Process / tooling (not a version-numbered feature, tracked here for visibility)
- ~~**v3 development structure**~~ — **SETTLED 2026-07-27 18:05 EDT, shipped as v2.35.6.** Design: `docs/superpowers/specs/2026-07-27-v3-development-structure-design.md`. v3 is built on a long-lived **`v3-pre-release`** integration branch cut off `main`; v3 features use the normal convention (`feat/*` → squash-merged PR) but with **`--base v3-pre-release`**, and `main` stays live-safe at every commit. v2 is *mostly frozen, case-by-case* — hotfixes still land on `main` and deploy normally. **Sync is one-way `main` → `v3-pre-release` by MERGE, never cherry-pick** (this corrects the 2026-07-14 directive; a cherry-pick never advances the merge base, so the same conflicts resurface on every sync). During pre-release: `docs/CHANGELOG.md` only, entries titled `Pre-Release v3.MAJOR.MINOR`, `package.json` carrying a matching `-pre` suffix, and **no git tags until `v3.0.0`**. The squash-vs-merge-commit shape of the final launch merge is deliberately left open until launch. Supersedes the branch/versioning half of memory `project_dior_builds_changelog_system`'s 2026-07-14 directive.
- ~~**First CI for the repo**~~ — **SHIPPED as v2.35.8** (2026-07-27 18:25 EDT). `.github/workflows/ci.yml`: `npm ci` → `npm run check` (`node --check` across every non-`node_modules` `.js`) → advisory `npm audit`. Triggers on **both `main` and `v3-pre-release`** — with `main` alone, no v3 PR would run CI at all, and that failure is silent. Expand to real tests/linting only once those tools exist here.
- ~~**Git branch/PR/merge workflow overhaul**~~ — **SHIPPED as v2.33.0** (the inaugural dogfood squash-merge on `feat/git-workflow`). Replaced the old "everything on `main`, push = version bump" model with Branch → Commit → Push → PR → Merge → Deploy; version now mints at merge (squash), not push. Design: `docs/superpowers/specs/2026-07-24-git-branch-pr-workflow-design.md`; canonical description: memory `project_git_workflow.md`. (Notes file L138, PRIORITY #2 — filed by Harkirat 2026-07-24 11:29 EDT, marked shipped in-file once this merge actually lands.)
- `[P3 · M · ⛓️blocked-by nothing, just deferred]` **Auto-deploy on merge** (GitHub Action/webhook → VM `git pull` + restart) — considered during the workflow-overhaul design and explicitly REJECTED for the initial rollout: it's real new infra, and it complicates `deploy.sh`'s deliberate-vs-crash restart-reason labeling (the `.restart-reason` marker assumes a human or script running `deploy.sh` directly, not a webhook firing unattended). Deploy stays a manual, separate, per-merge-optional step for now (a merged version can sit undeployed indefinitely, docs-only being the obvious case). Revisit only if the manual step becomes an actual friction point, not preemptively.

### 🏁 v2 IS SHIPPED — everything below this line is a LEGACY QUEUE, not a work plan (Harkirat, 2026-08-10 20:22 EDT)

**v2 is officially closed. New features go into the v3 release from here, including anything in these sections that is still open.** The three "batch" lists below were filed between 2026-07-14 and 2026-07-18 against a live v2; some of it shipped, some was overtaken, and some is still wanted — **but none of it is v2 work any more.** The parallel-track note the second and third batches carry ("ship to `main`/live NORMALLY even while v3 pre-release runs") describes the era they were written in and **no longer applies**; treat it as history.

✅ **SWEPT 2026-08-10 21:00 EDT — every item below now carries an evidenced verdict.** *(This paragraph used to warn that the lists had never been audited and that un-struck entries were "unverified, not outstanding". That warning is discharged; it is kept in the history rather than restated, because a permanent "unverified" banner over verified items is itself misinformation.)* **Result across all three batches — 10 items, 4 buckets:**
- ✅ **Already done (5).** Four were correctly struck and re-verified in code this pass — pagination loop-back (`utils/paginationRow.js:44`'s 2-page clamp), the `/manage` loadout data-entry overhaul (`checkImageExists` live in `utils/loadoutRender.js`), `/manage` accent colours, and the `/settings` half of the button-disable item. **The fifth was NOT struck and should have been: the pagination double round-trip perf fix**, done in PR #93 and finished in v2.60.0 — see its line below.
- ❓ **Still genuinely open, re-tagged v3 (3)** — View Colors' unset pages, richer in-bot logging (half-shipped), the admin `/status` command. Each now names the code that proves it.
- ⏭️ **Folded into v3 by a prior decision (2)** — the passive button-disable pair and View Colors' wider colour variety.
- 🗑️ **Abandoned (0).** Nothing here was overtaken or is unwanted; the queue was stale, not dead. Recorded explicitly so a later reader does not assume the bucket was skipped.

⚠️ **The one real defect this sweep found is worth naming, because it is the same shape as the button-disable miss.** The pagination line was a *stub* here while its `⇄` twin in `docs/db-deferred-list.md` carried the full record of the work shipping — so the file that answers "which version is this in" said open while the file that answers "how big is it" said done. **Closing one copy and not the other is the failure mode**, and it has now happened twice. Whoever closes a `⇄` item next closes both lines in the same edit.

**Already reclassified by that decision:** the passive button-disable work, which was committed to v2 earlier the same day and is **now v3** — see the two entries below, both re-tagged.

### Legacy v2 queue — batch 1 (filed 2026-07-14 from Harkirat's plan-notes file)
- ~~**Pagination double round-trip perf fix**~~ — ✅ **ALREADY DONE — swept 2026-08-10 20:59 EDT.** This line sat un-struck and tagged `[P2 · M]` for four days after the work shipped, which is precisely the drift the `⇄` rule at the top of this file exists to prevent: the deferred twin recorded the implementation and this stub was never closed. **Evidence:** the hybrid landed in `adcec5a` (PR [#93](https://github.com/HarkiratMangat/dioreo/pull/93), 2026-08-06) — `utils/sendV2Payload.js:85-91` goes single-hop with a `type: 7` UPDATE_MESSAGE when the interaction is not yet acked, and every already-deferred (heavy/attachment) caller still patches, exactly the agreed split. ⚠️ **Four routes were then deliberately REVERTED to two-hop in `138b25c` (PR [#97](https://github.com/HarkiratMangat/dioreo/pull/97), v2.60.0)** — `calpage_`, `price_region_`, `price_subpage_`, `set_page_` — because a single-hop re-render blanked a *different* button's emoji, isolated by A/B on the dev bot. **That revert is the finished state, not outstanding work**; do not "re-optimize" those four. ⇄ Full design, the live-test failure, and the ruled-out hypotheses: `docs/db-deferred-list.md` (🧹 Someday) → "Pagination perf hybrid" — still the richer copy. *(The old text also pointed at "Known open issues above", a section that no longer exists in this file.)*
- ~~**Actually disable expired buttons, not just reply with a message**~~ — **SHIPPED for `/settings` 2026-07-18, deployed live to the VM 2026-07-19** (confirmed via `scripts/vmstatus.sh`). ⚠️ **`/settings` ONLY — the strikethrough on this line has been reading as "done everywhere" and that is wrong.** Every other command with a 10-minute window still leaves its buttons and dropdowns clickable past expiry, answering with Discord's own "didn't respond in time" error. Harkirat re-raised it in the scratchpad, where it sat unfiled for days precisely because anyone checking "is this tracked?" found this struck-through line and stopped. **The remainder is now `docs/db-deferred-list.md` (🗂️ Queued), "Actually disable expired buttons on EVERY command"** — filed 2026-08-10 16:10 EDT. ⚠️ Harkirat has NOT yet live-tested the actual 10-minute idle behavior itself (open `/settings`, leave it alone for the full window, confirm the buttons go dead with zero clicks) — deployed ≠ behaviorally verified, don't conflate the two. See "Passive idle-timeout auto-disable" in the "Panel interaction locks" section above for the shipped design (`utils/passiveExpiry.js`) — a passive, sliding 10-minute idle `setTimeout` per message, not a click-triggered check.
- `[P1 · M · 🧩needs-design · 🔗bundle]` **Extend the same passive auto-disable pattern to more commands** — ⚠️ **NOW A v3 ITEM.** Harkirat committed it to v2 at 2026-08-10 18:42 EDT and reclassified it to v3 at **20:22 EDT the same day**, when v2 was closed outright; it is listed here only because this is where the mechanism half has always lived. 🔗 **It is one job with `docs/db-deferred-list.md`'s "Actually disable expired buttons on EVERY command" — `P1`, and NEITHER line should be closed without the other.** This line is the *mechanism* (nothing schedules expiry for these commands at all); that entry is the *visible behaviour* (components render disabled instead of erroring). Building one alone leaves either a command that expires invisibly or disabled buttons on a panel that never expires. draws/calendar/drawprices/loadouts have NO expiry/auto-disable at all today, unlike `/settings` now. Extending `utils/passiveExpiry.js`'s `schedulePanelExpiry` to their own render/re-render sites is the mechanical part. ✅ **The design question is CLOSED — 10 minutes everywhere (Harkirat, 2026-08-10 21:08 EDT: *"yeah 10 minutes is fine"* — and, in the same message, that it had already been discussed during the original `/settings` implementation).** ⚠️ **This item therefore has NO open design question and the `🧩needs-design` flag is discharged** — it is straight implementation across four commands' render and re-render paths. Do not re-open "should some commands use a different window": it was settled once for `/settings` and is now settled bot-wide. *(The 15-minute interaction-token ceiling still bounds any hypothetical longer window, but at 10 minutes it is not a live constraint.)*

**Legacy v2 queue — batch 2 (filed 2026-07-15 from the plan-notes file).** ⚠️ **The parallel-track rule below is HISTORY, not instruction** — v2 closed 2026-08-10 20:22 EDT, so nothing here ships to `main`/live as a v2 item any more; anything still wanted becomes v3 work. *(Original text, kept for context: these ship to `main`/live NORMALLY even while v3 pre-release work runs in parallel — see the parallel-track note in [[project_dior_builds_changelog_system]]; each one also gets cloned into the v3 branch once it exists.)* **7 of the original items in this batch shipped in v2.21.0** (2026-07-18): `/timestamp` `format`→`view`, `/settings`' `hidden` option, the mobile description trim, short/partial loadout search, the reworded action-blocked message, admin override on panel action-blocks, and the View Colors download buttons — see CHANGELOG.md's v2.21.0 entry and the relevant CLAUDE.md sections (Panel interaction locks, View Colors panel, `/timestamp`'s view option) for what actually shipped. Still open from this batch:
- ~~**View Colors: extract a wider variety of colors**~~ — **RESOLVED, confirmed by Harkirat 2026-08-13 10:52 EDT.** Shipped via the V3 two-pool OKLab extraction rewrite (seed starvation fix + accent pool), which fixed the exact juul's-avatar case this item was filed against. See `docs/archive/resolved-list.md` for the full writeup; the tracker itself lagged the actual fix since it shipped under different framing.
- `[P3 · S · Sonnet5-Medium · ⏸️UNDECIDED · 🔗bundle-with personality pass]` **View Colors: always show the Display Name / Nameplate / Deco pages even when unset/no Nitro** — instead of hiding them, render a humor/"bully" page (no colors shown). Ties into the personality direction in the v3 list below. ⏸️ **DEFERRED AND UNDECIDED — Harkirat, 2026-08-10 21:24 EDT: *"defer the view colors slash command pages hop feature. undecided honestly. might abandon it, might try to figure out a solution."*** **Downgraded `P2 → P3` to match.** ⚠️ **This is a live "might abandon", not a queue item waiting its turn** — do not schedule a session for it, and do not treat the technical note below as a green light. If it is picked up, it starts with *do we want this at all*, not *how do we build it*. ❓ **Verified still unbuilt in code 2026-08-10 21:00 EDT.** `utils/colorPaletteView.js:247`'s `getAvailableSources()` still filters an unset source out of `SOURCE_ORDER` entirely (`avatar` always; `name` only when `displayNameColors != null`; the rest only when the key is present), and `buildColorPalettePanel()` builds its selector from that filtered list — so an unset page has no button at all, which is exactly the behaviour this item wants replaced. ⚠️ **Preserve the distinction the code comment at `colorPaletteView.js:255-261` protects:** key *presence* means "has it" while a `null` palette means "has it, extraction failed" — a humor page belongs on the first case only, never on the second, or a real extraction failure starts reading as a joke. 🔗 **Bundle with the `/colors` wider-colour-variety session** (scheduled next) if that session is already in this file — same module, same render path — but it is not a prerequisite for it.
- ~~**Pagination loop-back**~~ — **SHIPPED v2.28.0 (2026-07-21); the 2-page case CRASH-FIXED in v2.30.2 (2026-07-21).** `buildPaginationRow` wraps last→first / first→last for 3+ pages and never disables those arrows. **⚠️ The original "loop at EVERY page count including exactly 2" was a bug, not a feature** — at exactly 2 pages the page-based (`makeCustomId`) arrows both targeted the one other page, producing an IDENTICAL custom_id that Discord rejects, taking down the whole render on `/draws`/`/calendar`/`/settings`/View Colors/`/alerts` (found live 2026-07-21). v2.30.2 clamps + disables the boundary arrow at exactly 2 pages on that path; the legacy direction-encoded path (loadout cards) still loops. So the "2-page redundant-arrows should revert to disabled?" question is now ANSWERED (reverted, forced by the crash) — the only remaining option is the cosmetic single-toggle alternative. See the `buildPaginationRow` note under "Shared UI builders" for the full mechanism.

**Legacy v2 queue — batch 3 (filed 2026-07-18 from the notes file — Harkirat's 2026-07-17 intake).** ⚠️ Same **historical** parallel-track rule as batch 2, and equally retired — see the v2-closed banner above. *(Original text: ship to `main`/live normally, clone into the v3 branch once it exists.)* **The `/manage` `section`→`data_for` rename also shipped in v2.21.0** (2026-07-18) — see the Command architecture section's `/manage` note. Still open from this batch:
- ~~**`/manage` loadout data-entry UX overhaul**~~ — **SHIPPED 2026-07-18, deployed live to the VM 2026-07-19** (confirmed via `scripts/vmstatus.sh`). ⚠️ Harkirat has NOT yet live-click-tested the actual `/manage` loadout flow in Discord — admin-only impact, so he's deliberately continuing other work first; deployed ≠ behaviorally verified. Added a "How Images Work" info block to both Loadouts pages, clarified the Add/Edit Loadout modals' field labels + placeholders (attachments now has an example; the Cloudinary Image Key field explains the real convention), and — the real meat — a genuine live Cloudinary existence check (`utils/loadoutRender.js`'s `checkImageExists()`) that now warns in the confirmation message if a saved image key doesn't resolve to anything, on Add/Edit/Bulk Add alike. The Cloudinary mystery itself is now fully documented with the ACTUAL confirmed workflow (verified live against the account, not guessed) — see the new "The Cloudinary image workflow, finally documented" subsection under "MP loadout system" above for the full writeup, including the root cause (Cloudinary assigns the Public ID from the uploaded file's own name unless renamed — confirmed via still-unrenamed `IMG_XXXX` assets sitting in the live `gun-builds` folder).
- `[P2 · M · Opus5-Medium · 🧩needs-design · ⏭️AFTER v3 LAUNCH]` **Much richer in-bot logging/tracking** — 📌 **SCHEDULED AFTER THE v3 LAUNCH, Harkirat 2026-08-10 21:24 EDT: *"deferred to a future session after v3 launches properly, its an internal feature and can be worked on whenever we want honestly so no immediate rush. would prefer to finish the main v3 features first."*** So it keeps `P2` — it is genuinely wanted, unlike the two `⏸️UNDECIDED` items above — but it is **explicitly not competing with v3 feature work**, and its zero user-facing surface is the reason. ❓ **STILL OPEN, but HALF OF IT SHIPPED — swept 2026-08-10 21:00 EDT.** ⚠️ **Do not read this as untouched, and do not re-scope it as if nothing exists.** v2.41.0's `utils/logger.js` delivered the **transport** half: `patchConsole()` rewrites every `console.*` call into a structured Cloud Logging record carrying real severity plus `service`/`version`/`commit` (`SERVICE_CONTEXT`, line 102), with a journald-aware file sink. **What it did NOT deliver is the half this item is actually about — attribution.** Its exports are `{ patchConsole, logBootBanner, VERSION, COMMIT, LOG_FILE }`; there is no component/subsystem field anywhere in the record, and it is required in exactly one place (`index.js:16`), so a failure still surfaces as an undifferentiated line with no way to say *which* subsystem produced it. **The remaining design question is therefore narrower than the original wording suggests:** how a component identifier gets attached — a per-module child logger, an explicit field at each call site, or a stack-derived tag — not whether to build a logging system. 🔗 Bundles naturally with the `index.js` per-subsystem split (a module boundary is exactly what a component tag needs). Right now when something breaks it's hard to tell WHICH component failed and why. Add granular internal logging so failures are attributable. Related-but-distinct from the webhook-alerting heavy half (per-alert IDs / downloadable text-log — see "Deployment & Ops (GCP)") and the v3 `/admin` DB-change audit log: this one is about internal diagnostic logging, not user-facing alerts. Scope at build.
- `[P3 · S · Sonnet5-High · ⏸️UNDECIDED]` **Admin `/status` command** — ⏸️ **DEFERRED AND UNDECIDED, Harkirat 2026-08-10 21:24 EDT: *"still deferred and unbuilt and still undecided if i even want to build it."*** **Downgraded `P2 → P3`.** ⚠️ **This is the item's SECOND deferral on the same grounds** — it was un-bundled from the webhook-alert work on 2026-07-20 with *"unsure of its usability at the moment"*, and the doubt is unchanged twenty-one days later. **Twice-deferred on usefulness is data:** if it comes up a third time, the honest question is whether it belongs in 🚫 Decided-no rather than back in the queue. ❓ **Verified still unbuilt 2026-08-10 21:00 EDT.** Nothing was ever built: `commands/` holds no `status.js`, and `setName('status')` appears nowhere in the tree. *(Phrased to avoid writing the absent path as a path — `docs-audit`'s `xref` check reads one as a live cross-reference and fails CI on it, which it did on the first draft of this line.)* The two `↻`/`UN-BUNDLED` notes below remain the live guidance, and the permission asymmetry they flag is still the thing to settle before scoping. — surface VM health/metrics in-bot (a mini ping test: is the bot holding up, gateway state, RAM/CPU, restart count), admin-only, building directly on `scripts/vmstatus.sh` / `scripts/vmpeaks.sh`, which already compute all of this — so checking the bot doesn't require asking Claude to run a script. (Notes item L60.) **UN-BUNDLED 2026-07-20 and still deferred** — it was originally queued alongside the webhook-alert improvements, but when that session started Harkirat chose to defer `/status` on its own ("unsure of its usability at the moment, don't want to spend time on it right now"). The webhook half **shipped without it** (v2.26.0, see `docs/reference/deployment-and-ops.md` — the alert store now exists, so a future `/status` CAN read severity counts / last-error from `utils/alertStore.js`'s `getAlertSummary()` if it's ever built). Note the earlier plan for `/status` to read historical CPU/RAM peaks needs the keyless-ADC + Cloud Monitoring path (see `utils/visionExtract.js`'s token fetch); live VM state (systemctl/gateway/RAM) needs no such thing and no journal permission (gateway state comes straight off `client.ws`). Revisit as its own session if/when Harkirat wants it. **↻ Materially easier since v2.41.0 (2026-07-28 15:52 EDT).** The `vmstatus.sh` overhaul added structured Cloud Logging (`utils/logger.js` → the Ops Agent) carrying real severity plus the running version and commit per entry, and settled the three-tier error model (errors / alerts / noise) that a `/status` panel would want to display. ⚠️ **But note the permission asymmetry it also surfaced:** the VM's service account can WRITE logs and not read them back, so a `/status` command running *in the bot* cannot query Cloud Logging the way `vmstatus.sh` does from the Mac — it would need either a broadened service-account role or to stay on `getAlertSummary()` + live `client.ws`/`process` state. Decide that before scoping the session, not during it.
- ~~**`/manage` accent colors**~~ — **SHIPPED 2026-07-20.** See "`/manage` per-page accent colors" under `.claude/rules/manage-panel.md` for what actually landed — it ended up as real sampled colors off the Legendary-rank and DMZ emoji assets (via the bot's own `getDominantColor()` pipeline), not invented hex values.

### v3 (next MAJOR version) — roadmap (filed 2026-07-14 from Harkirat's plan-notes file)
Harkirat's own planned feature set for the next whole-number version. Most of the list below is still captured intent to brainstorm/spec when picked up; some overlap (noted inline). The v3 branch / pre-release-versioning / test-bot strategy lives in [[project_dior_builds_changelog_system]], not repeated here.

⚠️ **This section used to open with "Not started; nothing here is a committed design yet". That went stale without anyone noticing** — corrected 2026-08-10 19:48 EDT, by which point **five pre-releases had shipped on `v3-pre-release`**: v3.1.0 `/help` · v3.1.1 guild install · v3.2.0 per-server profile colours · v3.3.0 nameplate/deco WebP · v3.4.0 `/server`. A "not started" banner on a section describing five shipped features is the kind of thing that makes a reader distrust the whole file.

#### 🚦 v3 launch checklist — the whole path, in order
⚠️ **RUN THIS NEAR LAUNCH, NOT NEXT.** Harkirat, 2026-08-10 20:22 EDT: *"let's hold off on the prelaunch hardening until we're closer to launch. I want to do a proper verification audit towards the end and a prelaunch hardening should be part of that."* This reverses his own 19:48 EDT call to harden immediately, after reading the roadmap and finding **more v3 work outstanding than either of us had counted** — hardening five features while a sixth and seventh are still coming just means hardening them twice. **Steps 1–3 below are the hardening half; they belong INSIDE the end-of-v3 verification audit, not in front of it.** Steps 4–6 are launch mechanics and were always end-of-line.

*Kept written down now, while the detail is fresh, precisely because it will be picked up weeks later by a session with none of this context. **Pointers, not copies** — each item's real detail lives where it is filed, and duplicating it here would rot.*

> ### ⚠️ STEP 0 — BUILD WORK STILL STANDS IN FRONT OF THIS CHECKLIST (added 2026-08-10 21:40 EDT)
> **Every step below assumes v3's features are finished. As of 2026-08-10 they are not**, and two launch-blocking build items were promoted into v3 *after* this checklist was written — **neither of which it mentions**:
> - **Split `index.js` into per-subsystem handler modules** `[P1 · L]` — promoted 21:24 EDT, *"definitely part of v3 before launch."* First slice needs scoping with Harkirat.
> - ~~**`/draws` + `/calendar` auto-expire** `[P1 · M]` — scoped 21:39 EDT as one session for both commands, *"before the v3 launch as well."*~~ **DEFERRED INDEFINITELY 2026-08-13 12:04 EDT, no longer a launch blocker** — see the full item below.
>
> **This directly compounds the reason step 1–3 were deferred in the first place.** Harkirat held the hardening pass at 20:22 EDT after finding *"more v3 work outstanding than either of us had counted"* — and within ninety minutes, two more items surfaced. ⚠️ **The ordering consequence is real, not bookkeeping: the `index.js` split rewrites the interaction router that step 1's entire click-test exercises.** Click-testing before that refactor means click-testing twice; refactoring after means the click-test validated code that no longer exists. **Sequence the split BEFORE step 1.**
>
> 🛑 **Do not start this checklist while `[P1 · ⏭️v3, BEFORE LAUNCH]` items remain open.** Re-check that tag across `docs/ROADMAP.md` and `docs/db-deferred-list.md` before beginning — the count has moved twice in one evening, so read it fresh rather than trusting this list.

1. **Click-test what already shipped. This is the actual risk.** ⚠️ **PARTIALLY DONE, per Harkirat 2026-08-13 10:42 EDT — three of four sub-items are click-tested live, `/server` is the one deliberate holdout.** Registration checks and unit tests are not a click. On the dev bot in 𝔇𝔯𝔢𝔞𝔪𝔩𝔞𝔫𝔡:
   - ✅ **`/help`** — click-tested in a real guild.
   - ✅ **Per-server profile colours** (v3.2.0) and **nameplate/deco WebP** (v3.3.0) — both are render paths that fail *silently* by design, so a green deploy alone would have proven nothing; both confirmed by eyes on a real render, in a real guild.
   - ✅ **Guild install** — the dev bot is live-installed in **2 real guilds** right now, confirming the mechanism works end-to-end (setup, presence, commands responding as a guild member). The checklist's original narrower ask — specifically a member who has NOT user-installed the app running a public command — was not itemized as separately confirmed; note if that exact case still wants its own check.
   - ⏳ **`/server`** — **still genuinely open, deliberately.** Harkirat 2026-08-13 10:42 EDT: *"still needs a detailed real world click tested but I'll do it some other time."* Not urgent, not forgotten — his own call to defer. The seven cases still to run, from `.claude/rules/server-controls.md`'s model: non-admin refused · a channel rule clamping a public-by-preference command in that channel and not elsewhere · a channel-scoped role rule beating the channel rule inside its channels only · a command rule clamping a member while an admin stays public · the clamp reaching one of the eight dynamically-built weapon commands (they never call `resolveEphemeral`, so they are the real test of the method wrap) · "Show Everyone" absent under a rule, and refused server-side on a panel opened before the rule was set · the clamp surviving a navigation click.
2. **Close the two silent-failure blockers before the deploy, not during it** — `img2webp` + the two cache-channel env vars on the prod VM (`docs/db-deferred-list.md`, 🔔 Reminders). Both no-op quietly if missed, and a successful deploy proves nothing about either.
3. **Verify what the VM is actually running** — `docs/db-deferred-list.md` records `main` as well ahead of the deployed bot with the live version **unverified**. Settle that before adding a release on top of it.
4. **Finalize `PRIVACY.md` v1.11** — a three-line edit, done *at* launch, deliberately not before. Filed in `docs/db-deferred-list.md` with the exact edit.
5. **Enable guild install on the live bot** — the launch action itself, the `[P1 · M]` item below.
6. **Dev Portal listing** — description + banner, Harkirat's own action, unblocked now that `/help` exists.

**Decided 2026-08-09 11:35 EDT (Harkirat) — SPLIT OUT OF THE v4 GUILD-INSTALL ITEM BELOW:**
- **Enable GUILD INSTALL on the live bot at v3 launch, with slash commands working in servers.** `[P1 · M]` Concretely: tick guild install in the Developer Portal (or `PATCH /applications/@me` adding `integration_types_config` key `"0"` with scopes `bot` + `applications.commands`, permissions `0`), and change **`setIntegrationTypes([1])` → `setIntegrationTypes([0, 1])` on every public-facing command**, then re-register. ⚠️ **This is the point at which root `CLAUDE.md`'s "This bot is user-installed only — it is NEVER a guild member" invariant becomes FALSE** — rewrite that section as part of this work, do not leave it contradicting reality. The `50001 Missing Access` explanation and the reason "Show Everyone" routes through the interaction-response mechanism both change meaning once the bot is a real member.
  - **What does NOT move to v3, deliberately:** text/prefix commands and the privileged **MESSAGE CONTENT** intent stay in v4 (see below). Guild install does not require MESSAGE CONTENT — that intent exists for reading message text, which only prefix commands need — so the split is clean and v3 takes on no Discord-approval dependency.
  - **Why this got separated from v4 at all:** bot presence (the green dot + the "Watching …" activity card) is delivered **only to users who share a guild with the app**, confirmed by controlled experiment on the dev bot 2026-08-09 11:27 EDT. At `guild_count: 0` it renders for nobody. Guild install is the only mechanism that makes it visible. **Treat it as a nice-to-have, not a channel to rely on** — Harkirat's explicit call: it must never become the primary way users discover `/help`, since it reaches only members of servers the bot has joined. See `docs/db-deferred-list.md`'s presence entry for the settled findings and the two open sub-tests.

- **Server-admin control surface — COUPLED TO THE GUILD-INSTALL ITEM ABOVE, treat as a launch blocker for it.** `[P1 · L · Opus5-High · 🧩needs-design · 🔗bundle]` *(filed 2026-08-09 11:47 EDT, Harkirat: "we don't want the bot to run freely bypassing everyone's oversight.")* **Model pick reasoning:** premise **Med** — the API facts are now measured (roles, bitfields, `owner_id` all verified below) but the *design* premises are not: what controls admins actually expect, allow/deny precedence, and the default posture for a new server are all open · deliberation **Very high** — a new schema plus a gate at the router choke point that every command passes through, interacting with the existing per-user `/settings`. Grid → `Opus5-High`. **First slice (required for an `L`):** the read-only permission check — resolve "is this invoker a server admin?" from the interaction payload and prove it against a real guild, before any settings model exists. A command for **server admins — distinct from Harkirat's own owner-level `/manage`** — letting each guild allow/restrict commands, usage, channels, and message behaviour (restrict or redirect responses), plus whatever else falls out at design. **Why it blocks:** shipping a guild-installable bot with no server-side controls means any member can invoke it in any channel with no moderator recourse; that is the kind of thing that gets a bot mass-kicked or reported, and it is far cheaper to design in than to retrofit. Do not ship the v3 guild install without at least a minimum viable version of this.
  - ✅ **The permission plumbing it needs already works — measured on the dev bot 2026-08-09 11:45 EDT, `permissions=0`, no privileged intents:** `GET /guilds/{id}/roles` returns all 43 roles **with their permission bitfields**, `GET /guilds/{id}` returns `owner_id`, and a single-member fetch returns that member's role ids. So "is this invoker a server admin?" is answerable by OR-ing their roles' bits and testing `ADMINISTRATOR`/`MANAGE_GUILD`, plus an `owner_id` match. **No GUILD_MEMBERS intent required** — that intent gates *enumerating* members, which this feature does not need. ⚠️ Cheaper still: a guild interaction payload already carries the invoking member's computed `permissions`, so the common path likely needs no REST call at all — verify that before building a fetch-based design.
  - ✅ **DESIGNED 2026-08-10 15:38 EDT — spec: `docs/superpowers/specs/2026-08-10-server-admin-visibility-policy-design.md`.** The `🧩needs-design` flag is discharged; what remains is implementation. Two decisions reshaped the brief and should not be re-litigated: **"blocked in a channel" means forced-EPHEMERAL, not refused** (the bot holds zero standing guild permissions, so response visibility is the only lever it can actually enforce — and since it is slash-command-only, an ephemeral answer is complete for the invoker and invisible to everyone else), and **"redirect" is scratched entirely**, not deferred as a smaller version of itself — answering in another channel needs a real channel permission on the invite link, which is a trust decision, not a coding one. Filed in `docs/db-deferred-list.md` along with the audit log and per-command role gating.
  - ⚠️ **Discord's native permissions cover far more of this than the entry above assumed — measured by Harkirat 2026-08-10 15:38 EDT, and both are settable per-role AND per-channel.** Disabling **Use Application Commands** removes every slash command from every app; disabling **Use External Apps** forces app output to ephemeral. So user-installed invocations are **not** ungoverned by server admins, which was this item's original headline justification.
  - ⏸️ **"Usage" — the one noun in this item's own wording that was never defined — is PARKED, not delivered.** This bullet asks for "allow/restrict commands, **usage**, channels, and message behaviour". Commands, channels and message behaviour all shipped or were filed; nobody ever established what "usage" meant, including Harkirat, who parked it 2026-08-10 18:42 EDT: *"i honestly dont know either. but its fine. if i ever think of a case in the future, i'll just launch a new session."* Recorded here so striking this bullet through does not quietly imply it was built. Detail: `docs/db-deferred-list.md`.
  - ❌ **AND THE FALLBACK JUSTIFICATION WAS REFUTED TOO — measured 2026-08-10 18:04 EDT.** The entry above hedged that a **guild-installed** app is not "external", so the native lever would stop applying in exactly the configuration v3 launches. **It does apply.** Harkirat tested with a member who does *not* have the dev app user-installed, so the invocation came from the guild-installed copy, and disabling **Use External Apps** for the channel forced the response ephemeral. **Two narrower gaps survive and are the whole justification for what shipped:** the native lever is blunt (it quiets *every* app in that channel at once, where `/server` quiets this bot alone), and nothing native can target a **single command** while leaving the rest public. The channel and role tiers duplicate a native overwrite in shape and are worth keeping only for per-app targeting — **do not re-file this item on the retired sizing.**

- **`/draws` + `/calendar`: auto-expire old data from view once the season ends** `[P3 · M · Opus5-Medium · 🧩needs-design]` *(twin created 2026-08-10 21:40 EDT; **this item had lived only in `docs/db-deferred-list.md` since 2026-07-31**, the same one-copy blind spot that made the `index.js` split read as unfiled.)* **Display filtering, NOT deletion** — Harkirat's original wording was "automatically disappears from **view** instead of having to manually be removed", and that distinction is the whole feature. 🔗 **Both commands in one session, non-negotiable:** `/calendar` already has the mechanism (Active/All toggle + `isEventEnded()`), `/draws` has **none** (verified again 2026-08-10 21:00 EDT — zero hits for any expiry or season-end filtering in `commands/draws.js`), so building `/draws` alone would invent a second definition of "ended" alongside the one already in `calendar.js`. ⚠️ **The design question is Harkirat's:** what "the season has ended" means for a draw specifically (own release date / `bpEnd` / `rankEnd`), and whether `/draws` mirrors the Active/All toggle or does something simpler.
  - ⛔ **DEFERRED INDEFINITELY 2026-08-13 12:04 EDT — no longer a v3 launch blocker.** Harkirat: *"let's defer the entire auto expiry thing indefinitely for the time being. It's a QoL thing and honestly I want to wait a bit and build it more towards when the current season ends so I can correctly see what I need and how to handle it based on my real world need."* Dropped from `P1`/before-launch to `P3`/someday — revisit as the current CODM season nears its own end, not on a fixed date. ⇄ **`docs/db-deferred-list.md` holds the fuller analysis**; both updated together.

- **Split `index.js` into per-subsystem handler modules** (filed 2026-07-22 13:55 EDT, during the CLAUDE.md modularization — Harkirat explicitly invited evaluating this; deliberately NOT done in that docs-only session because it's a live-bot code refactor, a different risk class). `[P1 · L · Opus5-H · 🧩needs-design · ⏭️v3, BEFORE LAUNCH]`.

  🔺 **PROMOTED TO v3 AND RAISED `P2 → P1` — Harkirat, 2026-08-10 21:24 EDT: *"index.js also still is pending a split of some of its code. idk if it's been notes/deferred anywhere? but it's definitely part of v3 before launch."*** **Answering his question directly: it WAS filed — but only here, in the v5/housekeeping section, and it has no `⇄` twin in `docs/db-deferred-list.md`.** That is exactly why it read as unfiled: the v3 launch checklist above never mentions it, and this section is the one a reader skips as "someday". **It is now a launch-blocking v3 item and belongs in the checklist's ordering discussion, not in housekeeping.** ⚠️ Note his wording — *"a split of some of its code"* — which is narrower than this entry's full decomposition and matches the incremental approach already recommended below. **Scope the FIRST SLICE with him before building; "some" is a decision, not a measurement.**

  ⚠️ **The line count below was stale by 25% and is corrected — `index.js` is now 4,157 lines (measured 2026-08-10 21:28 EDT), not ~3,313.** It grew ~840 lines while sitting in a queue whose stated justification was its size. *(A present-tense number in prose rots — see [[feedback_no_duplicated_state_in_prose]]. Kept here as a **dated measurement** rather than a bare claim, which is the convention that survives.)*

  **The problem:** `index.js` was ~3,313 lines when this was filed and is **4,157 as of 2026-08-10**, the bulk of it a single `client.on('interactionCreate')` handler — a giant custom_id-prefix switch that has only grown. **Proposed shape:** extract per-subsystem routing into `handlers/*.js` (e.g. `handlers/{manage,settings,colors,loadouts,drawprices,autobuild,nav}.js`), leaving `interactionCreate` a thin dispatcher: anti-spam guard → centralized `/manage` panel guard → route by prefix to the right handler. **The real risks to design around (why it's L / its own session, not a bundle-in):** (1) the outer top-level try/catch AND the `client.on('error')` net must still wrap every extracted handler — the crash-resilience invariant can't regress (see `.claude/rules/interaction-router.md`); (2) many handlers close over shared state (`buildSyntheticInteraction`, `resolvePanelActor`, the short-lived token Maps `manageUndoStore`/`pendingManageEdits`/`pendingBulkDeletes`/`dynamicColorCache`/etc.) that must move to a shared module or be passed in, not duplicated; (3) custom_id routing ORDER matters (the panel guard stays first); (4) it needs a real boot test (gateway actually connects) + a live interaction test + a VM deploy + verify — per [[feedback_verify_fix_actually_works]]. **Approach: incremental** — extract ONE self-contained subsystem first (e.g. `drawprices` or `colors`), boot-test + deploy + verify, then the next; never a big-bang rewrite. Bundles naturally with the "richer in-bot logging" and "hot-reload" items (a modular router makes both easier). Do it in its own session.
**Filed 2026-07-21 from the notes scratchpad (new intake, not yet designed):**
- **Multi-user admin access (provide/revoke).** ⏭️ **Scoped for next session 2026-08-13 12:25 EDT, bundled with the announcement feature + `/colors` sub-option — see `docs/db-deferred-list.md`'s 🗂️ Queued.** Let Harkirat grant/revoke selected users access to some or all admin-level commands. **Store the allowlist in MongoDB, not hardcoded** (decided 2026-07-21) — it must be editable at runtime (grant/revoke with no code change + redeploy); hardcoding the IDs would make every access change a deploy. The current single-admin `ALLOWED_ADMIN_ID` const stays as the ultimate owner; the Mongo allowlist layers on top. ⚠️ **Scope narrowly against the EXISTING `/manage` surface** — the `/manage` → `/admin` restructure this bullet mentions is its own large, undesigned item; don't pull it into this session.
- **"Recommended" loadout badge** — for weapons with multiple build variants; the recommended build always renders FIRST in pagination order. (New badge alongside the existing Meta/Toxic/rank badges — see the MP loadout badges in `.claude/rules/loadouts.md`; a weapon-level or per-build flag, decide at design.)
- **"Troll Build" badge** — for joke/silly builds. Emoji provided: `<a:TrollBuild:1529192136208154725>`. Independent flag like `isToxic` (see `buildBadgesLine()`).
- **Optional paginated multi-weapon loadout view** — browse many weapons in one paginated view with thumbnails + category-switch controls; for multi-build weapons show the "Recommended" build. **Overlaps the `/meta` and `/loadout` consolidation items below** — reconcile into one shape at design time, don't build three separate paginated browsers.
- **Partial "hot-reload" of a pushed file** — a way to make a pushed code change go live WITHOUT a full VM redeploy each time (its own design session — Node doesn't hot-swap `require`d modules trivially; realistic options are a watch-and-restart, a targeted module reload, or accepting the current `deploy.sh` restart as already fast). Scope when picked up.
  - **Still open, but materially less painful since 2026-07-26 13:45 EDT.** The local dev bot now covers the *iteration* half of this pain: `node --watch --env-file=.env.dev index.js` auto-restarts on every save and branch switch, so trying a change no longer requires a deploy at all. This item is now specifically about **the VM** — avoiding a full redeploy for an already-pushed change. Note `--watch` is a **full process restart**, not a module hot-swap, and Node documents it as not-for-production, so it is not itself the answer here. See `docs/reference/deployment-and-ops.md`.
- **`/meta` command** — view all weapons marked Meta. Options `mode:MP/DMZ`, `category:AR/SMG/...`, same `visibility` option as others (renamed from `hidden` 2026-08-08, see `.claude/rules/commands-overview.md`); visibility tied to the `loadoutVisibility` toggle. Paginated through each meta build (a weapon's multiple builds shown in order, then the next weapon); in-panel dropdown to jump to a specific meta weapon; category-switch buttons below the embed; per-category accent color (reuses `getMpCategoryAccent()`); badges (incl. the Meta badge itself) hidden from this view. **NOTE: overlaps with the `/loadout` meta subcommand idea below — likely the same feature reached two ways; reconcile at design time rather than building both.**
- **Draw cost calculator** — given the user's CP region, draw type, attempts already done/remaining, and current CP balance: compute cost to finish the draw, and suggest the top-up package needed if the balance is short. Builds on `/draw prices`' existing per-pull `DRAW_DATA`.
- **Rename `/manage` → `/admin`, with slash-command-driven actions ALONGSIDE the existing panel.** Keep the interactive dashboard embed, but also support: `/admin` (opens dashboard), `/admin command:{x}` (opens dashboard on that command's page), `/admin command:{x} action:{y}` (opens that action's modal directly — add / bulk add / export new-or-returning draws / purge / etc.). The `action` choices must be scoped to only the actions valid for the chosen `command`, not a flat list of every action across all commands. Examples: `/admin command:loadouts action:add`, `/admin command:loadouts action:export SMGs visibility:public`, `/admin command:draws action:bulk delete`, `/admin command:season titles-&-deadlines` (no action — always just opens the modal). Also bundle in an internal DB-change logging/tracking system (log edits made via the admin command — e.g. a draw's info being edited).
- **`/settings` jump-to options** — `/settings customize:visibility|preferences|colors visibility:…` to land directly on page 1 (visibility), page 2 (preferences), or open the colors menu directly.
- **Detach `/colors`'s visibility from `/settings`** — give `/colors` its own visibility preference (while keeping the "View Colors" button ON the settings panel tied to settings visibility), and add `/colors` visibility toggles into the settings page.
- **Consolidate MP loadout commands into one `/loadout weapon:{fuzzy autocomplete}`** (leave `/dmz` as-is, already consolidated). Ideally one command that can search ALL weapons OR be scoped to a single category. Plus a **meta subcommand**: an embed listing just Meta-marked weapons, a dropdown to pick one (option description = category / main use-case, TBD at build), and pagination for multi-build weapons. (Overlaps with the standalone `/meta` above — pick one shape.)
- **Update/add new builds + audit current loadout data** so it's current with the live season.
- **Different view options for the slash commands** (unspecified in the notes — expand when picked up).
- **Ship the redesigned changelog artifact** (personal-use release-log visual — see [[project_changelog_redesign]], currently paused). ⇄ Also in `docs/db-deferred-list.md` (🧹 Someday) with its `[P3 · M · Opus5-M]` tag. Same item; this is the version-horizon view.
- ~~**A `/help` command**~~ — **SHIPPED as Pre-Release v3.1.0** (2026-08-08 22:34 EDT). Categorized Components V2 panel (Gunsmiths/Draws/Seasonal Info/Utilities/Preferences), a mascot hero header, per-command Usage/Options/Examples, live-queried per-weapon-category commands, and a contact mention — built iteratively from Harkirat's own JSON mockups + direct review feedback. Also shipped alongside it: the bot-wide `hidden`→`visibility` option rename (see `.claude/rules/commands-overview.md`). See `commands/help.js`.
- **Update the bot's Discord Developer Portal listing** — name, description, and banner image. Folded into v3 2026-07-27 23:23 EDT (Harkirat's call) since the description rewrite naturally depends on `/help` existing first. **The description/banner half is now UNBLOCKED** — `/help` shipped 2026-08-08 22:34 EDT — but still a pure Discord Dev Portal task, not something Claude has UI access to do; Harkirat's own click-through. ⇄ Also in `docs/db-deferred-list.md`'s 🔔 Reminders (horizon-neutral action-item view).
- ~~**Privacy Policy / Terms of Service**~~ — **STALE, already shipped, caught 2026-08-13 12:14 EDT while auditing this list.** Filed 2026-07-18 before either existed; `docs/legal/PRIVACY.md` (51.4K) and `docs/legal/TERMS.md` (33.2K) are real, live, published documents (see `.claude/rules/legal-site.md` and root `CLAUDE.md`'s "⚖️ Licensing" section). The only remaining sliver — finalizing `PRIVACY.md` to v1.11 — is already tracked separately in the launch checklist (step 4) and `docs/db-deferred-list.md`, not here.
- **Urban Dictionary integration, a `/define` command** (filed 2026-07-18, notes, "lmao") — `[P3 · S]`, pure fun/personality, not CODM-related. Someday-bucket, no urgency.
- **Richer usage analytics/telemetry** (filed 2026-07-18, notes) — `[P2 · M · 🧩needs-design]`. Distinct from the already-filed "richer in-bot diagnostic logging" below (that one is about FAILURE attribution; this is about USAGE — who ran what command, when, how often, and ideally a sense of how people actually navigate a command/feature (e.g. do they use the dropdown or retype the slash command). Needs design: what's tracked, where it's stored, retention, and — important — this is exactly the kind of data a Privacy Policy (above) needs to disclose, so these two should ship together or in the right order, not independently.
- **Personality pass: "bully people who are broke"** (filed 2026-07-15, Harkirat's words) — a silly running gag to give the bot some character. No fixed home yet; sprinkle it in as we go. Already has two concrete landing spots picked: the unset Display Name/Nameplate/Deco humor pages (v2 list above) and the reworded action-blocked message (v2 list above). Keep it light/jokey, never actually mean.
- **Announcement feature** ⏭️ **Scoped for next session 2026-08-13 12:25 EDT, bundled with multi-user admin access + the `/colors` sub-option — see `docs/db-deferred-list.md`'s 🗂️ Queued.** (filed 2026-07-18, the notes file) — a `/manage` (`/admin`) dropdown → modal where Harkirat writes an announcement. If a NEW announcement exists, the next time each user runs ANY command the bot replies to their command AND follows up once with an embed of the announcement; it's never shown to that user again until the next announcement is posted. Needs a per-user "last-seen announcement" marker (a `UserPreference` field) plus the announcement text/timestamp on `SeasonalData` or its own doc. Use case: "sorry the bot was down today — we've moved to much better hosting." Follow-up embeds must go through the interaction-response mechanism (this bot has zero standing guild permissions — see the user-installed-only invariant in root CLAUDE.md), same constraint "Show Everyone" already works within.
- **Easy bot sharing / `/invite`** (filed 2026-07-18, the notes file) — a first-class way to share / user-install the bot. Sharing the raw install URL is awkward, and in servers where user-apps are blocked every bot reply is ephemeral, so you can't even tell someone to click the bot's name → "Add App". Need a share path that works in those blocked-context cases AND is shareable OUTSIDE Discord entirely (a link/page). A `/invite` command is the obvious start but hits the blocked-user-apps wall — solve for that case too. Relates to the v4 guild-install direction, which would change the sharing story again (reconcile at design time).
- **First-use welcome guide** `[P2 · S]` (filed 2026-07-29 18:24 EDT from the notes file) — on a user's very first command, append a short welcome/orientation message: what the bot does, its main features, and a pointer to `/help` for detail. Needs no new permissions and works in DMs — it is one extra component on a first-use flag in `UserPreference`, and an interaction response never pings anyone. **The design question worth settling before building is where that flag lives and how it interacts with `utils/passiveExpiry.js`**, since a user whose panel idles out and comes back must not be re-welcomed. Pairs naturally with the `/help` command already queued above — build them together or the welcome message points at something that isn't there yet. Remember to add the flag to the Mongoose schema in the same change (the schema-save gotcha).
- **Show a user's ACTUAL local time, not their configured timezone** `[P2 · M · 🧩needs-design]` (filed 2026-07-29 18:24 EDT from the notes file) — Harkirat asked for a way to read a user's current time from their device or location rather than the timezone they set in `/settings`, and asked directly whether it is even possible. **Answered, because it changes what this feature can be: NO. Discord exposes no device clock and no geolocation to bots — there is no API for it, and no permission, intent, or guild install unlocks it. It is a data-availability limit, not a permissions problem, so this cannot be built as literally described.** His other concerns resolve easily by comparison: it would work in DMs, and nobody gets pinged (an interaction response is not a mention). What IS buildable is better *inference plus less friction* — either a one-tap timezone picker surfaced on first use (a select menu, no typing, no hunting through `/settings`), or a guess derived from the user's Discord locale that they confirm or correct in one click. Both are honest about being a guess, which the current silent default is not. Bundle with the welcome-guide item above: first use is exactly the moment to ask.

### v4 — roadmap (filed 2026-07-15, further out than v3; nothing designed yet)
- **Add TEXT/PREFIX commands to the (by then already guild-installed) bot** — e.g. `d b ak117` ("dior build ak117"), plus a manually-settable per-server prefix. ⚠️ **RE-SCOPED 2026-08-09 11:35 EDT: the guild-install half of this item moved to v3** (see the v3 section above) — v4 is now ONLY the text/prefix layer and its privileged **MESSAGE CONTENT** intent. Read the rest of this bullet with that split in mind: the portal's Guild Install toggle and `setIntegrationTypes([0, 1])` are v3 work and will already be done; MESSAGE CONTENT (and its 100-guild Discord approval gate) is what remains here. Commands like the prefix-setter should be server-EXCLUSIVE (the slash version only appears in a guild, never in a DM). **⚠️ This breaks the single biggest architectural assumption in this file** — see "This bot is user-installed only — it is NEVER a guild member with roles/permissions" above. That whole section becomes false under v4, and things it explains (the `50001 Missing Access` wall, why "Show Everyone" had to route through the interaction-response mechanism instead of a channel POST) would change. Re-read and rewrite that section as part of v4, don't leave it contradicting reality. **Discord Dev Portal changes required** (confirmed 2026-07-15): enable Guild Install in the portal's Installation settings, add `setIntegrationTypes([0, 1])` to commands, and — the big one — enable the **privileged MESSAGE CONTENT intent**, which needs Discord's approval once the bot passes 100 guilds. Real guild membership with View Channel / Send Messages also becomes a genuine requirement.
- **User-submitted loadouts, gated behind Harkirat's manual review** — a submission never goes live until he approves it. Needs a review surface where he can Deny / Accept / Accept-with-edit each submission (likely an extension of the `/admin` panel from v3).
- **OCR a CODM "UID" out of a user-supplied screenshot** `[P3 · M · Opus5-High · 🧩needs-design]` *(filed 2026-08-10 16:10 EDT from `docs/ideas/diors-notes.md`, Harkirat's 15:57 EDT idea.)* A UID is the long numeric CODM player ID used to find and add accounts (e.g. `7560148759649910787`); the ask is to pull one out of a screenshot so a player does not have to transcribe nineteen digits by eye. **The extraction half already has precedent and is the easy half** — `/autobuild` runs Gemini vision over screenshots today (`utils/visionExtract.js`), and a long digit run is a far easier target than a loadout's attachment names. **The hard half is that this is the FIRST feature to accept an image from an ordinary user rather than from Harkirat.** `/autobuild` is admin-only and reads an attachment off an admin's own command, so every existing assumption about who can send the bot a file, what it costs per call, and what happens when someone uploads something abusive is untested here. Design the intake before the OCR: rate limiting, what is retained (ideally nothing — see `docs/legal/PRIVACY.md`, which currently states images are never stored), the Vertex AI cost per invocation at public volume, and the failure copy when a screenshot simply has no UID in it.
- **A stateless MCP server over the bot's own data** `[P3 · L · Opus5-High · 🧩needs-design]` *(filed 2026-08-10 16:10 EDT from `docs/ideas/diors-notes.md`; Harkirat's own framing is v4 "or maybe v5". It sat unrouted in the scratchpad for days — his 15:52 EDT catch.)* Expose the draw-pricing, loadout and calendar data so a natural-language question can be answered against it: his example is *"I've done 3 pulls of the Mythic Fennec draw, how much more do I need to spend to finish it?"* → *"7 pulls left, x CP total (~$x USD); pull #4 costs x CP."* Could serve the bot, the site, or the admin docs. **The draw-pricing case is the one worth prototyping first** — `commands/drawprices.js` already computes the full per-pull cost curve, so the MCP would be exposing an existing calculation rather than inventing one, which makes it a genuine test of the interface instead of a test of the maths.
- **Semi-automate seasonal data retrieval from Harkirat's leaker announcement channel** (filed 2026-07-18, notes, moved from a "v5 maybe" to v4 since it likely NEEDS the same privileged MESSAGE CONTENT intent v4 already requires above) — `[P3 · L · 🧩needs-design]`. Today Harkirat manually reads a Discord channel where a group of "leakers" relay CODM news/leaks/updates and hand-enters it all into `/manage`. Wants the bot to help retrieve/parse that instead. Real open questions: filtering/refining informal leak text into structured data, and — critically — a moderation/approval step so nothing auto-publishes unverified. Likely shares infrastructure with the "User-submitted loadouts" review surface above (both are "something comes in, Harkirat approves before it's live").

### v5 — roadmap (filed 2026-07-15; deliberately far-future, NOT speculative)

⚠️ **This heading used to say "most speculative", and that was wrong — corrected 2026-08-10 21:52 EDT on Harkirat's own reading:** *"v5 is not 'speculative' per say, it's just stuff that id rather do much into the future. they're still very real plans"* — either experimental, not needed right now, or pushed out as a step-ladder of features and improvements. **These are real, wanted plans.** What puts them at v5 is one of three things — they are **experimental**, they are **not needed yet**, or they sit further up a **step-ladder** whose lower rungs come first — and in several cases they are **very large feature reworks** (the gunsmith generation pair below is the clearest example). ⚠️ **"Speculative" invited exactly the wrong handling: it reads as "may never happen", which is how real work gets skimmed past.** Do not restore that word.

🧹 **This section was also a DUMPING GROUND until 2026-08-10 21:52 EDT, and that is why it read badly.** It held three different kinds of thing under one heading: genuine far-future features, **completed-work status notes** from July that were never work items at all, and a **decided-no**. Harkirat flagged it; the sweep moved everything that was not a real v5 feature. **Only far-future feature work belongs here.** Maintenance and tech-debt now live in their own version-neutral section below; finished status notes went to `docs/archive/resolved-list.md`, where a record of what shipped belongs.
- **Generate the gunsmith image + share code ourselves, removing the manual-screenshot requirement.** Given a weapon + its attachments, the bot builds the image and the Gunsmith code, then stores it in Cloudinary. Groundwork this needs: teach the gunsmith CODE structure, teach the gunsmith LAYOUT design, and supply the base no-attachment gunsmith page for each weapon (they differ per weapon). Harkirat's own note: explore the idea further at v5 time — this is a research spike, not a spec.
- **User-built custom gunsmiths in-bot** (depends on the above working). Pick weapon → pick that weapon's available attachments → generate image → share/download. Plus a "my builds" command to save and view custom loadouts, merged INTO `/loadout` results for that specific user when they search that weapon — but visually distinguished so a custom build is never mistaken for one of the bot's own official builds.

- **Write a detailed, user-friendly bot/ops guide** (added 2026-07-18, the notes file — Harkirat's request, "not anytime soon"). A rich but noob-friendly, clean, well-organized, nicely-worded doc covering how to operate the bot end-to-end: accessing/managing the GCP VM, where + how it's hosted, the deploy flow, checking status/logs/metrics, the whole backend — so Harkirat can maintain it himself instead of relying on Claude for every operation. Distinct from CLAUDE.md (architecture/design truth aimed at Claude) and from [[reference_vm_bot_commands]] (a terse command card) — this is a human operator's friendly how-to guide. ⇄ Also in `docs/db-deferred-list.md` (🧹 Someday) with its `[P3 · M · Opus5-M]` tag. Same item; this is the version-horizon view.

### 🧹 Maintenance & tech-debt — VERSION-NEUTRAL (not tied to any release; created 2026-08-10 21:54 EDT)

*These four were sitting in the v5 section and are **not v5 work** — they are maintenance, and maintenance has no version horizon. Harkirat identified them as "genuinely pending and should be properly placed" when he corrected the v5 framing. **Filing them under a version number was the bug**: it implied they were gated on v3 and v4 shipping first, when in truth any of them could be done in any week where a session has room. ⚠️ **Nothing here is a v5 prerequisite and nothing here blocks a launch** — but nothing here should be read as "someday" either; that was the old v5 heading talking. Each keeps its `⇄` twin in `docs/db-deferred-list.md`, which stays the canonical copy for size and scheduling.*

- **The real "search + multi-select" flow — `/manage`'s Delete Multiple (all entities) and Loadouts' Replace Multiple** `[P2 · L · Opus5-H]` — deliberately deferred out of the 2026-07-12 `/manage` panel redesign at Harkirat's explicit request, to avoid risking a usage-limit interruption mid-build on top of everything else in that pass. Today both are placeholder paste-a-list flows; this is the genuinely-new interaction they are meant to become — **search first, then tick which matches to act on.** See `.claude/rules/manage-panel.md` for what is a placeholder now vs. what the real version must do. ⚠️ **It is an `L` with no named first slice**, which by this project's own rule makes it un-schedulable as written — decompose it before booking a session. ⇄ Also in `docs/db-deferred-list.md` (🧹 Someday).
- **Full DEVLOG backfill from prior chat transcripts** `[P3 · M · Opus5-M · ⛓️blocked-by:token budget]` (added 2026-07-13) — `DEVLOG.md` shipped a v1 covering 2026-07-13 richly plus brief `[backfill — expand later]` stubs for earlier milestones. The task is to retrieve the actual prior transcripts — which hold reasoning, interactions and discoveries that never reached `CLAUDE.md` or memory — and merge them into Part A's journey and Part B's lessons ledger. ⇄ Also in `docs/db-deferred-list.md` (🧹 Someday).
- **Decide whether `patchnotes.js`'s media carousel needs component-count chunking** `[P3 · S]` — `calendar.js` and `draws.js` both carry defensive chunking; `patchnotes.js` does not, and Components V2 caps a message at **40 components counted recursively** (a real production crash this project has already hit). ⚠️ **UNVERIFIED, not confirmed broken** — the cheapest close is to count what a worst-case real patch-note entry actually renders rather than building chunking speculatively. If it is nowhere near 40 this becomes a not-a-real-issue archive entry and costs nothing further. ⇄ **`docs/db-deferred-list.md`'s 🐞 Active Bugs holds the full detail** — it was moved there 2026-08-06 out of the constraints file, because the thing at fault would be *our* missing guard, not a Discord limit.
- **General bot/code housekeeping session** `[P2 · M · Sonnet5-H]` (added 2026-07-15, Harkirat's request — "at some point soon", not urgent). A dedicated pass for accumulated cruft rather than doing it piecemeal mid-feature. ⇄ Also in `docs/db-deferred-list.md` (🧹 Someday).
  **Most of this batch DONE 2026-07-20:**
  - ~~Delete leftover config backups~~ — DONE: both `.claude/settings.local.json.bak-20260715-110452` and `~/.claude/settings.json.bak-20260715-102110` deleted, after confirming the current settings files they back up still parse as valid JSON.
  - ~~Audit for other stale absolute paths~~ — DONE, came back clean: no live config/code references the pre-relocation path anymore. The only remaining `/Applications/Diors-Builds` mentions are historical incident narrative in `docs/SESSION-START.md`/`CHANGELOG.md`/`DEVLOG.md`, correctly describing the past hook bug, not stale active paths. **Note (updated 2026-07-28 01:41 EDT): the memory store no longer sits at the `-Applications-Diors-Builds` slug — it was MIGRATED to the repo-slug path `-Applications-Claude-Code-Diors-Builds`, retiring the "deliberate and move-proof" rationale this line used to state. See the canonical-memory-path invariant in `CLAUDE.md` (this line previously pointed at "the top of this file," where no such note has ever existed).**
  - ~~General dead-code / stale-comment / unused-dependency review~~ — DONE. Found and fixed 2 real items: (1) the unused top-level `mongodb` npm dependency (declared in `package.json` but never directly `require()`'d anywhere — only `mongoose`, which bundles its own driver, is actually used) — removed via `npm uninstall mongodb`, confirmed `mongoose` and every touched command module still load fine, `npm audit` unchanged (same pre-existing tracked discord.js/undici/xlsx findings, nothing new). (2) `index.js`'s Express "keep-alive" HTTP server (the old "PHASE 1" banner) — existed purely to stop Render/Railway's free tier from idling the bot; confirmed dead now that the bot's been on the GCP VM under systemd since 2026-07-17 (doesn't idle/spin-down), and nothing else in the repo referenced that endpoint or port 3000. Removed (code + the now-unused `express` dependency) with Harkirat's explicit confirmation first, since it's a live-runtime change, not pure repo hygiene. A breadcrumb note was left where "PHASE 1" used to sit, same convention as the existing removed- "PHASE 5" note further down in `index.js`, so the phase numbering (now starting at 2) doesn't read like something's missing. Checked every `utils/*.js` file for orphaned/unreferenced files — none found.
  - **Still open from that batch:** the `patchnotes.js` media-carousel chunking question — **now its own bullet above in this section**, rather than buried as a sub-item of a mostly-finished housekeeping pass. *(It was also pointing at "Known open issues", a section that no longer exists in this file; the detail now lives in `docs/db-deferred-list.md`'s 🐞 Active Bugs.)*
