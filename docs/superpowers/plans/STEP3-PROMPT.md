---
kind: plan
status: live
---

# STEP 3 — the starting prompt for the next session

> 🔴 **THE BLOCK IN §1 IS AN OPENER, NOT A SUMMARY OF THIS FILE. Read the whole thing before acting on it.**
>
> The opener tells you the unit of work and where to start. It does **not** carry §3, and §3 is the half that stops a session repeating a paid mistake — that `critique` must not be run on another realm, that the Track lane-collapse claim was measured and refuted, that the last 21 memories are deliberately left un-homed, that `.claude/settings.json` must not be committed. **A session handed only the opener will run a critique pass, because nothing in the opener forbids it.**
>
> Nor does the opener carry §2, which is the evidence for each of its own lines — including that the category-accent decision had read *do not re-derive this* for two days at the moment it was put to Harkirat as an open question.

*Written 2026-09-06 20:41 EDT. Tracked on purpose: a gitignored handoff does not survive a fresh clone, and this is the one file the next session is told to open first. The copy-paste block is §1; everything after it is why each line is there.*

## 1 · The prompt

```
Read docs/superpowers/plans/2026-09-06-portal-step3-step4-completion.md IN FULL, now, before any other tool call. Then local/handoff/2026-09-06-portal-step3-exhaustive-handoff.md §0 — twelve rows of things your context may assert that are false.

We are on step 3 of four. Nine bucket-A items are closed; nine remain and they are listed in the plan's §2.4 with an ownership column. Six need nothing from me.

THE UNIT OF WORK IS THE FILED ITEM. Never the realm, never the component. Three plans were retired on 2026-09-06 and a per-realm loop is the first of them — linksee anchor #7 records it. Batch items that share a file or a verify pass; nine separate open-fix-verify-commit cycles is that loop wearing a different label.

Before any pop-up asking me to decide: ctx_search the decision ledger for that surface. Four settled items were put to me as live work in one day. A filed item is a claim with a Verify condition, and its Verify line is usually what refutes it.

Before filing a portal defect: check the mockup. Two of three Home P1s turned out to be the portal matching the design byte-for-byte.

Verify in the browser, not by assertion. Force a fresh document — the harness caches hard. Gate every commit on a command's own exit status, never on a search finding a line.

Do not run the suite until something is about to be pushed. Do not write mid-run summaries. Mega-batch: independent calls in one message, multi-site edits in one python heredoc with the gate chained on.

Start with the four Export drawers — one fix in exportPanel.js lands on four realms.
```

## 2 · Why each line is in it

| Line | The failure it prevents |
|---|---|
| Read the plan first, then §0 | A compacted context asserts twelve things that are now false, including that pixel conformance is the target |
| The unit is the filed item | V1 sequenced five steps per realm across seven realms at 100–175 turns and was killed; a status line reading *one surface out of seven* revived it in new words hours later |
| `ctx_search` the ledger before a pop-up | The category-accent row had said **do not re-derive this** for two days when it was put to Harkirat as an open fork |
| Check the mockup before filing | `home.js:62` and `app.css:3434-3438` are byte-identical to the package; both were filed as portal failures without looking |
| Verify in the browser, force a fresh document | A CSS fix landed on the wrong of two identical selectors, every gate stayed green, and only `getComputedStyle` caught it |
| Gate on the exit status | `rg '^exit=' log && git commit` runs when the log says `exit=1`; a commit shipped on a red suite |
| No suite until a push | *"why are you running npm test when your still mid work? are we pushing? no."* |
| No mid-run summaries | Objected to four times in one session |
| Mega-batch | *"why are you wasting turns on sequential turns?"* |
| Start with the Export drawers | One `exportPanel.js` fix moves four realms' numbers — the clearest demonstration that the unit is the item, not the realm |

## 3 · What the next session must NOT do

- **Do not run `critique` on another realm.** It is discovery, and discovery has never been the bottleneck — a 94-item backlog already existed when two separate plans proposed generating more findings.
- **Do not re-run the supersession audit.** Finished; verdicts are in `docs/archive/resolved-list.md`.
- **Do not re-open the Track lane collapse.** The filed claim counted items; the rule counts rows. Measured and refuted.
- **Do not hand-drain linksee's distill queue.** ~580 rows at 8 per call.
- **Do not re-home the last 21 memories.** They are `consolidate` summaries with no project word; guessing is worse than leaving them.
- **Do not commit `.claude/settings.json`.** The `Stop` hook is deliberately off and uncommitted.
- **Do not push, PR or merge** without approval restated at the moment of the action. PR #186 is open and must not be merged.
