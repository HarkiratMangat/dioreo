---
kind: archive
status: dead
---

# 🪦 Graveyard — resolved & filed intake from the notes scratchpad

**Archive. Not active content. Do not read this file by default.**

Where resolved and filed intake from **`docs/diors-builds notes.md`** comes to rest. Split out of that file into `docs/archive/` on **2026-07-25 21:43 EDT** (Harkirat's ask) — it used to be a `# Graveyard` section living at the bottom of the notes file, where it was pure context bloat: a dead archive riding along in every read of an active scratchpad. The notes file now ends at its working sections and points here instead.

## The sweep rule (unchanged, only the destination moved)
- An item is swept out of the notes file's working sections once it is **marked** (`[x] ✓` shipped / `[x] ✗` abandoned) **AND** carries **Harkirat's own confirmation mark**. Claude's ✓/✗ alone is not proof he has seen the answer — without his mark an item stays in the working sections indefinitely, however many sessions pass. (His rule, 2026-07-18.)
- Sweep on the **next** session's read, never the same session an item was marked — marks stay visible in-context for one cycle first.
- Don't run the sweep more than once per session.
- Items moved here keep their original wording, marks, dates, and comments verbatim. **Never delete — always move.** Append new sweeps as a dated `### 🗂️ YYYY-MM-DD sweep` block, newest at the bottom.
- Feature ideas that got **filed** into their real home (`docs/ROADMAP.md`, a `.claude/rules/*.md`, `docs/db-deferred-list.md`) simply leave the notes file — the destination is their home now, not here. Only a *filing ledger* recording where they went belongs here.

## Its sibling in this folder
`docs/archive/resolved-list.md` — the same idea for **`docs/db-deferred-list.md`**: resolved, shipped, and dropped deferred-list entries move there. Two archives, deliberately separate, so it stays obvious which active file a given dead item came out of.

*Also in this folder: `diors-builds notes 2026-07-18 (pre-tidy).md`, a dated pre-tidy snapshot from before the notes file was tracked in git. Largely superseded by `git log` now.*

---

## 🗂️ Sweeps & filing ledgers (newest last)

### 🗂️ 2026-07-21 sweep (ℋ-confirmed items moved from the working sections — overdue, they had carried Harkirat’s confirmation since 2026-07-18)
- [x] ℋ ✓ (2026-07-18) ~~Also I’ve noticed there’s no longer the v2, v3, etc sections in this file. Can you explain/help me understand how you sorted/formatted things during your cleanup of the file? And how I should write future notes or thoughts directed specifically at those versions? Im not mad, im genuinely wondering. Like did you have a better method in mind? Do I just dump them into a collective place and you then use your reasoning to sort them into their appropriate sections wherever you have them stored? And you’d also create and modify sections to fit that collective dump of thoughts, or confirm/ask me, or use something I stated/mentioned in the thought, etc? Just want us to both be on the same page. :)~~
  <!-- ANSWERED 2026-07-18 (Claude): the whole v2–v5 list used to be duplicated here AND in CLAUDE.md, and the two drifted apart over time — this file's copy was the bigger driver of the "endless file" problem you flagged, so the 2026-07-18 tidy deleted the duplicate and left CLAUDE.md's "Next planned work" as the ONE place it lives (mirrored into both CHANGELOG roadmap sections, never this file). Going forward: just keep dumping version-specific thoughts here as plain intake in Questions/Notes or General/Meta — you don't need to sort them by version yourself. On a tidy pass I read CLAUDE.md's existing version buckets and place each thought into whichever one it actually fits, creating or modifying a bucket if none fits, or asking you directly if it's genuinely unclear which version something belongs to — same judgment call I already make for feature ideas today, just applied to version-tagging too. -->
- [x] ℋ ✓ (2026-07-18) ~~maybe 𓉸 or ✴︎ or ⚡︎ or ⌯⌲ or 𝓓 or ℋ or ⟢ for the confirmation/acknowledgement symbol? Or do you suggest something else?~~
  <!-- RESOLVED 2026-07-18 (Claude): went through several rounds against a growing artifact (7→9→15 candidate symbols/colors, with size/contrast/hue-sorted views) before landing on the final set now live in the 🔑 Legend above and in the real MarkEdit menu: 4 shortlisted symbols (✴︎ ✦ ◆ ℋ), 8 final colors. All switchable, not narrowed to one fixed pick — matches how the extension was actually built (a live Default Symbol / Default Color menu, not hardcoded values). -->

### 🗂️ 2026-07-18 filing ledger (moved out of the working sections into their real homes — nothing lost)
- **Filed to CLAUDE.md "Next planned work" + both CHANGELOG roadmaps:** `/manage` loadout data-entry UX overhaul (old L55/L73 + image-naming half of L59) · reword `/manage` `section`→`data for` (L56) · richer in-bot diagnostic logging (L57) · admin `/status` command (L60) · `/manage` per-page accent colors (L61) · announcement feature (L64, v3) · easy bot sharing / `/invite` (L58, v3) · user-friendly bot/ops guide (L34, someday) · verify Cloudinary folder organization (L59, Known-issues) · webhook alerting heavy-half added to the changelog roadmap for parity (L72 — already in deferred-items + the Deployment section).
- **Already-synced roadmap items that used to be duplicated here** (v2 batch 1&2, all of v3/v4/v5): removed from this file — they were already in CLAUDE.md, so keeping a second copy here was pure drift risk.
- The full pre-tidy file (including every one of the above in its original wording) is preserved at `docs/archive/diors-builds notes 2026-07-18 (pre-tidy).md` — written here as `local/notes-archive/…` at the time; the notes file and its snapshots moved to tracked `docs/` on 2026-07-18, and `docs/notes-archive/` was renamed `docs/archive/` on 2026-07-25 21:43 EDT.

---

## 📦 Earliest swept intake (pre-2026-07-18, grouped by the notes section it came out of)
*Swept before the dated-sweep convention existed, so these carry no sweep block of their own. Their own inline dates (2026-07-15 → 2026-07-17) are the real record.*

### Questions/Notes for Claude

- [x] ✓ ~~The Perf finding — explain it simply, why we have the current method, the tradeoff of reworking it, and why it wasn't flagged during yesterday's efficiency/CPU pass.~~ <!-- ANSWERED 2026-07-15: pagination does 2 network hops (deferUpdate, then a separate patch to update the message); the alternative is a single UPDATE_MESSAGE (1 hop, snappier). Agreed HYBRID: light string-building commands → single-hop; heavy/attachment paths (View Colors, ffmpeg, k-means) → keep defer-then-patch, where blowing the 3s ACK is a real risk. Not flagged yesterday because that pass hunted the View Colors CPU/freeze bug, not baseline click latency — a different question. Full writeup in CLAUDE.md "Known open issues". -->
- [x] ✓ ~~Can a user-installed bot fetch *another* user's colors if I add a user-select option? Can it see all members of a server/GC without being a guild bot?~~ <!-- ANSWERED 2026-07-15: (1) Fetch another user by ID — YES, GET /users/{id} is global, no shared guild needed; avatar/banner/display_name_styles/nameplate/deco all return. (2) Get the ID via an .addUserOption() picker — very likely YES (client-side picker, no member-list access) but confirm at build. (3) Enumerate ALL members — NO, needs the privileged GUILD_MEMBERS intent + real guild membership (the 50001 wall). You don't need enumeration; the picker sidesteps it. Note: tweak the header wording when a user pulls someone else's colors instead of their own. -->
- [x] ✓ ~~Do we need to change any bot settings on the Discord Dev Portal? Any permission changes?~~ <!-- ANSWERED 2026-07-15: v2 — none. v3 — none. v4 (guild install + text commands) — YES: enable Guild Install, add setIntegrationTypes([0,1]), enable the privileged MESSAGE CONTENT intent (Discord approval past 100 servers), and real guild membership w/ View Channel + Send Messages. v5 — none (Cloudinary-side). -->

### General / Meta

- [x] ✓ ~~Stop repeating reminders I already know and have documented~~ (e.g. "I can't boot the bot locally without racing the live instance"). Only worth restating if I haven't touched the bot in weeks/months. <!-- DONE 2026-07-15: filed into feedback_be_usage_conscious — drop caveats that just restate my own documented rules; keep only the useful "what was/wasn't verified" residue. -->
- [x] ✓ ~~Recommend ONE effort level, not a range~~ (e.g. "Opus 4.8 medium", not "medium-high"). <!-- DONE 2026-07-15: feedback_suggest_model_switch — one model + one effort; tie-break to the lower level. -->
- [x] ✓ ~~On a big session-opening batch, auto-suggest the best model+effort for the session, and flag which tasks to defer to their own session (with each one's own model+effort, so I gauge the workload).~~ <!-- DONE 2026-07-15: "session-start batch triage" in feedback_suggest_model_switch. -->
- [x] ✓ ~~Show the active model+effort in the session so I don't accidentally run on the wrong one.~~ <!-- SOLVED 2026-07-15: not a hook (nothing can set the CURRENT session's title, no model-change hook exists). Instead a /rename convention — I hand you a ready-to-paste string at session start: `[HOLD/MonDD] Model<Ver>-<Effort> · Title · Mon DD` (e.g. `Opus4.8-M · Central notes intake · Jul 17`). The desktop picker is global/live and can't remember per-session; the title can. Spec in feedback_suggest_model_switch. -->
- [x] ✓ ~~reword the description for this section. While yea not explicitly bot features, this section can include features that affect other projects or might be directed towards a different project.~~ <!-- DONE 2026-07-17: reworded the section description above per this request. -->
- [x] ✓ ~~we implemented git tags to the GitHub commits but I think some of them might be "off/incorrect", we need to verify those. I guess that's part of the "backfill git tags" task that was deferred during that particular session.~~ <!-- VERIFIED 2026-07-17: all 6 tags (v2.17.3→v2.19.0) map correctly to their CHANGELOG commits (multi-commit pushes tagged on the LAST commit). Nothing off. Pre-v2.17.3 remains deliberately untagged (no clean 1:1 mapping) = the still-open 'backfill' task. --><!-- UPDATE 2026-07-21: BACKFILL NOW COMPLETE — every version v1.0.0→v2.30.2 is tagged (58 tags, zero gaps). The "no clean 1:1 mapping" reason turned out false: nearly every pre-v2.17.3 CHANGELOG entry already cited its own commit hash; the 2 that didn't (v2.6.0/v2.7.0) were resolved from the git log, and the whole mapping was verified monotonic before pushing. See CLAUDE.md's version-tagging note. This task is fully done. -->
- [x] ✓ ~~also do you want to reference/link that deferral .md into this notes file? … wait scratch that, let's keep it separate.~~ <!-- RESOLVED by Harkirat inline (~Jul 17): keep deferred-items.md SEPARATE, not merged/linked into this file. -->
- [x] ✓ ~~also, regarding the deferral list file, if you keep it separated, tidy it up like how you did here. Adjust the design/structure to best fit its own situation.~~ <!-- DONE 2026-07-17: restructured deferred-items.md — 🐞 Active Bugs at top + Reminders/per-project(with model+effort)/Someday/Resolved grouping. -->
- [x] ✓ ~~can we implement a way where, for items deferred to a new session, at the end you give me a solid start prompt + title for that planned session, so it's easier to continue seamlessly. Use judgement on whether a deferral warrants a full handoff prompt vs just keeping it pending.~~ <!-- PRACTICED / standing behavior (feedback_session_handoff_prompts): end-of-session handoff prompts for deferred work — e.g. the alerting IDs/log handoff. -->

### Resolved intake — bugs & one-offs

- [x] ✓ ~~Tried to edit an MP loadout via /manage → Edit; searched "FSS", clicked Edit on the ephemeral prompt, got "Dior's Builds didn't respond in time". Adding a new FSS Hurricane loadout first didn't help — same error.~~ <!-- FIXED 2026-07-17 (v2.20.0): the mng_editbtn_ button handler was misplaced in the isModalSubmit() block → dead code → no ACK → timeout. Moved to isButton(); broke Edit for ALL entities, all fixed. Needs one live re-click after the deploy to confirm end-to-end. See CLAUDE.md "SEQUEL BUG" note. -->
- [x] ✓ ~~why are the Edit and Search Again buttons in 2 separate rows? Can't you put them inline as 1 row?~~ <!-- DONE 2026-07-17 (v2.20.0): single-match Edit prompt now puts Edit + Search Again in ONE row. Multi-match keeps 2 rows (a select menu must be its own row, can't sit beside a button). -->
- [x] ✗ ~~I think the bulk replace button for MP loadouts just opens the bulk add modal — both open the same modal.~~ <!-- NOT A BUG (2026-07-17): intentional & documented in CLAUDE.md. Loadouts' "Replace Multiple" is a deliberate placeholder that routes into the same upsert modal as "Add Multiple" until the real "search + multi-select" admin flow is built (on the deferred list). The upsert already covers replace semantics. Marked abandoned-as-stated. -->

### 🗂️ 2026-08-03 sweep (ℋ-confirmed items overdue since the 2026-07-25 21:43 EDT tidy — a full housekeeping pass, general scope not just this file)
*Every item below carried Harkirat's ℋ confirmation mark on disk before this session started (added live in MarkEdit across several sessions since 2026-07-25). Moved verbatim, oldest-in-file-order first. The `📋 SESSION STATUS #1–#7` blocks that used to sit between these items in the working file are included here too — they're historical narrative whose subject items are now all resolved/filed, so they move as a block rather than getting individually re-homed.*

- [x] ℋ ✓ (2026-07-18) ~~okay I like this new coloured check/cross system. But I do have another thought to expand on our workflow, I currently have it mentioned that you should move completed items to the graveyard on a new session’s read of this file. Honestly, that can have its own issues. Like what if I hadn’t read your comments/answers about a question I had asked and you had answered inline in this file? So we need a proper confirmation system before deciding what to do with the item. First, let’s create a “legend” area at the top to collectively breakdown and explain our formatting design and setup (please dont be lazy about it and use your proper judgement for creating this area and what to include within it. 2 things I know for sure should be included are what ✓ and ✗ represent/mean). Let’s also add another symbol that would act as my confirmation towards you from me that I’ve acknowledged and read the item and you’re free to sort it as you’d like. Idk what symbol but make it a different color than green, red, white; orange/color maybe? Since you added these using markedit’s api, can also implement the confirmation thing into the Extensions tab in the app’s menu bar, as well as a toolbar button that I can install (I believe you can use the settings package or something to do a workaround to create toolbar buttons for markedit. Look at the direct-preview extension’s .js file, they implemented a toolbar button)? This was I can have a simple button that would select an item and append the ‘confirmed’ symbol to it. Can you change the shade of green/red for the check/cross? Something a bit more vibrant or brighter so something it’s looks nicer on this #1f1f24 background color (better contrast basically. Cuz rn the Red Cross kind of appears dull due to the background color).~~
  <!-- FULLY BUILT 2026-07-18 (Claude): everything asked for here shipped. Legend section added above (this file). Confirmation-mark system built as a real MarkEdit extension (`editor.js` + `scripts/markedit-dior-confirm.js`, NOT in this git repo — see `reference_markedit_extension_api` memory for exact file paths) with a full "Confirmation Mark" menu: insert at cursor, insert at bullet front, insert check/cross, bulk "Update All Existing Marks", Default Symbol (4 shortlisted: ✴︎ ✦ ◆ ℋ) and Default Color (8 final: amber/orange/pink/violet/periwinkle/cobalt blue/cyan/turquoise) submenus with a live checkmark-style indicator on the current pick, and a one-time toolbar-button installer. ✓/✗ colors brightened to #3fb950/#f85149 (GitHub's own dark-theme status colors). Confirmed working live by Harkirat after 2 rounds of real bug fixes (toolbar-button routing mismatch, symbols only coloring once a full [x]...✓ context existed) — see DEVLOG for the full build/debug narrative. One tried-and-abandoned piece: a colored swatch icon next to each color pick in the menu — tested 5 guessed field names (iconColor/tint/color/symbolColor/hierarchicalColor) against a real circle.fill icon, none tinted it, gave up rather than keep guessing; the hex is still visible in every item's own title as a substitute. -->

- [x] ℋ ✓ (2026-07-18) ~~a note regarding how you type your inline or block comments, can you type them as 1 line? Markedit auto wraps the line. This was it’s just better formatted. You seem to be using a new line for each part of your sentence and that causes parts of it to randomly jump to the next line. If you actually list bullet points as part of the commit, those would be fine in a new line. But keep a paragraph or sentence together and just let the text wrap handle the formatting instead of you manually dividing it up.~~
  <!-- ADOPTED 2026-07-18 (Claude): agreed, and applied immediately — every comment I write here from now on stays one continuous physical line, letting MarkEdit's own soft-wrap handle display instead of me hard-breaking it, with a real bulleted list as the one exception (those stay one bullet per line, per your own carve-out). Reformatted the one existing offender in this file (the Cloudinary/FSS answer above) to match. -->

- [x] ℋ ✓ (2026-07-18) ~~I like you including the date in your comments about when something was resolved, implemented, discussed, etc etc. but can you also add a timestamp on when you marked something as ✓ and ✗, beside the symbols? This date would literally be when you marked that item off, while your comment date would be when the action was actually taken, such as if we had already built 4 days ago, or like if something was answered last week, or if something was resolved today, etc etc.~~
  <!-- DONE 2026-07-18 (Claude): added to the 🔑 Legend above — a `(YYYY-MM-DD)` right after the ✓/✗ mark is now the mark-date (when I actually checked the item off), kept separate from whatever date already lives inside the item's own comment (when the real-world thing happened). Applying it on every new mark going forward; not backfilling it onto already-marked Graveyard items since their existing comment dates already cover the same information for anything that old. -->
- [x] ℋ ✓ (2026-07-18) ~~we need a solid clarification of exactly what “document" means for our workflow, what “commit” means, what “push” means, and what “deploy” means.~~
  <!-- DONE 2026-07-18 (Claude): rewrote docs/SESSION-START.md's NON-NEGOTIABLES section into a real glossary — commit = local git only; push = code reaches GitHub, the live bot is untouched; deploy = the VM actually pulls + restarts, the step that goes live; document = syncing CLAUDE.md/changelogs/DEVLOG/memory/this file. The default assumption unless told otherwise is still that "push" as a spoken instruction means the whole chain (commit → document → push → deploy → verify) — v2.22.0 tonight is the exact case that exposed the old wording as wrong, since we explicitly held the deploy after pushing. Synced into user_working_agreement.md too so the two copies don't drift. -->
- [x] ℋ ✓ (2026-07-20) ~~I was planning a cloudinary and loadout “automation” system: like my flow as im thinking... screenshot > auto extract weapon name, code, 5 attachments using an OCR engine such as PaddleOCR or apple vision framework > auto rename file in format of WEAPON-NAME-1 (where weapon name is in all caps and if a space in the name, such as FSS Hurricane, would be turned into a dash, and then a -1 is added at the end. if another build for that same weapon is also provided or exists, the additional build would become -2. so like FSS-HURRICANE-1, FSS-HURRICANE-2.). That would also be our “image key” or whatever to link it back to cloudinary. > auto embed the extracted name, code, and attachments as metadata into a json file of some sort, which can be stored in mangodb database > some auto method of uploading that screenshot to cloudinary in it’s appropriate asset folder > auto populate structured metadata field in cloudinary for that image. This way, when I want to add a new loadout, I simply have to provide the screenshot, either from my phone or as an existing URL from somewhere else, into a modal in the bot on discord, and a script or something in the backend could go thru the automation system and poof the loadout would be live in the bot. A simple near 1 click method to extract, save, classify, rename, link, and create the loadout within the bot. No more having to manually extract and type each attachment, the weapon names, the code, and manually uploading to cloudinary and manually having to pre-name the file, etc. — what would be the most efficient way of doing this above note? I’m so open to your thoughts and suggestions and hoping you can actually look at the practicality of this, along with do some deep thinking about it to maybe correct me or improve my idea or adjust it or change it or deny it, or whatever.~~
  <!-- DESIGNED (not yet built) 2026-07-20 (Claude): full design worked through and captured in CLAUDE.md's new "Loadout automation (screenshot → live loadout)" roadmap section — corrected two parts of the original idea: use an LLM vision call (Gemini, since Claude Pro doesn't cover API billing and you already have unused GCP credits + Gemini has its own free tier) instead of a hosted OCR engine, since structured field extraction needs semantic understanding a raw OCR+layout-heuristic approach doesn't hold up well against season-to-season UI changes; and submission goes through a slash-command attachment option, not a modal (modals can't carry file uploads at all, confirmed against your own emoji-bot screenshot). Kept your own fuzzy-match-attachments idea and the Number-Letter Gunsmith-code structural corrector as-is — both are the right pre-review safety nets. Marked as a standalone proof-of-concept slash command first, full /manage integration later. Build itself deferred to its own dedicated future session. -->
- [x] ℋ ✓ (2026-08-03 17:45 EDT) ~~I think you messed up something while working on the extensions because every time I press Return on my Mac, it adds a blank line between my bullet points. It didn’t do that before. Can you check the settings for Markedit?~~
  <!-- FILED 2026-07-28 01:41 EDT (Claude): already tracked as a confirmed bug in `/Applications/Claude Code/meta-deferred-list.md`'s 🐞 Active Bugs section (the MarkEdit-extension cluster), which is the right home — it's editor tooling outside every repo, even though it only reproduces in this file. Not actionable from a Diors session: the extension source (`editor.js` / `scripts/markedit-dior-confirm.js`) lives in MarkEdit's own extensions folder, not this repo, so it needs a dedicated MarkEdit-extension session. Leaving unmarked here because it is still genuinely open — filing it is not fixing it. --> ※ [2026-07-29 09:32 EDT] Check out `'/Applications/Claude Code/local/CleanShot 2026-07-29 at 09.30.35.gif’`. I made a gif of a short screen recording of the bug. You can even see in the screen recording exact where I click and the keyboard key(s) pressed. Notice how I press “return” on my mac’s keyboard *once* yet it adds 2 lines? ※ ∴ [2026-07-29 18:24 EDT] Got the GIF, thank you — and to answer the question you asked in chat: **no, the comment below this is NOT my reply to you.** It is dated 2026-07-20 and has been sitting there for nine days; that is exactly why you couldn't tell. My actual action on your follow-up was filing the recording into meta-deferred-list.md as proper repro evidence, and I left no signal here at all, which is the failure you're pointing at. Your "one Return, two lines" observation is the useful part and I recorded it as such: it reframes the bug from "an extra blank line appears" to "the Return handler is firing twice," which points at a duplicate keymap binding rather than at list-continuation logic — a materially different place to start looking. The bug itself is still open and still not fixable from a Diors session (the extension source lives in MarkEdit's own folder), so it stays unmarked. Point taken on the mechanism, too: from now on, when I act on one of your follow-ups I reply with this mark and say where the detail is, rather than leaving a past-dated comment to speak for itself. ∵ ∴ [2026-08-03 17:45 EDT] Root-caused and fixed. Your GIF's framing was exactly right ("one Return, two lines") but the duplicate-keymap theory wasn't — I moved every custom extension file out of MarkEdit and the bug reproduced in vanilla MarkEdit, so it was never anything in editor.js/case-tools.js/the confirm or follow-up extensions. Real trigger, confirmed live: pressing Return at the end of a line that's ENTIRELY an HTML comment (like the ones right below your bullets here) double-inserts a blank line — native MarkEdit list-continuation behavior, not a Dior's-Builds bug. Added a Return-key intercept to editor.js that catches this specific case and inserts exactly one line itself. Live-verified working. Full writeup in meta-deferred-list.md's Resolved section. ∵
  <!-- DISCUSSED + DEFERRED, recorded late 2026-07-20 (Claude): this was raised and talked through in a PRIOR session where Harkirat said it's not a priority and chose to defer it — but that session never annotated it here or filed it anywhere, so it wrongly resurfaced as "genuinely new" on the 2026-07-20 read (my error too — I should not have called it new with no basis). Now filed to /Applications/Claude Code/meta-deferred-list.md → 🐞 Active Bugs as a [MarkEdit ext] `[P3 · S]` confirmed bug with a repro, so it can't get lost again. Suspected side-effect of the 2026-07-18 confirm-mark extension work (editor.js / scripts/markedit-dior-confirm.js in MarkEdit's OWN extensions folder, NOT this bot repo) — that's when the Return→blank-line-between-bullets behavior reportedly started; needs a dedicated MarkEdit-extension session to investigate the CodeMirror keymap/input handling it added. Left UNCHECKED on purpose: deferred-but-open, not resolved. -->
- [x] ℋ ✓ (2026-07-21) ~~Is it safe for me to go to the discord dev portal and change the bot name to add (Beta) at the end of it? Will that affect anything within the code or am I safe?~~ <!-- ANSWERED 2026-07-21 (Claude): totally safe. The bot's display NAME is purely cosmetic — nothing in the code keys off it. Commands are registered/routed by the Application ID, and the bot authenticates with its token, neither of which changes when you rename it. Slash-command names, custom_ids, mentions, everything keeps working. (The only thing a rename touches is what users SEE as the bot's name.) -->
<!-- • FILED (feature idea, → CLAUDE.md v3 loadout roadmap): optional paginated multi-weapon loadout view with thumbnails + category switching; handle multi-build weapons by showing the "recommended" build. Overlaps the v3 /meta + /loadout consolidation — reconcile there. Recorded in STATUS #4 + the filing ledger. --> ※ [2026-07-24 17:50 EDT] When you create bullet comments, like these, they should NOT be using proper - bullet points. The way you should be implementing them is as: <!-- • FILED  -->. The bullet for a comment lives inside the comment, not outside of it. ※ ※ [2026-07-24 17:51 EDT] This would have been a perfect place for you to use *your* follow up mark once you read/understood to acknowledge my follow-up. Because a comment is more for answering the initial bullet point itself, while the follow-up would be questions and stuff directed towards me or continuing the conversation to one of my follow-ups which were directed towards you, yk? ※
<!-- • FILED (feature idea, → CLAUDE.md loadout-badges roadmap): a "Recommended" badge for weapons with multiple build variants; the recommended build always renders first in pagination order. -->
<!-- • FILED (feature idea, → CLAUDE.md loadout-badges roadmap): a "Troll Build" badge for joke builds. Emoji provided: <a:TrollBuild:1529192136208154725>. -->
<!-- • FILED (MarkEdit ext bug, → meta-deferred-list.md): "line skip" (blank line between bullets on Return) happens ONLY in this file, not a fresh file — reinforces the L52 confirm-mark-extension root-cause theory; filed alongside L52 so they're investigated together. -->
<!-- • FILED (MarkEdit ext request, → meta-deferred-list.md): auto-insert a space after inserting a confirmation mark. -->
<!-- • FILED (MarkEdit ext bug, → meta-deferred-list.md): inserting a mark before ✓ then adding a space glitches the space to BEFORE the mark ([x]<2 spaces>ℋ✓); likely same root cause as the line-skip bug; also check the separate case-tools.js extension (Harkirat's own, in his MarkEdit scripts folder, NOT this repo). -->
<!-- • FILED (infra idea, → CLAUDE.md roadmap, own design session): partial "hot-reload" so a pushed file's code goes live without a full VM redeploy each time. -->
- [x] ℋ ✓ (2026-07-21) ~~I also want a way for me to provide/revoke access to other selective users to some or all admin level commands. Would it be fine storing that info in mongodb or is it better to hardcode the user IDs within the code itself?~~ <!-- ANSWERED + FILED 2026-07-21 (Claude): store it in MongoDB, not hardcoded — a Mongo-backed allowlist is editable at runtime (grant/revoke without a code change + redeploy), which is the whole point of "provide/revoke selective access." Hardcoding means every access change is a code edit + full deploy. Filed as a v3/v4 admin feature in CLAUDE.md. (The current single-admin `ALLOWED_ADMIN_ID` const stays as the ultimate owner; the Mongo allowlist would layer on top.) -->
<!-- • FILED (autobuild follow-up, → CLAUDE.md /autobuild "still open"): add an ephemeral/hidden toggle to /autobuild. -->
<!-- • FILED (autobuild follow-up, → CLAUDE.md /autobuild "still open"): bulk /autobuild PoC — /autobuild amount:{single,multiple}; "multiple" opens a modal taking one build per line (`<category> | <badge(s)> | URL`, category/badges optional), runs the vision pipeline over each, shows a paginated review panel with per-page Edit/Confirm/Cancel plus Confirm All / Cancel All, and an "open loadout" dropdown on the all-confirmed message. -->
- [x] ℋ ✓ (2026-07-21) ~~Note for the future, we’ll rename this command to something like /admin autobuild or /manage autobuild or maybe something else since we have some notes about restructuring that whole admin/management flow later on.~~ <!-- NOTED 2026-07-21 (Claude): already captured — the /manage → /admin restructure is in CLAUDE.md's v3 roadmap, and /autobuild folding into that admin surface is part of the same plan. No separate action needed; it'll be reconciled when the /admin restructure is designed. -->
<!-- 📋 2026-07-21 SESSION STATUS (Claude) — disposition of the L66–L75 intake below, recorded IN-FILE so a future session KNOWS these were read and handled. This directly fixes the "acknowledged in chat but never marked here" gap the notes-acknowledgement item raises: a chat-only acknowledgement is invisible to the next session, so from now on reading/acting on notes gets annotated HERE, not just in chat. Items aren't inline-checked/swept yet (that's a tidy-pass action) — this block is the interim record. -->
<!-- • SHIPPED v2.29.0 (commit 7960454, deployed live to the VM): autobuild weapon-SKIN-name strip + ALL-CAPS casing, restricted-slot skip, canonical attachment order; L-CAR-9-2 ↔ CROSSBOW-1 image swap (verified by etag); J358 "Trigger Action" removed; 3 autobuild test builds deleted; Striker "Fast Reload Reload Case" confirmed correct (no change); /manage "How Images Work" text refreshed for autobuild. -->
<!-- • ANSWERED in chat this session: the duplicate-detection question (a build 1 attachment different is NOT blocked — the dupe filter is advisory only, and needs code-identical + at least 4/5 attachments, OR code within 2 edits + all 5 matching) and the "LOCUS-type partials are stored-data typos" clarification. -->
<!-- • DEFERRED (read + filed, not lost): L61 partial-refresh/hot-reload (own design session); L62 admin provide/revoke (Harkirat deferred earlier); L63/L64 autobuild ephemeral toggle + bulk PoC (bundle with the autobuild test session); L68 Cloudinary quality (q_auto is already adaptive ~75-85%, not 100% — a q_auto:eco / q_80 change is a quick bot-wide follow-up); L72 DMZ full-slot vision (blocked on the DMZ slot-layout teaching below); L74 Advanced-Legendary UI redesign (mockup in local/advanced leggy_format.json) + L75 full-caps draw headings (one draw-prices visual session); bot-wide reorder of EXISTING builds' attachments into canonical slot order. -->
<!-- • ALSO DONE 2026-07-21 (Harkirat gave ground truth, pure Cloudinary/Mongo data swaps, no deploy): L73 remaining crossed-image bugs fixed — 3-LINE RIFLE builds 1↔2, TYPE 19 DMZ builds 1↔2 (MP were fine), LW3-TUNDRA build 1 (2F4D5A8A9E) ↔ build 3 (1A2F5A8A9E). Verified via distinct etags + fresh versions. L68 Cloudinary quality: SCRATCHED (q_auto already in the URLs, Harkirat's call). --><!-- (kept on one line per the file's comment rule) -->
<!-- • STILL NEEDS HARKIRAT INPUT: L66 proper /manage attachment→per-slot-metadata fix — DEFERRED, Harkirat wants a fuller explanation of the fix when we actually tackle it (the autobuild EDIT path is already handled safely; /manage needs either a slot input or slots stored in Mongo — a design call); DMZ slot-layout teaching (Harkirat will provide in a future session — send one DMZ screenshot with EMPTY slots so labels show, or list the fixed slot positions top-to-bottom). -->
<!-- 📋 2026-07-21 SESSION STATUS #2 (Claude, draw-prices visual session — SEPARATE from the autobuild session above) — SHIPPED v2.30.0, deployed live to the VM: L74 Advanced Double Legendary page rebuilt to the mockup (local/advanced leggy_format.json; 30 CP region cloned from the same design with its own derived numbers) + L75 all draw-prices entry headings full-caps. Both marked [x] ✓ inline below. NOT handled this session (read but out of scope — they're autobuild-domain, left as intake for the autobuild follow-up session): the /manage attachment→slot fix (L72 above), and the new re-asks at the bottom of this intake block (Cloudinary 80/90% quality, dupe-detection elaboration, L-CAR/CROSSBOW explanation, LOCUS-partials clarification, DMZ full-slot vision, the 10 bulk-vision partials). These already have prior-session answers/dispositions in the blocks above — the autobuild session should reconcile the re-asks against those rather than re-deriving. -->
- [x] ℋ ✓ (2026-07-24 18:07 EDT) ~~Your note about "**Accepted gap:** editing a build's *attachments* via `/manage` doesn't update the per-slot fields (only `/autobuild` has the slot mapping) — old slot values are left as-is, not wiped.” I’d actually prefer to patch this up with a proper fix so it isn’t a gap.~~
  <!-- FIXED 2026-07-24 18:07 EDT (Claude): built the approach that was already decided 2026-07-21 (see .claude/rules/autobuild.md) — `Loadout` gained a real `attachmentSlots` field (models/Loadout.js), populated by /autobuild's writeLoadoutDoc on every new build AND backfilled onto pre-existing builds by scripts/backfillLoadoutSlots.js (so already-vision-processed loadouts benefit too, not just new ones going forward). index.js's edit_loadout_ now keeps the stored slots (and actually re-syncs the real Muzzle/Barrel/etc. Cloudinary fields) when your edited attachment list is byte-for-byte unchanged from what's stored — the common "just fixing a typo or badge" edit — and clears them when the attachment list genuinely changes content or order, since slot identity can't be safely carried forward onto a different attachment set without re-running vision (same rule /autobuild's own Edit path already used). Syntax-verified (node -c on all 4 touched files); not yet live-tested against a real /manage edit in Discord — worth one real click-through after this deploys. Full detail in .claude/rules/loadout-images-and-metadata.md and autobuild.md. -->
- [x] ℋ ✓ (2026-07-21, new session) ~~Also, during the july 21 Autobuild bugfixes + Cloudinary metadata + Advanced Legendary draw entry session, I asked you to look at the new entries ("Hey, never mind on the admin whitelist for 455506392544182272. I want you to do something instead. I added some notes in my notes file, please go look at those new entries.") I had made in this notes file. You acknowledged the read in the session chat ("**On the new notes entries** — I read them (L52–L64). They're future intake (autobuild ephemeral toggle, bulk `/autobuild`, the provide/revoke admin-access system that made you drop the whitelist ask, a few MarkEdit bugs, partial-refresh deploys). I left them as intake rather than filing — that's a tidy-pass action, and I didn't want to touch your workflow without you asking. Flag me when you want them filed into the roadmap.") but you did nothing to showcase it here in this file. Preciously, we also had this same issue and we had explicitly discussed it. Like how would a future session of yours know that you’ve read these notes? Please review that memory and figure out a way to resolve this ignorance mishap.~~
  <!-- RESOLVED 2026-07-21, new session (Claude): the mechanism is now real and enforced. Whenever a session reads or acts on this file, it writes a `📋 SESSION STATUS` block IN THIS FILE recording the disposition (shipped / answered / filed-to-X / deferred) of every item touched — so a chat-only acknowledgement is never the only record a future session sees. Blocks #1/#2/#3 exist from the 2026-07-21 sessions; this audit session added #4 (at the end of this intake) dispositioning every remaining open item, and marked every item it actually touched. The governing memory was reviewed and sharpened this session (project_central_notes_file + feedback_docs_at_push_time) so the rule can't quietly lapse again. This is exactly the fix you asked for, now backed by both the file's own workflow AND memory, not just intention. -->
- [x] ℋ ✓ (2026-07-24 18:07 EDT) ~~Can you edit the cloudinary links we have utilized inside the bot so they load at 80 or 90% quality instead of 100%? I feel like that would help save quite a bit of loading time for the user if they have slow connection with a very minimal loss in quality? Is that possible without breaking those images in the bot?~~
  <!-- ANSWERED 2026-07-24 18:07 EDT (Claude, re-asked several times — answering directly at the bullet this time instead of only in a SESSION STATUS block): the premise isn't quite right — the in-bot URLs already use `q_auto` (Cloudinary's adaptive quality, roughly 75-85% depending on the image), not 100%. So there's no "100%→80/90%" jump available; a further explicit `q_auto:eco` or a hard `q_80` param would shave a bit more off file size on top of what q_auto already does, safely (Cloudinary handles the transform, nothing breaks), but the marginal savings on top of adaptive compression that's already ~15-25% off full quality is small. Your call whether that's worth a bot-wide URL-builder tweak — say the word and it's a one-line change in buildImageUrl(). -->

- [x] ℋ ✓ (2026-07-21) ~~A question/note regarding our duplicate detection, what if I actually want to add a build thats only 1 attachment difference? Would that still get stopped by the duplication filter? Or would I be safe? Can you elaborate how you actually implemented the filter into code?~~ <!-- ANSWERED 2026-07-21 (Claude) — full answer in SESSION STATUS #4 below. -->
- [x] ℋ ✓ (2026-07-21) ~~Can you correct the "`L-CAR-9-2` and `CROSSBOW-1` images are SWAPPED” data bug?  And explain how it may have happened?~~ <!-- ANSWERED 2026-07-21 (Claude) — full answer in SESSION STATUS #4 below. -->
- [x] ℋ ✓ (2026-07-21) ~~"LOCUS-type partials are stored-data typos”, confused what you meant by this?~~ <!-- ANSWERED 2026-07-21 (Claude) — full answer in SESSION STATUS #4 below. -->
- [x] ℋ ✓ (2026-07-21) ~~Let’s also fix the "10 partials are all pre-existing data bugs the batch surfaced” from the bulk vision call of the loadouts. As well as, "Same-weapon builds with crossed images”, “stored typo: `STRIKER` B1”, "Minor miss: `J358` B1’s”. Let me know which information you want me to verify and provide during the session and I’ll help you resolve know unknowns.~~ <!-- ANSWERED 2026-07-21 (Claude) — full answer in SESSION STATUS #4 below. -->
- [x] ℋ ✓ (Jul 21 — SHIPPED v2.30.0, live on VM) ~~I also want to update the UI and format design of the recently added “Advanced double legendary draw”. See '/Applications/Claude Code/Diors-Builds/local/advanced leggy_format.json’.~~
- [x] ℋ ✓ (Jul 21 — SHIPPED v2.30.0, live on VM) ~~Can you also update all the other draw prices draw headings to be in full caps pls. (Like the lines/headings with the legendary or mythic emoji infront of them).~~
- [x] ℋ ✓ (Jul 21 — SHIPPED v2.30.1, live on VM) ~~Correct the new advanced leggy draw ui; despite you literally getting a hand-made example of it, you messed it up. See ('/Applications/Claude Code/Diors-Builds/local/Screenshots/CleanShot 2026-07-21 at 20.16.48@2x.png’). Remove this extra dividers that should not me there. Edit the strategy heading so it’s exactly: **The Strategy. If You Want...**~~ <!-- Claude 2026-07-21: removed the 3 internal dividers (dividerBefore {1,4,6}) that were never in the mockup, and changed the strategy heading from `### **The Strategy, If You Want...**` to plain-bold `**The Strategy. If You Want...**` (comma→period). Verified via buildContainer() dump: 2 dividers total (title+footer), both regions, totals still derive, ~26 comps. --><!-- UPDATE, new session 2026-07-21 (Claude): this SHIPPED — v2.30.1 pushed + deployed to the VM (commit c419fcf, tagged v2.30.1) and re-verified live (2 dividers both regions, correct heading). The audit session that shipped it also caught + fixed a SEPARATE live crash while verifying the deploy (v2.30.2 — pagination duplicate-custom_id at exactly 2 pages, was breaking /settings /draws /calendar /colors /alerts); both are now live. -->

<!-- • 📋 2026-07-21 SESSION STATUS #3 (Claude, draw-prices correction) — FIXED L83 (the v2.30.0 Advanced Legendary UI mistake), documented in CLAUDE.md + CHANGELOG "Unreleased" as proposed v2.30.1, and committed. Per Harkirat's explicit instruction this was NOT pushed or deployed — v2.30.1 is committed-only and the next session must push+deploy it and verify live. A large audit/organization/backfill/memory-alignment pass was handed off to the next session (see the handoff prompt produced this session). -->
<!-- • 📋 2026-07-21 SESSION STATUS #4 (Claude, the handed-off audit session) — the disposition of everything in this intake block, recorded IN-FILE. SHIPPED this session: v2.30.1 (pushed + deployed + verified live, tagged) and — found while verifying that deploy — v2.30.2, a live pagination crash (duplicate custom_id at exactly 2 pages, was breaking /settings /draws /calendar /colors /alerts). Also this session: CHANGELOG graduated (v2.27–v2.30.2 into the numbered list), CHANGELOG-SUMMARY backfilled so NO version number is skipped, the missing v2.26.0 git tag backfilled, and memory reviewed/sharpened. -->
<!-- • ANSWERED inline this session (marked [x] ✓ above/below): L57 bot-name "(Beta)" — safe, cosmetic, no code impact. L75 Cloudinary quality — images already use q_auto (adaptive ~75-85%, NOT 100%); a further q_auto:eco / q_80 is an optional bot-wide tweak, Harkirat's call. L76 dupe detection — advisory only, NEVER blocks Confirm; fires on code-identical + ≥4/5 attachments OR code within 2 edits + all 5 (so a genuine 1-attachment-different variant is fine, especially with a different code). L78 "LOCUS-type partials" — not backfill failures: the slot-backfill matcher declined to write slot metadata when vision's read didn't match the AUTHORITATIVE stored attachment names, which surfaced pre-existing stored-data problems (image swaps / crossed attachments / typos) rather than corrupting them. L77 L-CAR-9-2 ↔ CROSSBOW-1 — already fixed + live in v2.29.0 (bytes swapped back, verified by etag; likely caused by the 2026-07-19 manual re-upload landing under the wrong key). L80 the 10 bulk-vision partials — resolved/dispositioned: 3-LINE RIFLE B1↔B2, TYPE 19 DMZ B1↔B2, LW3-TUNDRA B1↔B3 fixed (data swaps, live); STRIKER "Fast Reload Reload Case" confirmed correct (real label); J358 "Trigger Action" removed. -->
<!-- • FILED into their real homes this session (see the 2026-07-21 filing ledger in the Graveyard; these leave the working section): L54 optional paginated multi-weapon loadout view → CLAUDE.md v3 (overlaps /meta + /loadout consolidation). L55 "Recommended" badge (shows first in pagination) + L56 "Troll Build" badge (emoji <a:TrollBuild:1529192136208154725>) → CLAUDE.md loadout-badges roadmap. L59 auto-add a space after inserting a confirm mark + L60 the confirm-mark space-insert glitch → meta-deferred-list.md (MarkEdit ext, cross-project). L61 partial "hot-reload" of a pushed file without a full redeploy → CLAUDE.md roadmap (own design session). L62 provide/revoke selective admin access (+ the store-in-Mongo-vs-hardcode question: Mongo is the right call — editable at runtime, no redeploy per change) → CLAUDE.md v3/v4 admin. L63 /autobuild ephemeral toggle + L64 bulk /autobuild PoC + L79 DMZ full-slot vision (needs DMZ slot-layout teaching) → CLAUDE.md /autobuild "still open". L65 rename /autobuild → already covered by the /manage→/admin restructure note. -->
<!-- • STILL NEEDS HARKIRAT INPUT (left unchecked on purpose, deferred to the /autobuild session): L73 the proper /manage attachment→per-slot-metadata fix (a design call — slot input in the modal, or store slots in Mongo); and the DMZ slot-layout teaching for L79. Not resolvable without his input, so not marked done. -->
<!-- • NOT swept this session (no Harkirat confirmation mark yet): every [x] ✓ item above without an ✴︎/✦/◆/ℋ mark stays put until he confirms, per the Legend. The two ℋ-confirmed items (the v2/v3-sections and the confirmation-symbol-candidates questions, both 2026-07-18) WERE swept to the Graveyard this session — they'd carried his confirmation for multiple sessions and were overdue. -->
<!-- • MarkEdit bug cluster note: L52/L58 (Return→blank-line-between-bullets, only in this file) + L60 (confirm-mark space glitch) are very likely the SAME root cause in the confirm-mark extension's CodeMirror keymap; L60 filed alongside L52 in meta-deferred-list.md so they're investigated together. Harkirat's own pointer to check case-tools.js (in his MarkEdit scripts folder, NOT this repo) is captured in the meta-deferred-list.md entry. -->
<!-- • 📋 2026-07-22 SESSION STATUS #5 (Claude, CLAUDE.md modularization) — split the 3,272-line CLAUDE.md into a 182-line root (invariants + a 🗺️ nav map) + 13 path-scoped .claude/rules/*.md (load only when you touch matching code) + docs/ROADMAP.md (now the authoritative roadmap) + docs/reference/ (deployment-and-ops, known-issues, design-history). Verified zero content loss (section→destination ledger, line accounting 3272→3618, 22-phrase subsystem spot-check, YAML frontmatter parse of all 13 rules). ~30 cross-file "see X above/below" refs rewired; docs/README.md + docs/SESSION-START.md + ~8 operative memory pointers updated (roadmap authority → docs/ROADMAP.md); new memory reference_claude_md_rules_structure + design spec docs/superpowers/specs/2026-07-22-claude-md-modularization-design.md. Filed the index.js → handlers/*.js split as its own future session (evaluated, not shrugged). Documented as proposed v2.31.0 (CHANGELOG Unreleased + SUMMARY one-liner + DEVLOG Part A entry). NOT yet committed — awaiting Harkirat's go; docs only, no VM redeploy needed. Next session should verify path-rule loading with /context. -->
- [x] ℋ ✓ (2026-07-28 15:52 EDT — SHIPPED v2.41.0) ~~We need date/time timestamps and stating the current commit hash that the bot was running on when it created that log line in the `vmstatus.sh logs` output. Cuz right now, I can’t tell when the log is talking about or when something stated in logs happened. Also, we do have of storage on the VM, correct? If yes, then why don’t we log a longer period than just 1000 lines, maybe like 3000? Can we also add an arg to the script to view it filtered by live trail or by a time period up to the last 30 days, such as 250m, 24h, or 3d 2h, or 60h, etc (only using m=minutes, h=hours, d=days. No need for weeks or months since we only track the last 30 days of history anyway.), stacking alongside the default lines? No time period arg would be show live trail for the default # of lines. Or could select 2h-7d to show logs from last 7 days *up to* the last 2 hours, but not showing the last 2 hours themselves. As well as bumping the default from 25 lines to 40. Examples of combined script: `vmstatus.sh logs <time> <lines>`, `vmstatus.sh logs 2h 60`, `vmstatus.sh logs 20h-5d`, `vmstatus.sh logs 30m-260h 200`. If time and lines arg are both present and there’s more lines available to show then the amount listed in arg, mention that total number of lines and ask user to rerun script if they want to see those additional lines.~~
  <!-- DONE 2026-07-28 15:52 EDT (Claude) — shipped as v2.41.0. Everything specced is in, but TWO items changed shape once verified, so read this rather than assuming the spec was implemented literally: (1) THE RETENTION BUMP WAS A NON-ISSUE. The "1000 lines" you remembered is the AlertLog *Mongo* store cap (utils/alertStore.js HARD_CAP), not a log setting. journald had NO retention config at all and was holding every line since install — 620 lines / 35.7MB / back to 2026-07-17, at ~56 lines/day. The 25 was a *display* cap. So nothing needed raising; instead journald is now explicitly pinned to MaxRetentionSec=30d + SystemMaxUse=200M so the 30-day window you assumed is actually enforced rather than incidental. Your disk question is answered: 4.0G/30G (13%), never the constraint. (2) THE ERROR COUNTER WAS BROKEN AT THE SOURCE, not in the display. It grepped line TEXT for error|...|reconnecting, so routine `🔌 Shard 0 reconnecting...` churn counted as errors (measured: reported 2, both were reconnects). And `journalctl -p err` could never have caught a real one — the bot logged everything to stdout so journald tagged EVERY line priority 6 (24h: p0-p5 all zero, p6=30). Fixed at the source via utils/logger.js. The panel now shows three separate tiers (errors / alerts / noise) on purpose — don't let a future session collapse them into one number. ALSO: the Google Cloud Ops Agent turned out to be ALREADY installed and running (~127MB RSS), shipping unparsed syslog nobody ever queried — so the Cloud Logging idea carved out of the Firestore review was free, not a new cost. It's now wired to a structured JSON sink carrying version+commit per line. BONUS ANSWER to your old RAM worry: the bot is ~121MB. The Ops Agent + guest agents are most of the rest. Nothing is leaking; the panel now breaks RAM out so it can't read as alarming again. NOT DONE (deliberate): Sentry. Flagged in the addendum below as complementary to the Discord webhook — with structured Cloud Logging now in place the gap it filled is materially smaller, so it's worth re-evaluating rather than adopting on the old reasoning. Left in db-deferred-list.md as its own item. -->
  <!-- ⚠️ FOUND WHILE VERIFYING (2026-07-28 15:10 EDT): the VM was running v2.35.13 (771ea76) while main was at v2.40.0 — 18 commits / 5 releases behind, undeployed. Merged never meant deployed. The new DEPLOY panel exists so this can't hide again; it flags drift on every run. -->

  <!-- FILED 2026-07-28 01:41 EDT (Claude): also NEVER filed anywhere before now — there was only a passing "vmstatus overhaul" mention inside an unrelated tool-discovery item, no actual entry. Now `[P2 · M · Sonnet5-H]` in `docs/db-deferred-list.md`'s 🗂️ Queued section with your full spec preserved: timestamps + running commit hash per line, retention 1000→~3000, default 25→40 lines, the `m/h/d` time-range arg including the `2h-7d` "up to but excluding the last 2h" semantics, and the "more lines available, re-run to see them" notice. ⚠️ One thing I did NOT treat as settled: your "we do have of storage on the VM, correct?" is an open QUESTION, not a confirmed fact — the entry says to verify disk headroom before raising retention, since bumping it 3x on an e2-micro without checking is exactly how you fill a small disk. Left unmarked: filed ≠ built. -->

  <!-- FILED 2026-07-24 18:07 EDT (Claude, answering inline instead of only in a SESSION STATUS block per your feedback below): well-specified, real, and its own dedicated session — [P2 . M]. Scope: per-log-line date/time + running commit hash; bump captured history 1000->~3000 lines (disk isn't the constraint, see the disk question below); a time-window arg (m/h/d, single like 2h or range like 20h-5d, stacking with a lines arg, with a "N more lines available, rerun to see them" hint); default 25->40 lines; a much richer + stylized standalone vmstatus.sh output (expand the errors(1h/12h/48h) tracker too). Left UNMARKED on purpose — deferred, not done. -->
  <!-- ADDENDUM 2026-07-26 19:06 EDT (Claude, tooling discussion): fold a Sentry (free tier) evaluation into this same session. Today's alerting (AlertLog + Discord webhook, see docs/reference/deployment-and-ops.md) tells you THAT something errored; Sentry would add the actual stack trace/breadcrumbs/repeat-error grouping this richer-vmstatus request is reaching for — complementary to the webhook, not a replacement (Discord stays the "ping me now" channel). Also relevant to the still-deferred admin /status command (docs/ROADMAP.md, un-bundled 2026-07-20) if that gets picked up in the same pass — Sentry's error summary could feed it the same way utils/alertStore.js's getAlertSummary() already can. Not decided yet, just flagged as a candidate for whoever scopes this session. -->
- [x] ℋ ✓ (2026-07-28 01:01 EDT) ~~I also want A LOT more details and info in the standalone `vmstatus.sh` output because right now it’s so barebones. I also want it a bit more stylized so it’s easier to view/understand in terminal. Also expand on the error tracker errors(1h)=0 | (12h)=0 | 48h=0~~
  <!-- MERGED 2026-07-28 01:01 EDT (Claude): same ask as the bullet above (L120's "richer + stylized standalone vmstatus.sh output" + "expand the errors tracker too"), just phrased separately and left unfiled/unchecked. No separate action needed — folded into that item's [P2 · M] scope so it isn't picked up twice as two different sessions. -->
- [x] ℋ ✓ (2026-07-24 18:07 EDT) ~~Also noticed we’re sitting at RAM: 536/969MB, isn’t that a bit high for the current bot’s code?? We were barely around 150/500MB when on render and the bot has not changed that substantially since then, except for some backend processing stuff. But even then, idle 536 is a bit much, no??~~
  <!-- ANSWERED 2026-07-24 18:07 EDT (Claude, answering inline instead of only in a SESSION STATUS block per your feedback below): not alarming on its own. Node/V8 grows its heap to fit and does NOT aggressively return freed memory to the OS, and resident native deps (jimp, the mongoose/MongoDB connection pool, ffmpeg spawns, the vision/Cloudinary stack) inflate RSS well beyond Render's lighter image — so a steady ~536MB idle is plausibly just resident footprint, not a leak. The real signal is TREND, not the snapshot: if it climbs monotonically over days that's a leak worth chasing; a stable plateau isn't. Worth one real process.memoryUsage() + RSS-over-time check — folding that into the vmstatus.sh overhaul session below rather than guessing further here. -->
- [x] ℋ ✓ (2026-07-24 12:12 EDT) ~~**PRIORITY:** WHAT A HUGE MISS BY US... We never added a method to add a new season’s patchnotes into the /manage command page. The current edit, url 1, and url 2 buttons just adjust the current actively displayed info and patchnotes. We need another section on that page (same design/structure) to add a new season and another section to edit prior season. I created an example UI, see ('/Applications/Claude Code/Diors-Builds/local/manage patch notes_ui example.json’). Some notes: leave the “current season” as a 3 button setup. Implement “Season Title” field as part of that “Date/Info” button’s modal to manually override the season title, if needed. If nothing input into the field, then automatically reverts to using the Season Title from the /manage season titles/dates modal. Similarly for the “Add New Season” modal, have the manual override season title field available. Because for example, it’s jul 23 and the new patch notes have already released yet the new season’s title/info is still pending. So I need to manually override a placeholder title there because the current season’s title wouldn’t make sense.~~
  <!-- SHIPPED 2026-07-24 (Claude) as v2.32.0 (`987750a`), the "Part 3" patch-notes work carried over uncommitted from the prior Sonnet session: "Add New Season" + "Past Seasons" on the /manage patch-notes page, plus a per-entry `titleOverride` (blank = auto-synced title) threaded through every display site via `displayTitle()`. Matches this item incl. the Jul-23 placeholder-title case. ⚠️ Untested on the live bot — committed to main, deploy held. Awaiting Harkirat's confirmation mark before it moves to the Graveyard. -->
- [x] ℋ ✓ (2026-07-24 18:18 EDT) ~~**PRIORITY #2:** We actually need to do a major versioning system update. We’ve currently only been working on the `main` tree, committing and pushing, where push = version bump. We *need* to be using the full branches, commits, pushes, PRs, and merge flow, with merge = version bump. Branch for the feature(s) we’re working on, commits being a “save progress”, pushes being a “alright, upload the various saves to the cloud”, PR/draft PR being a “alright, Im done with this work. Anyone want to double check it? Or im done but still pending testing. etc.”, and merge being the final “okay, everything is good, lets make this live for the world.”. We’ve currently been over simplifying that process by just working on main, committing, and pushing. We need to use the proper, save flow. We also need to update your memories and other documents that mention, define, or trigger by documentation, commits, pushes, deployments, versioning. But also, where tf does documentation naturally fall in this flow?? Can you also start auto creating branches when we work on new features/related features and do u have access to create PRs as well? You might also need to adjust how git tags work, how we structure our changelogs, roadmap, unreleased, etc. This is a big open-ended task honestly, especially after the large document cleanup and re-structuring we recently did. We don’t want to mess things up or miss things. This is a dedicated opus 4.8-high session I think (or extra-high? Or just medium? Ur advise??). **Wait:** Do we really even need PR/draft PR since it’s just you and me working on this? PR just feels useless when we don’t have anyone else reviewing our work before making it live. Can’t we just go `Create Branch  ──>  Commit  ──>  Push  ──>  Test on Bot, if needed (dev version)  ──>  Merge Branch into `main` and version bump`??~~
  <!-- SHIPPED 2026-07-24 18:18 EDT (Claude): built the full Branch → Commit → Push → PR → Merge → Deploy flow exactly as you sketched at the end here (commit=free checkpoint, push/merge/deploy each asked separately, PR kept even solo since it's still the natural point to review your own diff before it goes live, not just a multi-reviewer gate). Version now mints at squash-MERGE, not push. Design doc: `docs/superpowers/specs/2026-07-24-git-branch-pr-workflow-design.md`. Dogfooded on this exact change: branch `feat/git-workflow` → PR #1 → squash-merged as `904dec8` → shipped as v2.33.0 (see CHANGELOG.md). Effort question: went with Sonnet 5 across the build, ended up fine for this — the design half benefited from stepping through it deliberately but wasn't Opus-hard, and the doc-sweep/implementation half was mostly mechanical once the design was set. -->
- [x] ℋ ✓ (2026-07-24 09:41 EDT) ~~Also I updated your legend slightly, please read it, review it, adjust it accordingly and fix it how you want. I didn’t bother properly organizing it, I just put my entire thought into the 1st point and comment.~~
  <!-- REORGANIZED 2026-07-24 (Claude): rewrote the 🔑 Legend above into 5 labeled subsections (the marks / two dates / confirmation mark / Claude's comment rules / outside-MarkEdit) instead of one long undifferentiated list, removed 3-way duplication of the date-mark explanation, and moved the symbol/color implementation spec out to point at reference_markedit_extension_api memory instead of keeping a second copy here — which had actually already gone stale (still listed Pink as a live color after it was removed 2026-07-19). Full detail in that Legend section itself, not repeated here. -->

- [x] ℋ ✓ (2026-07-24 09:41 EDT) ~~Can you also make a note for yourself to ignore reading the "Graveyard" section unless necessary. That area is an archive so no point waiting tokens reading it all when you only need something from these above active sections. Only go down there if you truly need to reference or find something from there. Or need to read it as part of your organization task of maintaining this file and placing things in there. You get what I mean, right? Like its a dead area so dont consistently waste tokens on it when not needed.~~
  <!-- ADOPTED 2026-07-24 (Claude): agreed, makes sense — added a line to the HOW THIS FILE WORKS header comment above (top of file) so it's a durable rule, not just answered here once. Going forward: only read the Graveyard when actually looking something up from it, or when doing the sweep/organization pass that moves items into it — never as a default full-file read. -->

- [x] ℋ ✓ (2026-07-24 09:41 EDT) ~~Im also kind of confused what these long "SESSION STATUS #N" are?? What are they for? What are you using them for? Reasoning for them? Or whatever. Just help me understand.~~
  <!-- ANSWERED 2026-07-24 (Claude): they exist because of the exact gap L75 above describes — you'd asked in chat whether I'd read new notes-file entries, I said yes in chat, but nothing in the FILE recorded that, so the next session had no way to know and the notes looked unread again. A SESSION STATUS block is that fix: a dated, numbered, in-file paragraph recording what a session actually DID with a batch of intake — shipped/answered/filed/deferred, item by item — written the same session the reading happened, so a future session (or you, skimming back) can see the disposition without re-deriving it from chat history. They're numbered per-session (not per-item) because a single session often reads/handles many items at once and one block covers all of them together. They're meant to be a temporary bridge, not permanent clutter — once an item is actually checked off `[x]` with its own inline comment (like this one), the SESSION STATUS block's job for that specific item is done; the block itself stays as a historical record until swept to the Graveyard along with everything else in a tidy pass. If they're reading as visual noise rather than useful, say so and I can make handling leaner — e.g. skip the block entirely when every touched item already gets its own inline `[x]` comment (which is happening more consistently now), and only write one for genuinely bulk/ambiguous dispositions. --> ※ The sessions status are honestly confusing me because they seem like they’re be notes/info *for you*, yet they also include information and comments directed towards me. If you do want to continue with the session status, implement it as it’s own actual session meant purely for other claude sessions to reference/view. Then take whatever comments/answers for me and just add them as inline/block comments directly under the bullet point. For example, I ask abouted "When you say disk in `disk: 4.0G/30G (15%)`, does that mean storage?” in line 133. Logically I got back to line 133 to check if you saw it and responded, yet I see nothing there. Then by accident I look at your “SESSION STATUS #6” and I see my question answered inside of there on line 151. THAT’S SO CONFUSING. Why is the answer/comment so disconnected from the actual bullet point?? ※
  <!-- ACKNOWLEDGED 2026-07-24 18:07 EDT (Claude): agreed, fixed this pass. Every actual answer that used to live only inside SESSION STATUS #6 (RAM, load average, disk, Cloudinary quality, vmstatus.sh filing, Firestore) now lives inline directly under its own bullet instead; SESSION STATUS #6 itself was rewritten down to short Claude-facing pointers ("answered inline at its own bullet") rather than repeating the answer text. Going forward: an answer directed at you always goes at the bullet it answers, full stop — a SESSION STATUS block (if one gets written at all) is purely a same-session index of what got touched, not a place where you'd ever need to look for content meant for you. -->


<!-- 📋 2026-07-24 12:30 EDT SESSION STATUS #6 (Claude, git-workflow-overhaul session) — a proper notes-review pass on the working sections (Harkirat caught that I'd skipped it at session start). -->
<!-- • Per Harkirat's 2026-07-24 18:07 EDT feedback right above (SESSION STATUS blocks reading as confusing/disconnected from their own bullets): this block is now Claude-facing only, dispositions/pointers, NOT the answer text itself — every actual answer below now lives inline, directly under its own bullet, in the working section above. -->
<!-- • RAM / load-average / disk questions: answered inline at their own bullets (search this file for "ANSWERED 2026-07-24 18:07 EDT"). -->
<!-- • Cloudinary 80/90% quality re-ask: answered inline at its own bullet — same underlying answer as SESSION STATUS #4's original disposition, this was a re-ask. -->
<!-- • vmstatus.sh overhaul (log timestamps/commit-hash/history-length/time-window arg/richer output): filed inline at its own bullet — [P2 · M], left unmarked, deferred to its own session. -->
<!-- • MongoDB → Firestore + Cloud Logging + gemini-3.6-flash: reviewed properly this pass (the recommendation doc was actually read, not just skimmed) — full assessment inline at its own bullet. Short version: not migrating to Firestore, Cloud Logging split out as a vmstatus-session candidate, gemini-3.6-flash pricing flagged for independent verification. -->
<!-- • The /manage attachment→per-slot-metadata fix (previously "still open," needs Harkirat's design input): BUILT this pass — see its own bullet's inline comment above for the implementation. DMZ full-slot vision remains genuinely blocked on Harkirat teaching the DMZ slot layout — no movement possible without that input. -->
<!-- • NOTE for the next session: the git-workflow-overhaul PRIORITY item above gets its inline [x] ✓ mark when the overhaul ships as v2.33.0 (the feat/git-workflow PR merges), not before. -->
<!-- • The re-asks that need real work (vmstatus overhaul, Firestore eval) each carry a [Priority · Effort] tag per reference_priority_tier_system so they can be picked up cleanly. -->

- [x] ℋ ✓ (2026-07-29 18:24 EDT) ~~You keep doing hard line breaks still. Looks at lines 127-151. Why are you manually breaking the line? Let the built-in system automatically wrap the line. I literally pointed this out in an earlier comment to you above, in the legend section. It’s even stated in the legend section to not do these hard breaks. Please check and implement a proper solution for yourself.~~
  <!-- FIXED + ENFORCED 2026-07-29 18:24 EDT (Claude): you were right, and you asked for a *solution* rather than another promise — so this is a hook, not an apology. Reformatted the two offending comment blocks at lines 127-151 into single continuous physical lines. Then built `.claude/hooks/notes-hardwrap-check.sh`, registered as a PostToolUse gate on Edit|Write in the TRACKED `.claude/settings.json`, which fires whenever an edit to THIS file hard-breaks prose inside an HTML comment. It honours your own carve-out: a real bulleted or numbered list inside a comment is one-per-line and is NOT flagged. 7/7 test cases pass, and writing them caught a genuine bug in my own hook — the closing `-->` on its own line was being miscounted as prose, which would have made the bullet exception fire constantly and trained me to ignore the warning. It is advisory and never blocks, and it only sees the text being written, not what is already committed. The reason this needed a machine gate is the honest one: it is the third time you have corrected me on the same rule, and per the `reference_enforcement_hooks` strategy a rule violated three times is a wish, not a rule. -->

- [x] ℋ ✓ (2026-08-03 17:47 EDT) ~~Another markedit extension/feature I want you to develop is a toolbar button to add a timestamp in our current `[YYYY-MM-DD HH-MM TZ]` format. Also make it a keyboard shortcut mapped to `Option+T`. This is so I can add timestamps whenever I make these note/thought bullet point entries so you know exactly when it was made and thus *help you* in staying on-top of them.~~
  <!-- FILED 2026-07-29 18:24 EDT (Claude), left UNMARKED because filing is not building: added to `/Applications/Claude Code/meta-deferred-list.md` as a `[P2 · S · MarkEdit ext]` item in the 🐞 Active Bugs section, which is where that file clusters ALL MarkEdit-extension work including feature requests. It belongs in the cross-project tracker rather than here for the same reason as the Return-key bug — the extension source (`editor.js` / `scripts/markedit-dior-confirm.js`) lives in MarkEdit's OWN extensions folder, outside every repo, so it is not actionable from a Diors session. ⚠️ ONE THING YOU SHOULD KNOW BEFORE THIS GETS BUILT, because it reverses your own earlier call: the `[YYYY-MM-DD HH:MM TZ]` insertion ALREADY EXISTS and shipped 2026-07-25 19:00-19:30 EDT — but folded into both marks' insert actions, because you asked for it that way and said explicitly "not a separate insertion command." You now want the standalone command too, so I queued it as an ADDITION rather than a replacement; tell me if you'd actually rather it replace the mark-integrated behaviour. Also: you wrote `HH-MM` with a hyphen, which is a typo against every other timestamp in this project — including the already-shipped insertion and the Edit/Write hook that greps for `HH:MM` — so I queued the colon and am flagging it rather than silently changing your words. And the toolbar-button mechanism is already solved (the confirm-mark extension ships one; `reference_markedit_extension_api` memory records which API fields work versus the five tested that don't). Your reasoning lands, by the way: an undated bullet gives me no way to tell a note written five minutes ago from one written last week. --> ∴ [2026-08-03 17:47 EDT] Built. `insertTimestampAtCursor()` added to `markedit-dior-followup.js`, reusing the same `formatTimestamp()` that already shipped — kept the colon (`HH:MM`), not your hyphen. Wired three ways: a top-level "Insert Timestamp" menu item, the `Option+T` shortcut (works immediately, no restart), and a "Timestamp" toolbar-button entry (needs a restart + dragging into the toolbar, same as your other custom buttons). Added as an ADDITION — the mark-integrated timestamp is untouched. Not live-verified this pass since your screen was in active use; try it whenever convenient. Full detail in `reference_markedit_extension_api` memory and meta-deferred-list.md's Resolved section. ∵

- [x] ℋ ✓ ~~Need a buff and nerf emoji for the patch notes “more info” field. I uploaded 2 emojis to the dev portal for use. Nerf: `<:nerf:1532528903028084826>` and Buff: `<:buff:1532528904210743376>`. Probably alias them to `b:` and `n:`, so if I type those, it automatically replaces it with the emoji in the rendered embed? But also how can I better format the additional info text so it actually looks nice to read instead of being a bunch of random styling choices?~~ <!-- RESOLVED 2026-07-30 22:24 EDT (v2.45.0, #59): the alias is built — typing a standalone `b:`/`n:` swaps in the buff/nerf emoji at render time, both modals that write this field hint at it now. The "how should this be formatted" half is a style question, not something with a checkable fix — left open for a future design pass rather than guessed at. --> ※ [2026-07-31 10:09 EDT] umm why are these comments showing up as normal text instead of the usual greyed out comment style? Is it maybe due to the double dashes you added within the comment? ※ ※ [2026-07-31 11:39 EDT] I think I decided on how I want to style the “additional info”, see `'/Applications/Claude Code/Diors-Builds/local/Screenshots/CleanShot 2026-07-31 at 11.38.34@2x.png’`. Basically `### Additional Changes` heading, then `__**Weapon**__`, then `Attachment`, then `> b:/n: details`. Sometimes a weapon might have multiple different attachment changes, or multiple changes for a single attachment, so be aware of that. But more or less, this would be the structure so make the field automatically format it accordingly pls. This is still a deferred item btw since I already manually edited the new season’s additional info as per my styling.  ※ ∴ [2026-07-31 12:10 EDT] On the comment-styling question: confirmed, that was exactly it. `<!-- -- -->` HTML comments break on a bare " -- " mid-content in MarkEdit's renderer, and I'd been using that as a dash substitute in every RESOLVED/ANSWERED/FILED comment (12 of them in this file). Replaced every instance with a real em dash (—) instead, file-wide — going forward I'll use — directly rather than typing out two hyphens. On the "Additional Changes" auto-format spec: read and understood (### heading → __**Weapon**__ → Attachment → `> b:/n: details`, handling multiple attachment changes per weapon and multiple changes per attachment) — leaving it unmarked/deferred exactly as you said, not building it this pass. ∵
- [x] ℋ ✓ ~~How do we handle a situation where the current season hasn’t ended yet but the new season’s info is already released and I want to add it to the bot? How do we address that overlap scenario for calendar and new/returning draws? We handled it for patch notes but patch notes has it’s own unique system anyway. We need to figure out calendar and draws pages.~~ <!-- RESOLVED 2026-07-30 22:24 EDT (v2.45.0, #59): built the "Next Season Draft" staging area in /manage — title/deadlines/draws/calendar can now be prepped completely separately from what's live, then swapped in atomically via Promote to Live. Same root cause as (and built together with) lines 197 and 201 below. -->
- [x] ℋ ✓ (2026-07-31 12:10 EDT) ~~In the add single draw confirmation message, it says `**Draw Added:** "Judgment Day - It Goes Two" (New, 4 item(s), releases Fri Aug 07 2026).`. For that time, please use the proper timestamp which will actually display within the embed instead of listing it in text. Same with the edit message and any other confirmation honestly.~~ <!-- RESOLVED 2026-07-31 12:10 EDT: all 4 confirmation messages (draw add/edit, calendar event add/edit) now use real <t:X:D> Discord timestamps instead of .toDateString() text. -->
- [x] ℋ ✓ (2026-07-31 12:10 EDT) ~~For bulk adding, I need a much better and more detailed confirmation than just `"✅ **Bulk Add Complete!** New Draws: added 2 (now 8 total)”`.~~ <!-- RESOLVED 2026-07-31 12:10 EDT: the draws bulk add/replace confirmation now lists the actual titles added/updated under each category, not just a count. Scoped to draws (the one your example quoted) - calendar's own bulk confirmation wasn't touched. -->
- [x] ℋ ✓ ~~Maybe also something similar where I can queue a season name or start date or something? Idk let me know your thoughts.~~ <!-- RESOLVED 2026-07-30 22:24 EDT (v2.45.0, #59): covered by the same Next Season Draft staging area as line 192 — "Titles & Deadlines" stages the next season's title/BP/Ranked/DMZ deadlines ahead of time, nothing goes live until Promote. -->
- [x] ℋ ✓ ~~I lowkey also forget what the bulk input formatting is. I need a “template” or somethinggg to help me. Or some way where I can provide raw data and get the bulk import format (maybe via gemini? Idk)~~ <!-- RESOLVED 2026-07-31 17:20 EDT: built as a rich, structured Guide (utils/manageGuides.js) reachable from every /manage bulk-having page (Draws/Calendar/Loadouts/Patch Notes/Next Season Draft), with a topic-switching dropdown, a paste skeleton, a field-by-field auto-formatting breakdown, and a real before/after example per topic - chosen over the AI-converter option as the scoped MVP. If this doesn't fully solve it, the converter is still on the table. -->
- [x] ℋ ✓ ~~I need a way to add a “-# comment” to a draw. So if I type -# {test}, make sure that doesn’t get formatted like a weapon/char name.~~ <!-- RESOLVED 2026-07-30 22:24 EDT (v2.45.0, #59): "-# text" in the Items field now parses to a comment-type item, rendered as plain subtext, never title-cased. --> ※ [2026-08-03 19:10 EDT] can we standardize the comment convention? Like “resolved” really feels more appropriate if a bug is *resolved*. For a feature, “Implemented” seems like a more fitting work, no? As such, we also need to audit other definitions used in this file and kind of standardize some things. Add this note to deferred list. We’ll do it sometime in the future; maybe during the line by line audit. ※ ※ [2026-08-03 19:16 EDT] Changed my mind, we’re going to do it now. ※ ∴ [2026-08-03 19:40 EDT] Done — a standardized lead-verb glossary (FIXED / IMPLEMENTED / SHIPPED / ANSWERED / FILED / ADOPTED / REORGANIZED / REVIEWED / DEFERRED, each defined by what actually happened) is now in the 🔑 Legend's "Claude's own comment-writing rules" above. Your two examples are exactly what it does: a bug fix is FIXED (not RESOLVED — that word's freed up for the general "this thread reached a real conclusion" sense, which is closer to how it was actually being used inconsistently), and a shipped feature is IMPLEMENTED or SHIPPED depending on whether a version number covers it yet. I did NOT retroactively rewrite the ~100 existing comments in this file to match — auditing confirmed their word choice already varies (RESOLVED/DONE/SOLVED/PRACTICED/VERIFIED all show up for what would now be one of the standard verbs), but every one is still unambiguous from context, and hand-editing that many historical entries risked introducing a real transcription error for a purely cosmetic gain. The convention applies going forward from here. ∵
- [x] ℋ ✓ (2026-07-30 22:24 EDT, v2.45.0, #59) ~~Bug with naming a draw, the title of the draw is `Jupiter Cannon/Void Implosion Draw`, but that “V” keeps getting turned into lowercase “v”.~~ <!-- RESOLVED 2026-07-30 22:24 EDT (v2.45.0, #59): toTitleCase now splits on "/" the same way it already split on "-" — "Cannon/Void" was one token with no hyphen, so only its first letter ever got capitalized. -->
- [x] ℋ ✓ (2026-07-30 22:24 EDT, v2.45.0, #59) ~~A weird bug where someone of the dates changed to July 31 (aug 1st midnight UTC) for somethings (I think stuff that was previously marked “until end of season") when I changed the season end date to sept 9th since I added the new season. This is why im saying we need something for that brief period when seasons overlap.~~ <!-- RESOLVED 2026-07-30 22:24 EDT (v2.45.0, #59): root cause confirmed — "All Season" calendar events always display the LIVE bpEnd (a deliberate earlier design decision), so a new-season event staged before bpEnd was updated correctly showed the OLD bpEnd; nothing was "reverting". The Next Season Draft staging area (192/197) fixes this structurally — draft data never reads or touches live bpEnd until Promote. -->
- [x] ℋ ✓ (2026-07-31 12:10 EDT) ~~I typed “TDB” for the BP season end date, because it’s truly unknown right now but I don't want to keep using the last season’s end date. However, it completely glitched out my existing season end dates as well, now all 3 (bp, rank dmz) show as ending on august 1 midnight utc.~~ <!-- RESOLVED 2026-07-31 12:10 EDT: root cause was parseAdminDate() silently falling back to `new Date()` (literally "right now") whenever chrono couldn't parse the text - "TDB" failed to parse, and since you saved it in the evening EDT, "now" landed almost exactly on Aug 1 00:00 UTC across all 3 fields (they all failed the same way in the same request). Now returns null on failure instead, and every caller (season titles/deadlines, draft version, calendar add/edit, draw add/edit, both bulk parsers) either leaves the field untouched with a warning, or rejects the submission outright rather than silently corrupting it. -->
- [x] ℋ ✓ (2026-07-31 12:10 EDT) ~~Can we redesign the calendar page? A section for draws, a section for events, and a section for playlists/game modes (with ### All caps headers, Or maybe ##)? (You think the bot could accurately self determine the difference or do you want me to implement a prefix or something when importing events to classify which section they fall under? Depending on that, idk if you’d have to adjust the way the data syncs to the database for the /manage calendar page? idk.) I know some of the draws in new/returning draw don’t have specified end dates but lets also link and add those to the calendar’s draws section. See: '/Applications/Claude Code/Diors-Builds/local/calendar_bulk.txt’, I took the events for season 7 and added some marks to help you + whatever is currently added to new/returning draws (note some of the draws are already mentioned in this event list so make sure not to dupe them/make a fuzzy filter for them). Probably could list the draws and events on page 1 and then the playlists/modes on page 2? That also brings up the question, should we change the nav for /calendar from a left/right pagination to instead just be a toggle/button to switch to the specific section?~~ <!-- RESOLVED 2026-07-31 12:10 EDT: 3 sections (### Draws/Events/Playlists), prefix-based classification matching your own calendar_bulk.txt convention exactly (d•/p•/e•, defaults to 'event' with no prefix so old data keeps working). Draws section auto-merges newDraws/returningDraws with fuzzy-dedup against explicit d• entries, confirmed with you before building. Nav stays left/right for now (page 1 = Draws+Events, page 2 = Playlists) per your call - section-toggle buttons filed in db-deferred-list.md's Queued section for their own pass since that's new UI worth seeing first. -->
- [x] ℋ ✓ (2026-07-31 12:10 EDT) ~~Reword the hint on the /calendar page to `-# Toggle between **All Events** or **Active/Upcoming Events** only.`. Let’s also correct the pagination buttons placement and move it above the hint divider, to match the styling of all other paginations.~~ <!-- RESOLVED 2026-07-31 12:10 EDT: hint text is exactly as specified; pagination row now sits between two dividers with the hint last, same structure /draws and /draw prices already use. -->
- [x] ℋ ✓ (2026-07-31 12:10 EDT) ~~Replace the buff & nerf emojis with `<:Buff:1532771212172984401>` and `<:Nerf:1532771213271892089>` respectively~~ <!-- RESOLVED 2026-07-31 12:10 EDT: emojiMap.js updated to the new IDs. -->
- [x] ℋ ✓ ~~honestly, we need a MUCH BETTER system of handling this file. We need to actively organize, edit, place/move things around so the file stays "fresh" instead of building up endlessly — points moved to a defer/to-do file or section, some removed, some sitting crossed out, some prioritized/de-prioritized, and some handled in the same session the file was read.~~ <!-- RESOLVED 2026-07-18 (this session): built the intake-only model documented at the top — thoughts get filed to their real home (CLAUDE.md/changelogs/memory/the deferred lists) and leave this file; resolved items are marked then swept to the Graveyard on the next read. The whole v2–v5 roadmap that used to live here was moved into CLAUDE.md and replaced with the pointer below. project_central_notes_file memory updated to match. -->
- [x] ℋ ✓ ~~maybe we should also sync it to GitHub? That way you can Diff the old committed file vs the local one to see what changed. Or can you already do that yourself even if it lives locally only?~~ <!-- RESOLVED 2026-07-18, then SUPERSEDED same day: originally answered "gitignored, can't diff — use dated snapshots instead" (below), but Harkirat then asked to actually un-gitignore this file (+ CHANGELOG/CHANGELOG-SUMMARY/DEVLOG/SESSION-START) so a real `git diff`/`git log` is available going forward, moving them all into a new tracked `docs/` folder. Snapshots up to this point (`docs/archive/`) are kept for history; going forward, real git history covers the same need, so the manual pre-tidy-snapshot step is no longer strictly necessary (Claude's call at the next tidy whether to keep doing it). Original answer, preserved for context: the main repo CAN'T diff this file — it's gitignored (and can't be pushed anyway, because of your private Space). So instead: DATED SNAPSHOTS. Before every tidy I copy the whole file to `local/notes-archive/diors-builds notes YYYY-MM-DD.md` (a raw byte-copy — private, stays in gitignored local/, preserves your Space untouched). Diff any two dates with a normal file diff. First snapshot: 2026-07-18 (pre-tidy). -->
- [x] ℋ ✓ ~~we DEFINITELY need a place to track current bugs so they never go missed or stale.~~ <!-- RESOLVED (built 2026-07-17): the 🐞 Active Bugs section at the top of /Applications/Claude Code/meta-deferred-list.md — a confirmed bug lands there with a repro the moment it's found, and only leaves when fixed or proven not-a-bug. A session touching a bug's area checks there first. -->

### 🗂️ 2026-08-07 sweep (ℋ-confirmed item moved from the working sections — caught by `npm run docs:audit`'s notes-sweep check, overdue since 2026-08-03)
- [x] ℋ ✓ (2026-08-03 19:40 EDT) ~~Can you update the follow up extension so it defaults to in Sentence-Case formatting? Regardless of where the follow-up is placed (Eg. In headings). The follow-up needs to supersede the Case Tools Extension for itself. I raise this issue because the first word I type after the timestamp is always lowercase, even tho it’s technically the start of the sentence. And I was typing a follow-up note in line 212, which is a h2 heading, and every word in the follow-up kept being capitalized since the case tools extension auto formats headings. So I want to manually set the format for the follow-up text and make it supersede the other extension.~~
  <!-- FIXED 2026-08-03 19:40 EDT (Claude): two separate root causes, both fixed. (1) `markedit-dior-autocase.js`'s per-keystroke sentence-casing never recognized a follow-up's own "symbol [timestamp] " prefix as a sentence start, so the first word after it was never capitalized even outside headings — added `findFollowupOpen()`/`isFollowupSentenceStart()` so a follow-up's own first word (and the start of any later sentence inside it) is detected and cased correctly. (2) `case-tools.js`'s whole-node heading reformat (fires on Enter when Auto-Format is on) blindly title-cased the ENTIRE heading text including any follow-up mark living inside it — your exact repro. Added `protectFollowupSpans()`/`transformPreservingFollowups()`, which mask out any `※...※`/`∴...∵` span before running a transform and restore it byte-for-byte after, so a follow-up's own casing is now NEVER touched by Case Tools at all — `markedit-dior-autocase.js` is the only thing that ever cases follow-up text, exactly the "supersede" you asked for. Verified with a standalone Node test (8 cases: heading+followup-with-a-date for the digit-collision risk, multiple followups in one heading, sentence-start detection, mid-followup words staying uncapitalized) rather than by eye — all passed. `node --check` clean on both files. Not yet live-verified by you in the running app. -->

<!-- • 📋 2026-07-25 21:43 EDT SESSION STATUS #7 (Claude, deferred-list restructure session) — what this session did TO this file, and deliberately did NOT do IN it. DID: split the entire `# Graveyard` section out into docs/archive/graveyard.md (it is no longer a section here); rewrote the HOW-THIS-FILE-WORKS header rules 4 and 5 for the new sweep destination; replaced the stale "Roadmap lives in CLAUDE.md" pointer with the 📍 routing table below (the roadmap moved to docs/ROADMAP.md on 2026-07-22 and that pointer had never been updated); swept every old file name (deferred-items.md → meta-deferred-list.md / docs/db-deferred-list.md, docs/notes-archive/ → docs/archive/) across this file's comments; and re-anchored the SessionStart hook that used to terminate its open-item scan at the now-deleted `# Graveyard` heading. DID NOT: touch, answer, mark, or file the 4 open intake items in the working sections above (the MarkEdit Return-key bug, DMZ 5-attachment prompt cap, /autobuild log timestamps + commit hash, richer vmstatus.sh output). They are still genuinely OPEN and untouched — this session's scope was the deferred-list restructure only, and each of those is real implementation work deserving its own session (recommended: Sonnet5-H individually, or Opus5-M batched). Recording that explicitly so a future session doesn't read this session's edits and assume the intake was handled. -->

