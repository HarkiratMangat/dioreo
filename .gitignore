# Dependencies & System Junk
node_modules/
.DS_Store
# `.env` alone is an EXACT match and does NOT cover sibling env files. The glob was added 2026-07-26
# 15:32 EDT when the local dev bot introduced `.env.dev` (dev Discord token + local Mongo URI) -- without it that
# file would have been silently stageable. Any future `.env.<anything>` is covered by the same rule.
.env
.env.*

# Project-Specific Local Files
gun-builds/
local/
.restart-reason
repomix-output.xml
builds.xlsx

# AI CLI & Memory Folders
# NOTE: ignore ONLY the local settings, NOT all of .claude/ -- .claude/rules/*.md (13 path-scoped rule
# files) and .claude/settings.json are deliberately TRACKED (see the CLAUDE.md modularization, v2.31.0).
# A blanket `.claude/` would silently stop new rule files from being committed. `.remember/` and
# `.superpowers/` are local skill scratch (never were tracked) -- fine to ignore (confirmed 2026-07-24).
.claude/settings.local.json*
.remember/
.superpowers/