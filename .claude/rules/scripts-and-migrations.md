---
kind: rule
status: live
paths:
  - "scripts/**"
---

# Scripts & migrations — where each is documented

*Loads when you touch `scripts/**`. There is no single "scripts" chapter — each script is documented in the subsystem rule it belongs to:*

- `migrateBuildsToMongo.js`, `applyBadgesBulk.js`, `createPlaceholderLoadouts.js` → `.claude/rules/loadouts.md`
- `backfillLoadoutMetadata.js`, `backfillPatchMetadata.js`, `createCloudinaryMetadataFields.js` → `.claude/rules/loadout-images-and-metadata.md`
- `backfillLoadoutSlots.js`, `test-vertex-extract.js` → `.claude/rules/autobuild.md`
- `backfillCalendarCategories.js` (one-time backfill of the `category` field for calendar entries that pre-date the 3-section redesign) and `calendarDedup.test.js` (regression test for `utils/search.js`'s `isSameDrawTitle()`, built from real seasonal titles) → `.claude/rules/design-decisions.md` + `commands/calendar.js`
- `syncMissingDevEmojis.js` (uploads any `utils/emojiMap.js` emoji the DEV application is missing, matched by name) → `docs/reference/deployment-and-ops.md` + memory `project_local_dev_bot`
- `syncNameplateCatalog.py` (the repo's first Python script — additively syncs `docs/reference/nameplate-decoration-catalog.json` against Discord's live `collectibles-categories/v2` endpoint; requires a Discord **user** token, a bot token is hard-rejected — run manually only, never scheduled, to stay out of self-bot territory) → `docs/reference/nameplate-decoration-catalog.md`'s "Keeping it fresh" section. Credentials live in gitignored `scripts/.env.discord-sync`, never in source.
- `syncCatalogToMongo.js` (added 2026-08-15 10:44 EDT — flattens `nameplate-decoration-catalog.json` into one `CollectibleCatalog` Mongo doc per SKU, additive: descriptive fields `$set` every run, this bot's own bulk-cache progress `$setOnInsert` only) and `bulkCacheCollectibles.js` (bulk-renders the catalog into `utils/nameplateWebpCache.js`/`decorationWebpCache.js`'s Cloudinary+Discord cache, grouped by design so a multi-variant design posts as ONE cache-channel message; `--sample`/`--collection`/`--sku`/`--dry-run`/`--retry-failed` flags, batched with a courtesy delay — real 429 protection comes from `@discordjs/rest`'s own bucket-aware backoff, per Discord's documented "don't hard-code rate limits" guidance) → `docs/reference/nameplate-decoration-catalog.md`'s "Where this feeds into" section. Pure logic (grouping/chunking **and the base-variant-first ordering** in `scripts/catalogGrouping.js`, the display-name rule in `syncCatalogToMongo.js`, the sample-selection/message-building/context-mapping logic in `bulkCacheCollectibles.js` itself, and **the Cloudinary cache-key construction in `utils/collectibleCacheKey.js`**) is tested in `scripts/catalogGrouping.test.js`/`scripts/syncCatalogToMongo.test.js`/`scripts/bulkCacheCollectibles.test.js`/`scripts/collectibleCacheKey.test.js`, all four in `npm test`. ⚠️ **`utils/collectibleCacheKey.js` is the ONE place a nameplate/decoration `public_id` is built** — the bulk path and the live path both go through it precisely so they cannot drift and stop sharing a cache; never inline the construction in a cache module. Its test re-derives every id from the real 925-SKU catalog and fails on any collision.
- `scripts/fixtures/loggerComponentProbe.js` (a real in-repo call site `scripts/logger.test.js` requires so `utils/logger.js`'s stack-derived `component` field has a genuine caller to resolve against — synthesized `node -e` code has no on-disk repo-relative path for the stack parser to match) → `.claude/rules/interaction-router.md`'s attribution section
- `pickerUI.test.js` / `settingsPickers.test.js` (added 2026-08-18 12:03 EDT, v3-pre-release review finding #48 — cover `utils/pickerUI.js`'s shared select-row/search-modal builders and `utils/settingsPickers.js`'s `SETTINGS_PICKERS` registry, the shared shape `/settings`' CP-currency and Timezone pickers now parameterize into) → `.claude/rules/settings-and-expiry.md` + `.claude/rules/commands-overview.md`. `helpBodySnapshot.test.js` (finding #49 — before/after fixture diff proving `commands/help.js`'s table-driven rewrite is byte-identical to the pre-rewrite output across a 4-way perms matrix; `scripts/fixtures/captureHelpSnapshot.mjs` regenerates the fixture, only when a rendering change is deliberate) → `.claude/rules/commands-overview.md`. `colorPaletteCrossViewFetch.test.js` (finding #57, closed 2026-08-18 13:16 EDT — instruments `interaction.client.users.fetch`/`fetchProfileExtras` via `require.cache` stubs on `utils/guildProfile.js`/`utils/accentColor.js` and asserts `getPalettePanelData`'s cross-view refresh sweep now calls each exactly once instead of twice; proven able to fail first, against the pre-fix code, before the fix landed) → `.claude/rules/accent-and-colors.md`. Full design/rationale: `local/handoff/review-remnants-plan.md` (gitignored).
- `deploy.sh`, `vmstatus.sh`, `vmpeaks.sh`, `devCommands.js`, `ops-agent-config.yaml`, `logrotate-diors-bot` → `docs/reference/deployment-and-ops.md` + memory `reference_vm_bot_commands`
- `buildLegalPages.js` (+ **`scripts/lib/chronicle.js`**) → **`.claude/rules/legal-site.md`**, which loads automatically alongside this file when you open either of them. *(It used to point at a 286-line section of the root `CLAUDE.md`; that was moved into its own path-scoped rule on 2026-08-01 23:40 EDT, because subsystem craft loaded on every session is exactly what the rules system exists to avoid.)* Renders `docs/legal/*.md` and four root documents → flat `public/*.html` at the site root (dioreo.app went live 2026-08-05 14:43 EDT; the site used to open at `/legal/` and now opens at `/`), **and the three records `docs/CHANGELOG-SUMMARY.md` / `CHANGELOG.md` / `DEVLOG.md` → `public/changelog/*.html`**, for Cloudflare Pages. Not a migration: a **generator**, and the only script here whose output is committed. Run it with **`npm run site`** (syntax-checks both files first, then builds). ⚠️ **The name is now narrower than the job** — it builds the whole site, not just the legal pages. Kept deliberately: `dior legal deploy`/`check` in the CLI repo, this line, and CLAUDE.md all name it, so a rename is a four-place change and its own commit. ⚠️ **`chronicle.js` receives everything it shares via the one-way `CHROME` bundle** — it imports nothing from `buildLegalPages.js`. Add a key to `CHROME_KEYS` when you pass a new one; `requireChrome()` throws on a missing key rather than rendering a page with a hole a content gate would not notice. ⚠️ **Two output directories.** Every nav/footer link goes through `hrefTo(target, from)`, never a bare `./name.html` — two pages are called `index.html` now. New page in a new directory ⇒ set `dir`, and add it to `PUBLISHED_TARGETS` (and `PAGE_ALIASES` if its output name differs from its source name). It deliberately hand-rolls its Markdown parsing rather than adding a dependency — `NOTICE` §3 commits to a copyleft-free tree that gets re-audited on every dependency change, and a formatter for two files is not worth a new supply-chain entry. If you touch it, re-run `node scripts/buildLegalPages.js` and require **100%** from its self-verifier.

  ⚠️ **The CSS lives inside JS template literals**, so a **backtick in a stylesheet comment** terminates the string and fails the build with a SyntaxError pointing at CSS. It happened twice, 2026-07-29 22:00 EDT. Run `node --check scripts/buildLegalPages.js` before a full run. Related trap: a `//` comment containing `/*` (a glob like the model-file path) reads as an unclosed block comment to naive tooling.

  ⚠️ **`parseBlocks()` strips HTML comments, and that is load-bearing** (added 2026-07-29 22:17 EDT). `CONTRIBUTORS.md` keeps its "format for new entries" template inside a comment, complete with a worked example row. Without the strip the comment rendered as visible content and the live credits page listed a **fabricated contributor** (`@example`) as though it were real.

  ⚠️ **Its gates are INDEPENDENT and passing one proves nothing about the others** (learned the hard way; the roster is printed on every run — read it there rather than from a count in prose, which said "THREE" here and "FIVE" in CLAUDE.md and was stale both times). Historically (learned the hard way 2026-07-29 18:55 EDT, v2.43.1; third added 2026-07-29 22:17 EDT). `verify()` checks that every multi-word run of source survived into the HTML; `linkAudit()` resolves every internal href against the deploy tree; `structureAudit()` asserts every column-aligned source line landed inside a `<pre>` or a heading. **The third exists because NOTICE holds its dependency and trademark tables together with runs of spaces — the *alignment* is the structure, and joining those rows into a paragraph destroys the table without changing one word**, so the other two stay green on a wrecked document. It was proven to fail before being trusted: neutering the column detector produced 24 findings naming the real dependency rows. Both documents reported **"100% of source content present" while shipping seven dead links** — content presence and link resolution are different properties, and only the first had a check. Never read a clean `verify()` as "the output is correct."
  - **`PUBLISHED_TARGETS` is the allowlist of what actually gets deployed.** The source Markdown cross-references plenty of repo-only files (`CLAUDE.md`, `ROADMAP.md`, `models/UserPreference.js`, the rules files); those render as inert `<span class="ref">` text instead of links, because a 404 inside a legal document is worse than an unlinked mention. **If you start publishing a new file, add it here or its references stay inert.** Do NOT "fix" these by pointing at GitHub — the repo can be private at any time, which is why the documents carry no repo links at all.
  - `CONTRIBUTING.md` **and `CONTRIBUTORS.md` ARE published now** (2026-07-29 22:17 EDT), as the two `EXTRA_PAGES` rendered by `warmShell()`. This reverses the earlier pull, and both original objections are answered: `CONTRIBUTORS.md` being published makes that link resolve, the rest degrade to inert text, and every page's header now carries a repo link. `linkAudit()` enforces this per build rather than trusting the note.
  - **The site root IS the homepage now, not a redirect to one** (flattened 2026-08-05 14:43 EDT when dioreo.app went live — `build()` writes `index.html` straight to `public/`, the site root, instead of to `public/legal/`). `public/_redirects` keeps a set of 301s from the old `/legal/*` shape to the new flat one (`/legal/terms.html` → `/terms`, etc.) purely for compatibility — bookmarks, the Discord Developer Portal's ToS/Privacy links if they still point at the old URLs — not because anything still lives there. Cloudflare Pages also serves **extensionless** canonical URLs and 308-redirects the `.html` form — so any script checking the live site needs `curl -L`, or it reads zero bytes and reports total drift. `dior legal check` in the CLI repo does this correctly; copy from it.

⚠️ **`vmstatus.sh` had a lost `#` for an unknown number of days** (fixed 2026-07-29 18:55 EDT, v2.43.1). Line 180 was a fragment of its own multi-line comment with the leading `#` missing, so the shell executed `you wait out the whole probe to be told` as a command and printed `you: command not found` on **every run**, from the Mac and on the VM. **`bash -n` cannot catch this** — the line is valid syntax, it just isn't a comment any more, which puts it in the same class as the bash-3.2 constructs noted above. When editing this script's long comment blocks, re-run it and read the FIRST lines of output, not just the panel; the error printed above the banner and had been read past as noise more than once.

⚠️ **`vmstatus.sh` runs in TWO places and the difference is load-bearing** (rewritten 2026-07-28 15:34 EDT, v2.41.0 — design: `docs/superpowers/specs/2026-07-28-vmstatus-overhaul-design.md`). Normally it runs **from the Mac** and reaches the VM over SSH. But `deploy.sh` runs it **on the VM** as its post-restart check, so the script detects its own host (`ON_VM`) and reads locally instead of trying to SSH into itself. On the VM it also **skips the Cloud Logging queries** — the instance service account writes logs but can't read them back, and paying those API round-trips for empty counters would slow every deploy. Both paths print an explicit `NOT LIVE` banner rather than a bare `0`. If you touch this script, test **both** hosts; the on-VM path was quietly half-broken from 2026-07-18 until this rewrite.

⚠️ **It targets bash 3.2** (stock macOS `/bin/bash`, and the only bash on Harkirat's Mac). No `declare -A`, no `${var^^}`, no `mapfile` — and `bash -n` does **not** catch these, they fail at runtime.

## `backupDb.sh` — the ONLY backup this database has (added 2026-08-16 11:02 EDT)

**The Atlas cluster is on the FREE (M0) tier, which provides no automated backups whatsoever** — confirmed by Harkirat 2026-08-16 10:57 EDT. Paid tiers get continuous or snapshot backups; M0 gets nothing. So this script is not a convenience, it is the entire disaster-recovery story, and a period during which it has not run is a period with no recoverable copy.

Writes one gzipped archive per run to `local/db-backups/` (gitignored) and keeps the newest `DIOREO_BACKUP_KEEP` (default 14). Restore with `mongorestore --uri=… --archive=<file> --gzip`, optionally with `--nsFrom`/`--nsTo` to land it under a different database name — which is exactly how the 2026-08-16 `test` → `diors-builds` rename was performed.

- ⚠️ **It never prints `MONGODB_URI`**, and reads it with `grep` rather than `source`ing `.env` — sourcing would execute the file and export every other secret into the shell for no reason.
- ⚠️ **It verifies the archive rather than trusting the exit code.** `mongodump` can exit 0 having written an unusable stub, so a sub-1KB archive is treated as a FAILED backup and deleted — a backup you believe in but cannot restore is worse than a missing one.
- Retention counts files rather than using `find -mtime`, so a long gap between runs can never delete the only copy you have.
- **Not yet scheduled.** Running it on a timer (launchd locally, or cron on the VM) is the obvious next step and is not done.

## `migrateRenderTimings.js` — the one-shot RenderTiming retirement (added 2026-08-16 13:24 EDT, v3.35.0-pre)

Migrates the `rendertimings` collection into the event plane **with its raw `discordId` replaced by the same keyed hash every other pseudonym in this project uses**, then drops the source collection. Documented in `.claude/rules/models.md` and `docs/legal/PRIVACY.md` §2.4b; the design is `docs/superpowers/specs/2026-08-16-observability-layer-design.md`.

- Dry run by default; `--write` migrates and drops. Safe to re-run — it no-ops once the source collection is gone.
- 🔴 **It refuses to start without `ANALYTICS_HMAC_KEY`, and must be run with the SAME key the target environment's bot uses.** A wrong key produces 64 hex characters that look perfectly correct and correlate with nothing, and there is no way to detect that afterwards.
- It runs the project's own raw-ID assertion **on the real data** before writing: every distinct source `discordId` is string-searched across the finished documents, and it exits non-zero rather than writing if any survives.

## `eventStore.test.js` — the raw-ID guard (added 2026-08-16 13:24 EDT, v3.35.0-pre)

27 pure-logic cases over `utils/eventStore.js`, wired into `npm test`. The one that matters serialises a finished event document and **string-searches** it for a raw Discord ID, because the realistic regression is a `detail` value that happens to carry one or a `customIdPrefix` capture grabbing a segment that embeds one — neither of which a field-by-field schema review would catch. It stubs `models/AnalyticsEvent` and `models/SearchTerm` through `require.cache`, so it needs no Mongo. ⚠️ **It was verified able to FAIL** (the scrub was disabled and the test went red) — a falsifier that cannot fail manufactures confidence.

⚠️ **Four cases (added 2026-08-18 14:30 EDT, v3-pre-release review finding #56) cover the `SearchTerm` buffer and the shutdown-flush memoization it exposed a gap in**, but they only prove the PURE LOGIC — `models/SearchTerm`'s `bulkWrite` is stubbed here too, so this file cannot prove a real process SIGTERM actually lands the write. That property was proven separately, live, against real local dev Mongo with a disposable harness (not committed): the pre-fix two-listener race reproduced with the write silently dropped, and the shipped `flushAllForShutdown()` fix landed it across 4 repeated real `SIGTERM` runs. See `utils/eventStore.js`'s `flushAllForShutdown()` and `installShutdownFlush()` comments, and `utils/instanceLock.js`'s `releaseLock` comment, for the full mechanism.

## The observability layer's stage-4 test scripts (added 2026-08-16 17:07 EDT, v3.37.0-pre)

Three tracked scripts from the health/roll-up stage, all wired into `npm test` and all needing no Mongo:
- **`rollupStore.test.js`** — pure-logic cases over `utils/rollupStore.js`, the aggregation half (`RollupState` plus the daily roll-ups `/bot analytics`' Health page reads).
- **`cloudObservability.test.js`** — cases over `utils/cloudObservability.js`, the GCP Cloud Monitoring/Error Reporting reader behind the Health page's three-tier error model.
- **`analytics.test.mjs`** — the reporting-side cases.

⚠️ **All three shipped with no entry in this pointer map**, which `docs-audit`'s `scripts-documented` check reports — and it reports at **WARN**, never ERROR, which is exactly why three of them accumulated unnoticed rather than one being caught on the day. If you add a script here, add its line in the same change; a warning that nothing blocks on is only as good as the habit. The subsystem itself is documented in `docs/superpowers/specs/2026-08-16-observability-layer-design.md`.

## `portSeasonalToLocal.mjs` — copy the LIVE season document into local dev Mongo (added 2026-08-23 16:24 EDT)

`node scripts/portSeasonalToLocal.mjs` (dry run) · `--write` to apply · `--keep-draft` to preserve the local `draft` field. Everything the portal's Season realm renders — draws, returning draws, the calendar, the season titles and end dates, the patch notes — lives in ONE document (`seasonaldatas`, `docType:'global'`), so this is a single-document copy rather than a multi-collection sync. Written because the portal's Season realm is only as reviewable as the data behind it: on a fresh dev database `bpEnd` is unset, which collapses the Track's window to a point (see `portal/ui/season.logic.js`'s `seasonWindow`).

🔴 **Three guards, because this script holds BOTH URIs open at once.** The **target must be `mongodb://localhost`** — it refuses otherwise, since a copy running the wrong way round would overwrite the live season with dev test data (same failure class `portal/server.js`'s `assertEnvironment()` exists for). The **source must not look local**, so a local-to-local copy cannot silently succeed and read as a real port. And the **local document is backed up** to `local/db-backups/` before anything is written, so an unwanted port is one restore away.

⚠️ **It verifies by RE-READING the target**, not by trusting `updateOne`'s result, and dies loudly if the re-read disagrees. ⚠️ **It parses both env files itself** (prod `.env` for the source, `.env.dev` for the target) because `--env-file` takes only one — and greps rather than sources them, since sourcing executes the file and exports every other secret into the process for no reason (same reasoning as `backupDb.sh`).

## `docs-audit.mjs` + `docs-audit.test.mjs` — the documentation invariants (added 2026-07-28 21:00 EDT, v2.42.0)
`npm run docs:audit` · `npm run docs:audit:test`. Not a migration — a **checker**, and the only script here wired into CI (`.github/workflows/ci.yml`) as a merge gate. Run `node scripts/docs-audit.mjs --list` for the current check roster -- no count is written down here, because a number in prose is a copy of state that nothing updates (see the `feedback_no_duplicated_state_in_prose` memory; this very file said "10" within an hour of the roster reaching 19). Two severities: `ERROR` fails the build, `WARN` reports and never blocks so a hotfix isn't held up by prose.

If you touch it:
- **Add the check AND its self-test in the same change.** `docs-audit.test.mjs` asserts two things per check — the broken fixture FAILS *and* valid input stays SILENT. The second half is what catches a matcher that fires on everything; skipping it nearly turned two correct `CHANGELOG-SUMMARY.md` range headings into a fabricated gap. The self-test found a completely dead check on its first run.
- **Exemptions carry a reason and a date.** `KNOWN_BAD_TAGS`, `TAG_RULE_FROM` (package.json wasn't bumped per release before v2.33.0 — verified), `DEVLOG_RULE_FROM`, the `XREF_SKIP_*` lists, and **`XREF_IGNORED_OPTIONAL`** all exist because an unexplained allowlist silences a real defect forever.
  - ⚠️ **`XREF_IGNORED_OPTIONAL` (added 2026-08-10 21:24 EDT) is the narrowest of these and must STAY narrow.** It retires the gitignored-and-absent WARN for **individually triaged** paths — currently only `utils/emojiMap.dev.json`, a genuine optional dev overlay that `utils/emojiMap.js:120` reads defensively under `NODE_ENV=development` and fails soft on. **It is NOT a return of the blanket skip.** That skip is what the WARN replaced after it masked a real bug: `CLAUDE.md` and the notes file both still pointed at Harkirat's private space file under `local/` after it had moved into `docs/ideas/`, and the `local/` ignore rule hid the broken pointer. *(Phrased without writing the old location as a path — `xref` cannot tell a historical example from a live stale reference, and it warned on exactly this sentence's first draft.)* **Every other gitignored-and-absent path still warns.**
  - **An entry belongs there only when the referencing docs are CORRECT and the file is optional at RUNTIME** — never to quiet a path nobody has actually chased down. Reason + date required, same as every other exemption here. Two self-tests pin both directions (the triaged path stays silent; an untriaged one still fires), so the exemption cannot widen unnoticed.
  - 💡 **Why it exists at all:** the `emojiMap.dev.json` warning was correct-by-design and had **already been triaged once**, in v2.42.0, which recorded it as "deliberately left alone" — and it then re-warned on every run for eleven days, costing the same investigation a second time. **A permanent warning about an answered question trains readers to skim the whole WARN block**, which is the same reasoning behind `SUPPRESS_CHRONICLE_DRIFT` below.
- **It must never report a conclusion it can't support.** It detects a shallow clone and downgrades the git-dependent checks to a warning; a depth-1 checkout otherwise yields 42 false `hash-chain` errors and sees 1 tag instead of 100+ (measured).
- `DOCS_AUDIT_ROOT` repoints the whole audit at a fixture tree; that's how the self-test works.

**`scripts/docs-audit-test-parallel.mjs` — sharded parallelism for the self-test's cold run (added 2026-08-21 11:20 EDT).** `docs:audit:test` now invokes this wrapper instead of `docs-audit.test.mjs` directly. Instrumented timing found the 71 checks near-uniform (median 3.1s, 1.5–5.2s range, no long pole), so process-sharding gives close to linear speedup: `DOCS_AUDIT_TEST_SHARD="i/N"` restricts one `node scripts/docs-audit.test.mjs` process to roughly 1/N of the checks, the wrapper spawns N of them (`DOCS_AUDIT_TEST_JOBS`, default 8) and aggregates. **`docs-audit.test.mjs` itself is unchanged in default behavior** — the shard env var unset (every normal invocation, including a bare `node scripts/docs-audit.test.mjs`) runs every check in one process exactly as before sharding existed; that default-safety property is deliberate and was verified byte-behavior-identical before this shipped. ⚠️ **The one property a shard change could silently break is coverage** — the meta-check asserting every check `docs-audit.mjs` registers has a `proves(...)` case. It stays correct in every shard because `proven.add(checkId)` is the unconditional FIRST line of both `proves()`/`provesSilent()`, always executed before the shard gate is consulted — so every shard's own `proven` Set contains all registered checkIds regardless of which subset it actually runs, and the coverage check (left completely unsharded, running identically in every shard) is a structural guarantee, not an aggregation step this wrapper has to get right. Proven with a real failure injection before shipping: neutering one check's `breakIt` (leaving its `checkId` untouched) produced exactly 1 reported failure and 70 passed across 8 shards — no false positives, no swallowed failure.

**Current state and its honest edges (2026-07-29 02:10 EDT, v2.42.0).** Run `node scripts/docs-audit.mjs --list` for the live roster — no count is written here, it would rot. Read the **accounting line** every run prints, not just the verdict: `N/M checks verified (K items examined)`, plus anything **SKIPPED** and anything that examined **nothing**. A check matching zero items "passes" while verifying nothing, and that is how a broken matcher survives indefinitely. A pass means *no known failure mode tripped* — never *the records are correct*.
**What it cannot cover is filed, not forgotten:** see `docs/db-deferred-list.md` → 🧹 Someday / tech-debt → "the limits it does NOT cover" (content accuracy, novel drift, the web-UI PR path).

**Two gates DELEGATE to this script — and both fail LOUD, never silent.** `.claude/hooks/devlog-toc-check.sh` and `.claude/hooks/docs-audit-gate.sh` call it rather than keeping their own copies, so there is one implementation of each rule. The cost of that is a single point of failure: delete or break `docs-audit.mjs` and a bare `exit 0` would have quietly retired both gates. So **"the audit could not run" is reported as its own finding** — missing file and invalid-JSON crash are both handled, and both were tested by actually removing and breaking the script. Deliberately NOT a duplicated fallback implementation: two copies drift, and the drift is silent too. The independent prior detection layer still stands beside it — the `gh pr merge` hooks (changelog, DEVLOG, release-doc check), the `git tag` invariant gate, the Edit/Write TIMESTAMP check and the `Stop` completion-claim hooks all run without this script and catch different failures.

⚠️ **`--json` mode means STDOUT IS A CONTRACT — never `console.log` from inside a check body** (learned the hard way 2026-08-06 00:23 EDT, within two minutes of writing the offending line). The whole report goes to stdout as one JSON document and the two delegating hooks parse it, so a single prose line printed from a check prepends itself to that document and both hooks report **"DOCS AUDIT CRASHED: it did not return valid JSON"** — the script itself was fine. Check bodies run in **both** modes; the summary notes near the bottom of the file run only in the human path, which is why `console.log` is safe there and not here. **Anything printed from inside a check goes to `console.error`.** This is the delegation cost the paragraph above describes, in its concrete form.

⚠️ **A SUPPRESSED check is a third state beside pass and fail, and it has three requirements.** `SUPPRESS_CHRONICLE_DRIFT` (added 2026-08-06 00:20 EDT, Harkirat's call — the chronicle pages are withdrawn from the nav, so that meter reported the same expected drift on every run and was training everyone to read past the whole WARN block). A suppression is only legitimate when: **(1)** the check still runs and still examines its items, so the vacuous-pass detector keeps watching it; **(2)** it still prints its state every run, so the information is withheld from the *findings*, never from the *reader*; and **(3)** it is gated on `DOCS_AUDIT_ROOT` being absent, so the fixture tree is untouched and `docs-audit.test.mjs`'s `proves()` keeps exercising the real logic. Miss (3) and the suppression disables its own failure test — the silently-dead gate this whole file exists to prevent. Every suppression also needs a **filed lift-trigger** (`docs/db-deferred-list.md`), because an exemption with no removal condition is permanent by accident.

## `devlog-add.mjs` + `devlog-add.test.mjs` — the only correct way to append a DEVLOG entry (added 2026-08-10 00:06 EDT, Harkirat's call)

`node scripts/devlog-add.mjs --title "<TITLE>" --body-file <path>` (`-` for stdin), wired into `npm test`. **Use it instead of hand-editing `docs/DEVLOG.md`.** Hand-writing an entry failed the same two ways on almost every release, and both were caught only by `docs:audit` **after** the bytes landed, so each cost an edit-and-recheck cycle: the entry appended at EOF lands **after the `# Part B` marker** (Part B is thematic; dated entries belong in Part A), and the Part A **TOC** goes un-updated. Neither needs judgement — both insertion points are structural. Harkirat, 2026-08-10 00:00 EDT: *"ive noticed that happening a lot."* `devlog-toc` and `record-structure` stay as the backstop and now name this script in their failure messages.

⚠️ **It writes with `writeFileSync` from a subprocess, so it does NOT pass through the `Edit`/`Write` tools — and therefore triggers none of their `PreToolUse` hooks.** Left alone that is a net loss: one manual failure removed, three gates silently disabled, invisibly. So it **runs them itself** against the same payload shape the harness sends, reusing the hook scripts rather than reimplementing them: **`timestamp-check` in BOTH modes** (`pre` denies an impossible/future stamp — blocking; `post` warns on a bare date — advisory), **`typos-check`** (blocking), and **`defer-in-place-guard`** (advisory). A missing hook is **reported, never silently skipped**. Soft-wrap, `devlog-toc` and `record-structure` are deliberately not duplicated here — `npm test` and `npm run docs:audit` already own them.

⚠️ **`timestamp-check` has two modes with different severities and running only one silently halves it.** A first version of the test asserted `pre` caught bare dates; it does not and never claimed to. Both modes are now pinned separately, so dropping either from the script fails the suite.

⚠️ **Three self-inflicted traps, all now pinned as tests** — each one fired against this very work: `new URL(...).pathname` percent-encodes the space in `/Applications/Claude Code/` into `Claude%20Code` (use `fileURLToPath`); the misspelling and future-stamp fixtures must be **assembled at runtime**, or `typos-check` and `timestamp-check` flag the test file itself; and `execFileSync` **discards stderr on success**, so an advisory gate that exits 0 looks identical whether it warned or was swallowed (use `spawnSync` and assert both streams).

## `reflow-prose.mjs` + `reflow-prose.test.mjs` — the soft-wrap migration and its guard (added 2026-08-08 11:45 EDT, Harkirat's call)

`npm run docs:reflow` · `npm run docs:reflow:test`, both wired into `npm test`. Not a migration and not quite a checker — it did a one-time job and then stayed as the gate that keeps it done. **Prose in this repo is soft-wrapped**: one logical line per paragraph, list item or quoted paragraph, with the editor wrapping for display. Measured at decision time — 13,582 of 21,020 prose lines (64.6%) continued a sentence onto the next line, so most multi-word phrases physically did not exist on any single line and `rg` could not match them.

⚠️ **This deliberately does NOT use `dior text unwrap`**, and that is not a preference. Verified against this repo before writing a line: its `flushQuote()` emits `"> " + parts.join(" ")`, collapsing an N-line quote to ONE `>` — which destroys the bare `>` that separates quoted paragraphs. It merged two distinct paragraphs in `docs/legal/TERMS.md` and dropped that file's `## PLEASE READ` heading into the body. The CLI is still right for one-off pasted text; it is not right for this tree.

**It is verified rather than trusted, at three independent levels** — token conservation (quote markers counted as structure, not content), structural counts (headings, table rows, HTML lines, quote breaks, list markers, byte-identical fenced blocks), and an independent **CommonMark oracle** via the `markdown-it` devDependency. The oracle exists because the hand-written invariants share assumptions with the reflow code itself; a real parser does not.

⚠️ **Render-equivalence is NECESSARY, NOT SUFFICIENT — the lesson this script paid for.** `docs/legal/TERMS.md` and `PRIVACY.md` open with a metadata block that `buildLegalPages.js` reads **one line per field**. Joining it folded `Version` and `Applies to` into the rendered `Effective` value on the live site. CommonMark calls both forms identical, so the oracle passed it and every structural invariant passed it too — it was caught **only** by rebuilding the site and diffing `public/`. Hence `isFieldLine`. **Any consumer that reads LINE STRUCTURE is invisible to a general parser; rebuild and diff the real output.**

Three other traps already paid for, all preserved as tests: `/^\s*</` classed prose as HTML and tore `…`git show` ⏎ `<sha>:package.json`…` out of its paragraph (now keyed on real HTML element names); unindented lines were absorbed into list items, which CommonMark renders identically but which destroys an authored block boundary *irreversibly*; and the CLI ran on `import`, so a test importing `verify()` executed it and `process.exit()`ed before any assertion ran, while printing a confident "0 failed".

If you touch it: the test file's job is mostly proving the VERIFIER can fail, not that reflow works — feed it known-corrupt output and assert it says so. It is idempotent, so a second run is a no-op rather than progressive damage.

## `reflow-comments.mjs` + `reflow-comments.test.mjs` — the CODE-COMMENT half of soft-wrap (added 2026-08-17 18:08 EDT, filed 2026-08-08 12:30 EDT in `docs/db-deferred-list.md`)

`npm run docs:reflow-comments` · `npm run docs:reflow-comments:test`, both wired into `npm test`. `reflow-prose.mjs`'s sibling, covering the corpus that script's own header explicitly excludes: BLOCK comments in `.js`/`.mjs`/`.sh` — a run of 2+ consecutive full-line `//` (or `#`) comments, or a multi-line `/** */` header whose continuation lines each start with `*`. Trailing `// comment`/`# comment` sharing a line with code is never touched. A **from-scratch implementation**, not a reuse — feeding code through a Markdown parser would either no-op or, if bypassed, corrupt live code.

⚠️ **JS comments are found via `acorn` (`onComment`), not a hand-rolled lexer** — added as a devDependency for exactly this file. A bespoke scanner has to correctly re-derive JS's string/template/regex rules to avoid mistaking `//` inside a regex literal (`/http:\/\//`) or a multi-line template literal for a real comment; acorn already solves that as a side effect of actually parsing the file. `.sh` gets a hand-rolled scanner instead, because the grammar needed is much narrower (only full-line comments are ever candidates) — it tracks single/double quotes and heredoc bodies, and asserts heredoc-body byte content conserved as its own invariant on top of the general non-comment-bytes-identical guarantee.

**The invariant is stronger than the Markdown one**: every non-comment byte must be byte-identical (holds by construction — output is built by splicing replacement text into exact `[start,end)` offsets, never a line-by-line rebuild), plus `node --check`/`bash -n` must still pass and comment-text tokens must be conserved per block.

⚠️ **Four real bugs found via a requested `sequential-thinking` audit BEFORE the first real `--write`, all now pinned as regression tests:**
1. Acorn's `allowHashBang` reports the shebang line itself as a fake Line comment (start=0) — merged into the file's first real comment block until filtered on `text.slice(start,start+2)==='//'`.
2. Acorn's block-comment `value` never includes the closing `*/` — a closer-detection regex written against `value` can never match, which silently disabled the ENTIRE JSDoc reflow path (0 of 79 blocks in the repo ever touched) with zero error. Fixed by deriving the closer's indent from the last split-line of `value` directly (it already IS that whitespace when the closer sits on its own line).
3. Non-idempotency: a comment line with one stray leading space fell into a gap between "join at indent 0" and the old "verbatim at indent>=2" rule, landing differently on a second pass. Fixed by lowering the verbatim threshold to indent>=1; proved via an idempotency sweep across all 252 tracked files' original content before trusting a real `--write`.
4. `verifyJs`/`verifySh` took the real output as a parameter but never actually read it (only re-derived their own comparison from `blocks`+`before`), so they were blind to any real construction bug — caught only because the regression suite fed a hand-corrupted `after` and asserted rejection, which the original version could not do.

⚠️ **A fifth, found live on CI rather than by audit (PR #156, fixed PR #158, 2026-08-18 20:44 EDT):** `externalCheck()`'s syntax-check step used to pipe content through `node --check /dev/stdin` (and `bash -n /dev/stdin`) — clean on macOS, but crashes on GitHub Actions' Linux runners with `ENOENT` on a `/proc/<pid>/fd/socket` path, because Node's `/dev/stdin` resolution assumes a regular pipe and that runner's process wiring hands it a socket instead. **It only fires for a file with a genuinely proposed change**, which is why three local `npm test` runs never caught it before CI did. Fixed by writing `text` to a real temp file (`os.tmpdir()`, cleaned up in a `finally` block) and checking that path instead — this function had zero direct test coverage before the fix, so 6 regression cases were added alongside it (valid/invalid JS and shell syntax, temp-file cleanup, and a structural guard that no *code* line references `/dev/stdin` any more, distinct from the comments that still document the old bug).

Also found by manual inspection (not caught by any verifier): `// ===...===` decorative section borders and `Usage:`-style indented literal examples were both getting merged into surrounding prose — fixed with a repeated-character rule detector and by requiring indent-0 (not "any indent while a paragraph is open") for plain-paragraph continuation.

**Verified beyond unit tests, on the two highest-risk real files in the corpus:** `.claude/hooks/mcp-layer-check.sh`'s own 23-case self-test suite, run against a reflowed copy of the hook — 23/23 passed (real behavioral proof, not just `bash -n`). `scripts/buildLegalPages.js` (by far the largest and most trap-laden file in the repo — see its own entry above; no line count is written here, the one that used to be moved within a week) still builds clean through `npm run site`'s full self-verifier after reflow: every source entry rendered, all inline scripts parse, aligned NOTICE blocks intact, no credentials published. ⚠️ **That verification run itself has a trap, paid for once already**: `npm run site` rebuilds `public/changelog/*.html` as a side effect, which can pick up unrelated pre-existing drift from `docs/CHANGELOG.md`/`DEVLOG.md` edits that happened for other reasons — per this repo's own standing rule (root `CLAUDE.md`), that drift is CORRECT and must never be committed opportunistically. Revert `public/` after using `npm run site` as a verification step for anything other than the nine real legal-site sources.

If you touch it: the test file's job is mostly proving each defect above STAYS fixed, same philosophy as `reflow-prose.test.mjs`. Idempotent — a second run is a no-op (bugs 3 exists precisely because that wasn't true once).

## `scripts/exportWeaponIndex.mjs` — the one committed copy of live data the website reads (added 2026-08-19 17:45 EDT)

Dumps weapon names, keys, categories and build counts out of the `loadouts` collection into `scripts/data/weapon-index.json`, which `/commands` inlines so its weapon options can autocomplete the way the bot does. **Read-only; it never writes to the database.** Run it with `node --env-file=.env scripts/exportWeaponIndex.mjs`.

🔴 **THIS IS THE ONLY THING ON THAT PAGE THAT CAN GO STALE**, and it exists because the site builds with **no database** — everything else is read from the bot's own registered builders at build time and therefore cannot drift. Re-run it after any bulk loadout import. `scripts/weaponIndex.test.js` fails on a missing, malformed or implausibly small file, but **nothing can check freshness without a connection**, so the file records the date it was generated.

⚠️ **The exporter refuses to write below a floor, and that guard is load-bearing.** Its first run used `weapon` where the schema says `weaponName`, which grouped every document into one null-keyed bucket, reported "1 MP + 1 DMZ weapons" and exited 0 — a well-formed file that was completely wrong. A generator that can quietly produce a near-empty artifact needs a floor, not just a success message.

## `scripts/lib/commandCatalog.js` + `scripts/commandCatalog.test.js` — what the website's `/commands` page knows (added 2026-08-17 20:07 EDT)

Reads the **real command builders** (`commands/*.js`'s own `.toJSON()`) at site-build time and returns the grouped, ordered, admin-filtered catalog the `/commands` page renders. Nothing is transcribed, so the page cannot drift from the bot. Wired into `npm test`; needs no Mongo, no Cloudinary and no env.

⚠️ **Ordering and admin-gating come from `commands/help.js`'s own `CATEGORY_DEFS`**, not from a second list here. That is what stops the website and the in-Discord directory disagreeing about how the bot is organised, and it means a new admin command is excluded from the public site by the same `requires` key that hides it in Discord — rather than by someone remembering to add it to an exclusion list. `help.js`'s own header records why that array became the single source of truth.

⚠️ **`buildCatalog()` THROWS on a public command with nowhere to go, and that is the feature.** The failure it prevents is a command that exists, works, and is invisible on the page whose entire job is listing commands. On its first run it stopped the build on `/invite`, which is registered and public but appears in no `CATEGORY_DEFS` entry. ⚠️ **That absence is DELIBERATE and already filed** — `docs/db-deferred-list.md` carries Harkirat's own instruction to keep `/invite` out of `/help` pending a placement decision, and says explicitly not to "fix" it as an oversight. `/help` omits itself for the obvious reason. Both are placed via `EXTRA_PLACEMENT`, because a page that exists to list commands has no reason to hide either.

⚠️ **Requiring the command modules from a build was MEASURED safe before this was written** (2026-08-17 20:05 EDT), not assumed: zero `mongoose.connect` calls, `readyState` 0, no handle that could hang a build, ~0.5s, exit 0 under `env -i`. If a command module ever connects on require or throws without credentials, this module is the wrong shape and the fallback is a generated, committed catalog with a staleness gate.

Its test's real job is proving the gate **can fail** — it feeds a `CATEGORY_DEFS` that has forgotten a live command and asserts the build throws *and names it*, then asserts the real tree stays silent. A falsifier that cannot fail manufactures confidence.

## `mcp-observation-metrics.mjs` — the measurement instrument (added 2026-08-02 14:59 EDT)
Not a migration and not a checker — an **instrument**. Read-only over `~/.claude/projects/*.jsonl`; writes nothing and touches no project state. It measures the 7-day MCP observation window opened 2026-08-02 14:43 EDT (protocol + pre-registered baseline: `docs/superpowers/specs/2026-08-02-mcp-observation-window-protocol.md`; close-out is a dated reminder in `docs/db-deferred-list.md`).

⚠️ **DO NOT EDIT IT DURING THE WINDOW.** The baseline and the treatment period must be measured the same way or the comparison is void. It already went v1 → v2 once (v1 measured turns but not tokens or model mix — half the cost model), which was only legitimate because zero treatment data existed yet. The same edit on day 3 destroys the experiment. If a change is unavoidable: note it in the protocol and re-baseline both windows.

⚠️ **`--to` is EXCLUSIVE.** Use `--to 2026-08-10` to include 2026-08-09. This off-by-one has already been caught twice — once here and once in `.claude/hooks/mcp-layer-check.sh`'s date comparison, where it would have reverted the window's final 17 hours.

⚠️ **`estCostUSD` is a RELATIVE INDEX, never spend.** Calibrated against the one real figure (session `38972d5e`: 404 Sonnet-5 turns → $44.02 at list vs $17.10 billed = **2.57×**), and that factor is **model-specific** — Opus and Sonnet rates differ ~5×, so it holds only while the model mix holds. **Check the `models` breakdown first at close-out**; an Opus-skewed week moves every number by itself.

It counts real `tool_use` invocations, never mentions — mention-counting `sequentialthinking` returns 38 vs 2 actual calls, because the name appears in every system-prompt tool listing.

*General rules: a migration/backfill script should be safe to re-run (clear/upsert, not blind insert). Never log a raw Cloudinary error object from a script — plaintext secrets; see `.claude/rules/loadout-images-and-metadata.md`.*

## Licence & attribution gates (added 2026-08-02)

Two invariants that CLAUDE.md's licensing section had stated in prose and nothing enforced — the consequence of getting either wrong is a licence-compliance defect, not stale documentation.

- **`dep-licences`** (ERROR) — no GPL/AGPL/LGPL/MPL/SSPL/CDDL/EPL/CC-BY-SA/OSL/EUPL anywhere in the tree, and **every package's licence must be KNOWN**. A licence resolves from the lockfile, then the package's own `package.json`, then its licence FILE text. ⚠️ **The text fallback is not optional**: `chroma-js` and `exif-parser` declare no licence field at all, and a scanner that reads "unknown" as permissive fails open — the exact failure it exists to prevent. Reading the file identifies them (BSD and MIT) with no allowlist to go stale. SKIPs with a stated reason when `node_modules` is absent rather than passing.
- **`notice-attribution`** (ERROR) — every runtime dependency appears in NOTICE §1 **at the version the lockfile resolves**. The version half matters: "regenerate when dependencies change" is mostly about changes, and a name-only check calls a two-majors-stale attribution correct. ⚠️ Its section boundary is the next **line-initial** numbered heading. The first version searched for the literal `"2."` and matched inside `chrono-node 2.9.1` — the first entry — so the section came out empty and every dependency was reported missing against a NOTICE that was entirely correct.

⚠️ **`scripts/checkEmojiCaptures.js` now runs in CI.** It was documented in memory as "run it after touching emoji rendering" and nothing ran it.

## `scripts/testCache.mjs` + `scripts/testCache.test.mjs` — session-scoped cache for a deterministic check (added 2026-08-21 09:45 EDT)

`node scripts/testCache.mjs <key> <input-path>... -- <command> [args...]` — wraps a command whose result is a pure function of a known set of files, hashes their actual bytes plus the wrapped command itself, and skips re-running it (replaying the last pass) when neither has changed since the last confirmed-clean run. Wired into `docs:audit:test` and `test:hooks` in `package.json`. Cache lives in gitignored `local/.test-cache/`, so it is local/session-scoped by design and never affects CI (a fresh checkout has no cache dir) — and `CI=true` is treated as an automatic no-cache regardless, so CI's own deliberately-kept second invocation of a wrapped command (see `ci.yml`) always re-executes for real rather than replaying the first invocation's result within the same job.

⚠️ **A FAILING run is never cached** — only a confirmed-clean result is ever replayed, so this can only ever save time on the "still clean" path. ⚠️ **Directory inputs are expanded non-recursively, and that precondition is enforced, not just documented** — a subdirectory found inside an expanded directory input throws loudly rather than silently hashing fewer files than the cache key claims to cover (a real bug this shipped with once, caught in code review, before the assertion was added). If a new consumer's input directory needs real nesting, either pass the nested directory as its own explicit input path or make `expandInputs` recursive.

## Portal core plan 2's op + handler-snapshot tests (added 2026-08-21 13:29 EDT)

`scripts/calendarOps.test.js`, `scripts/loadoutOps.test.js`, `scripts/patchnoteOps.test.js`, `scripts/seasonOps.test.js` and `scripts/announcementOps.test.js` cover each entity's `core/ops/*.js` file (validate/preview/apply/invert, pure-logic where possible). `scripts/calendarHandlerSnapshot.test.js`, `scripts/loadoutsHandlerSnapshot.test.js`, `scripts/patchnotesHandlerSnapshot.test.js`, `scripts/seasonHandlerSnapshot.test.js` and `scripts/announcementsHandlerSnapshot.test.js` are the pre-refactor modal-shape fixtures for `handlers/manage/*.js` (see `scripts/drawsHandlerSnapshot.test.js`'s own header for the technique — `feedback_snapshot_before_unclickable_refactor`). `scripts/loadoutBulkFormat.test.js` covers the labelled bulk-add/edit grammar `utils/adminParser.js` parses and `handlers/manage/loadouts.js` writes, separately from `loadoutOps.test.js` because the grammar is a user-facing contract rather than an op contract. `scripts/undoRetired.test.js` is Task 7's closing check: `registerUndo` is gone from every `handlers/manage/*.js` file, and every mutating registry action resolves to a real op. All wired into `npm test`; none need `scripts/testCache.mjs` wrapping — each runs in well under a second, unlike the 71-check `docs-audit.test.mjs` or the hook self-test suite that wrapper exists for. Design + the real defects found integrating each entity (a loadouts `shareCode` clobber, a season Cloudinary-metadata-sync gap, missing op-return fields) → `docs/superpowers/plans/2026-08-20-portal-core-remaining-entities.md`'s Task 2-7 sections and their own per-file header comments. Subsystem detail: `.claude/rules/manage-panel.md` + `.claude/rules/interaction-router.md`.

## `scripts/hotpatch.mjs` + `scripts/hotpatch.test.js` — the hotpatch CLI + its classifier/applier tests (added 2026-08-20 14:26 EDT)

`scripts/hotpatch.mjs` drives a reload of the running bot over `SIGUSR2`; `scripts/hotpatch.test.js` covers `utils/hotpatch.js`'s classifier, applier and orchestrator (13 cases, wired into `npm test`). Full design, the invariant, and every trap already paid for → **`.claude/rules/hotpatch.md`**, which loads automatically alongside `utils/hotpatch.js`, `scripts/hotpatch.*`, `handlers/router.js` and `bot/lifecycle.js`.

## Portal plan 3's server/auth/API/build/UI test scripts (added 2026-08-21)

`scripts/portalBoot.test.js` covers `portal/server.js`'s `assertEnvironment()` (the dev-vs-prod database guard, same failure class as the multiple-bot-instances rule). `scripts/portalAuth.test.js` covers `portal/auth.js`'s pure crypto/cookie-shape functions (host-only cookie attributes, the OAuth authorize URL's scope, state verification, session-id hashing) -- no network, no real Discord call. `scripts/portalApi.test.js` covers `portal/api/policy.js`'s `gateCommit()` (the tier-3 export + typed-confirmation gate) plus a source-scan invariant matching `scripts/botAccessPermissions.test.js`'s shape: every `route('POST', ...)` registered anywhere in `portal/api/*.js` must wrap its handler in `requireAdmin`. `scripts/portalUi.test.js` covers `portal/ui/track.logic.js`'s pure functions (`bandClass`/`laneFor`/`tierOf` — SHAPE carries state, COLOUR carries topic, spec §9). `scripts/portalRender.test.js` renders the real components server-side with `preact-render-to-string` and asserts each still emits what its design depends on -- the rail's per-realm links and icons, the masthead being DATA with no paragraph, the door's OAuth disclosure and its three states reading identically, the Manifest's topic dots and three state SHAPES, and the Board's four columns, blocked reason, stated inverse and review-not-blind-commit control. ⚠️ **STRUCTURAL, never a golden snapshot** -- a blob written by the session that built the thing would make a later verification pass circular; this catches a component silently losing a piece and says nothing about whether the piece is well designed. ⚠️ **It renders through a scratch tree at `portal/public/.ssr/` whose `vendor/` RE-EXPORTS preact instead of copying it.** The browser gets a vendored COPY; `preact-render-to-string` resolves `node_modules`. Two preact instances means two `options` objects and every hook throws `Cannot read properties of undefined (reading '__H')` -- found by running it, where the four hook-free components passed and every hook-using one failed identically. The scratch tree lives under `portal/public/` (already gitignored) rather than `/tmp`, because bare-specifier resolution walks up from the importing FILE. `scripts/portalContrastRendered.test.js` is the gap audit's §8 in full: it launches Chrome via `puppeteer-core`, renders a fixture page carrying every portal surface against the real built stylesheet, walks every element that paints its OWN text, and compares computed `color` against the nearest ancestor actually painting a background. **That last part is why it exists** -- `portalContrastAudit`'s static pass resolves a rule's own declarations, and cannot know whether `--topic-accent` was set on a given element, because JS decides that at render time (measured: set 0 times in CSS, 4 times inline, against 14 fallback references). Proven to catch what the static pass cannot: reverting `.bar.live`'s fallback to `--raised` is reported as `1.39:1 span.bl` — a CHILD element inheriting an unaccented parent's background — and the suite goes red. ⚠️ **`puppeteer-core`, not `puppeteer`**: it uses a Chrome already on the machine rather than downloading its own ~150MB copy; `PUPPETEER_EXECUTABLE_PATH`/`CHROME_PATH` override the search and GitHub Actions' ubuntu images ship Chrome at `/usr/bin/google-chrome`. ⚠️ **With no browser it SKIPS LOUDLY and says it is not a pass** — a contrast gate that silently checks nothing when its dependency is missing looks identical to a clean run, which is the exact failure this whole family of checks exists to prevent. It also asserts its own probe can fail, by reinstating the original login button and requiring 1.39:1 to be measured back. `scripts/portalRealms.test.js` covers the four Task-6 realms' pure functions, which live in their `portal/api/*.js` files rather than a duplicate `.logic.js` copy (Armory's `coverageFlags`, Access's `singlePointsOfFailure`). All wired into `npm test`. `scripts/portalChrome.test.js` covers the chrome every realm renders inside: `palette.logic.js`'s ranking (a prefix beats a containment, a keyword comes last, being on the current realm is worth half a tier) and its `paletteBlocked()` guard — ⚠️ **`inert` on the header stops the pointer and the tab order and does NOT stop a document-level keydown**, so without that guard `⌘K` behind an open modal focuses an input nobody can type into — plus `overlay.logic.js`'s `typedConfirmReady` (an ABSENT expectation must never read as satisfied, or a mis-wired tier-3 drawer becomes a one-click destructive button) and a conservation gate asserting **no native `confirm`/`alert`/`prompt` survives anywhere in `portal/ui`**. That last one exists because `board.js` kept one through the entire overlay build precisely because it worked. `scripts/portalAsync.test.js` covers `portal/ui/async.logic.js` — what a response MEANS — plus `portal/ui/httpClient.js` executed against stubbed responses. ⚠️ **Its load-bearing case is the one it nearly broke:** making `fetchJson` non-throwing almost collapsed every non-2xx into a generic failure, which would have silently destroyed the commit gate's 409, whose `{ok:false, reason}` body carries the only explanation a reader ever sees for a refused tier-3 commit. It also enforces the design's three-field rule — an error names WHAT failed, what it MEANS for your work, and ONE action — because an error missing any of them is the one that gets screenshotted and forwarded. `scripts/portalOneWay.test.js` covers `portal/ui/oneway.logic.js` and carries a **conservation check that reads `core/ops` directly**: every tier-3 op with a real `/manage` action must have a row in the strip. It exists because seven of them shipped with a full permission model, a commit gate and **no button anywhere**, and nothing could see it — every other gate measures what the code DOES, and a capability with no affordance does nothing. Its fixture is shaped like `/api/season`'s response (`draft` destructured OUT of `live`) rather than like the component, because the first version was shaped like the component and proved only that the component agreed with itself while the promote row read "No draft is active" over an active draft.

`scripts/portalOpsReach.test.js` is the **whole-registry** generalisation of that check, and it exists because the tier-3 version answered only one slice: `core/ops` declares 42 op types and **18 were named by no portal source file at all**. Every op must either be named by a `portal/ui/*.js` file or carry an entry on this file's `NO_SURFACE` list — and an entry there is a CLAIM THAT GETS CHECKED, not a suppression. An op excused as *inverse-only* must really have no `/manage` action and must really be produced by some other op's `invert()`; an op excused as *reached through an equivalent* must name an equivalent that is itself reachable, because an excuse pointing at a second absence is not an excuse. Each claim ships with a falsifier. **A new op with no way to run it fails this file rather than going quiet.**

`scripts/portalGapSplit.mjs` (`npm run portal:gapsplit`) answers *what is actually left*, which a coverage percentage does not: it splits `portal:coverage --missing` into **no CSS rule** (building it means authoring CSS), **already emitted somewhere** (instrument asymmetry, not work) and **genuinely unbuilt**, grouped by prefix so a nine-class cluster reads as one panel to decide about. It exists because a ceiling of "~92–94%" was stated out loud on 2026-08-26 and beaten within the hour — a third of what had been written off was buildable. ⚠️ Its "already emitted" scan strips comments first, for the same reason every other source gate here does.

⚠️ **Two instrument blind spots were closed the same day.** `portal:orphans` read classes only out of `class="…"` and `class=${…}`, so a class passed as a DATA VALUE was invisible — and `metaClass: 'rowlife'` shipped with no rule anywhere; it reads class-valued props now. And `portal:coverage` counted trailing-hyphen FRAGMENTS (`class="lvlb lv-${level}"` scans as the literal `lv-`) as classes, capping its own ceiling below 100% for no reason.

`scripts/portalAnalytics.test.js` covers the seam between `commands/bot.js`'s two shared stats functions and the portal that reuses them. Its finding: **`computeUsageStats` has always excluded admin traffic and `computeTimingStats` never filtered it at all**, so the Usage counts and the Timing percentiles — side by side on one screen, in Discord and in the portal — were computed over different rows. The conservation check is a COUNT (windows opened vs filters applied) rather than a parse, because Mongo filters nest and the parsing version reported three correctly-filtered queries as bare. It also checks the boot card's fields against `models/BootRecord.js`, so a rename there fails a test instead of rendering em-dashes forever.

`scripts/portalTips.test.js` covers the tooltip runtime (`portal/ui/tips.js`, `tips.logic.js`). Twenty-six `data-tip` hosts had **no reader at all** — `.tip` and `.tip .sub` were defined in the adopted sheet and unused, so an orphan check saw nothing wrong. Its load-bearing cases are the placement arithmetic at the EDGES, which is where a hand-check does not look: prefer right, flip left at the boundary, and **clamp when it fits on neither side**, because a bare flip runs off the other edge.

`scripts/portalArmoryBulk.test.js` covers the Armory Bulk view's preview arithmetic and `portal/ui/armory.logic.js`'s `bulkPasteSummary`/`armoryExportQuery`. Its load-bearing check runs against the **real** `parseBulkLoadoutList` rather than a fixture: that parser drops a block it cannot read and KEEPS one whose badge token it does not recognise, so the error array conflates a rejection with a warning and the split has to come from the block count. A fixture asserting that would agree with itself forever.

`scripts/portalExport.test.js` covers the shared Export strip (`portal/ui/exportPanel.js`, `exportPanel.logic.js`, `download.js`). Its load-bearing check is a **source scan asserting nothing in `portal/ui` opens a `data:` URL** — browsers block that as a top-level navigation, so three realms shipped an Export button that ran, said nothing and produced no file. It also checks client/server conservation: every export scope the UI offers must name a route the server registers. ⚠️ It **strips comments before scanning**, because the first version flagged three files whose only offence was describing the defect in the comment recording it.

`scripts/lib/portalRouteQuery.js` is not a test — it is the single map of which portal GET routes refuse a request naming no scope, shared by `portalRoutes.test.js` and `portalHarness.test.js` so the two drivers cannot drift.

`scripts/portalPatchNotes.test.js` covers the Season record panel's editor (`patchRecordRows`/`patchEditOps`). It asserts two properties of the real date functions rather than describing them: `formatReleaseDateTime` is **lossy below a minute**, and `parseReleaseDateTime('')` returns **now** rather than null — which is why an untouched field sends the stored ISO string, and why a blank one is refused instead of clearing anything.

`node scripts/portalCoverage.mjs` (`npm run portal:coverage`, `-- --missing` for the class names behind each number) is the measurement that stops a session claiming the redesign is finished when it is a third done — it reads every class the mockup's adopted `app.css` defines and counts how many `portal/ui` actually emits, per realm. 🔴 **DONE IS NOT 100%, and the script prints that in its own output**: a real slice of the delta is the mockup's reviewer scaffolding (`data-demo-only`, `S.audit()`, `data-async-host`/`data-skel`), some of it marked in capitals as MUST NOT SHIP, so a realm is finished when what remains is scaffolding. ⚠️ **It UNDER-counts** — components build class strings in variables (`const cls = 'bar ' + state`), which a source scan cannot see — so read it with the page open, never instead of. Its most useful output is the class-level diff rather than the percentage: a block of gaps identical across all eight surfaces including the Door is a CHROME gap, not eight realm gaps, and fixing one of those moves every number at once.

`node scripts/portalOrphans.mjs` (`npm run portal:orphans`) is its INVERSE and measures what it structurally cannot: every class `portal/ui` emits must be a class the adopted stylesheet defines. Adopting `app.css` whole deleted thirteen portal-authored sheets, and every class a component still emitted became an element with **no rules at all** — 46 of them, across nine files, with every gate in the suite passing. 🔴 **A RATCHET:** a class not on `KNOWN_ORPHANS` fails the run, and a class on it that has stopped being emitted **also** fails, so the list can only shrink and cannot rot into names nothing renders. ⚠️ **It cannot see a class that exists and means something ELSE** — `.spark` in the adopted sheet is a horizontal progress track, and the portal emitted vertical-bar markup into it for weeks; the class was defined, so this gate was silent. Open the page.

`scripts/buildPortal.js` emits `portal/public/` (gitignored -- a Node-served build output, unlike the legal site's Cloudflare-Pages `public/`, which has no server-side build step and must stay committed): copies `portal/ui/*.js` verbatim (no bundler, spec decision 6), vendors Preact + htm from `node_modules` into `portal/public/vendor/`, concatenates `portal/ui/*.css`, and runs its own `portalContrastAudit()`. ⚠️ **That function is NOT `scripts/buildLegalPages.js`'s `contrastAudit()` reused** -- the plan's Task 4 Step 3 assumed it was generic; actually calling it throws (`for (const page of built)` needs the site generator's own page-object array, and it checks only its own hardcoded `--desk`/`--card`/`--sig` variable names). `buildPortal.js` reimplements the same WCAG luminance/ratio math scoped to the portal's own token vocabulary. ⚠️ **It checks THREE populations now, and the second and third exist because the first was not enough** (extended 2026-08-23 16:10 EDT): the `:root` ink x surface pairs it always had (12), a declared `SIGNAL_TEXT_ON` map for signal colours used as TEXT (which nothing covered -- a kind-chip shipped at 4.15:1 under it), and **every RULE in the concatenated stylesheet that declares a `color`**, resolving `var()` chains and their fallbacks to a literal hex. That third pass is the one that matters: it found `.accent-fill` still declaring `#000` on `--raised` at **1.39:1** -- the login button's own bug, still latent on every Stage button, months after the door was fixed with a separate rule. Rules declaring both `color` and `background` are self-contained; rules declaring only `color` are checked against `--raised`, the lightest surface, which is the conservative bound for light-on-dark text rather than a guess about which panel they sit in. Non-hex values (gradients, `rgba`, `currentColor`) are skipped, never guessed. 157 pairs at time of writing; proven able to fail by reinstating each real defect. Proving it against real input (Task 4 Step 4) caught a genuine defect: the design spec's own `--ink3` value (`#6E7C87`) fails WCAG AA against every portal background; corrected to `#80909D` (same hue, scaled to clear 4.5:1) in `portal/ui/tokens.css`.

## Part 0's instruments — the reverse-orphan sweep, the geometry fixture, the states harness (added 2026-08-28 09:4x EDT)

Four scripts and two shared libraries, built as PART 0 of `docs/superpowers/plans/2026-08-27-portal-conformance.md`. They exist because **every gate this repo had asked its question in one direction**, and the defect that started the conformance pass runs the other way: the Preact migration carried `app.css` whole and dropped the markup that activates it, so rules sat matching nothing — silently, forever, because CSS is declarative and inert.

- **`portalReverseOrphans.mjs`** (`npm run portal:reverse-orphans`, `--ci` in `npm test`) — the inverse of `portal:orphans`: **does this RULE have an ELEMENT?** Four shapes: a `[data-*]` selector with no emitter · a `var(--x)` read with no setter anywhere · a class with ≥2 rules and no emitter · and a **NAME MISMATCH**, a class that IS emitted against which no rule matches. That last shape is why it runs a small recursive-descent evaluator over the subset of JS that appears in a class position instead of grepping: `armory.js` emits `trow t-${RANK_KEY[key]}` from a lookup table, so a literal-prefix scanner reports `t-` as an orphan, calls `.t-best` unemitted and stays silent about the four tier rows that really are broken — three wrong answers from one missing capability, a miss that had already happened twice. ⚠️ **Comments are blanked with `acorn` before anything is read** — this tree documents every trap beside the code that pays for it, `data-bare` and `.t-top4` included, and counting a comment would certify the exact defect being hunted. ⚠️ An expression it cannot resolve becomes a **dynamic prefix** or an **opaque count**, both printed: the blind spot is reported, never dropped, because a ③ finding behind an unreadable expression may be a false positive. `--why <class>` answers "is this finding real?" by naming the emitting file. A **ratchet** against `portal/fixtures/reverse-orphans.json`, failing on a new entry AND on a baseline entry that has been fixed without re-recording.
- **`portalReverseOrphans.test.mjs`** (`npm run portal:reverse-orphans:test`) — one case per shape, each fed a fixture tree carrying a known defect and asserted to name it, then the same shape wired correctly and asserted silent. `PORTAL_REVERSE_ROOT` repoints the scan at a fixture tree, the same technique as `DOCS_AUDIT_ROOT`. The load-bearing case is the lookup table: `.t-best` must read as EMITTED and `t-t4` as a MISMATCH.
- **`portalGeometry.mjs`** (`npm run portal:geometry -- --realm <r> --write|--check`; `--all --check` in `npm test`) — records a realm's `__grid.all()` counts **and its inventory** (h1, view tabs, column headers, section headings) to `portal/fixtures/geometry/<realm>.json` when its Part closes, then fails when they move. It exists because six realms share one stylesheet, which is how a `.lvlbars` block restyled charts three realms away while every gate passed. ⚠️ **Counts catch MOVEMENT, not WRONGNESS** — two compensating changes keep every number identical. The inventory travels beside them because `__grid` reports geometry, not identity: two elements can be perfectly on-grid and be the wrong two. An empty fixture directory prints "no fixtures recorded yet" and exits 0 — correct during Part 0, and never read as verified.
- **`portalGeometry.test.mjs`** — the diff, falsified without a browser. A `compare()` that returned `[]` for every input would certify every regression and look exactly like a stable realm.
- **`portalStates.mjs`** (`npm run portal:states`, `--ci` in `npm test`, `--record` to re-baseline) — walks every state in `portal/fixtures/states/<realm>.json` through the harness at 1282x888 and runs three **relational** passes. 🔴 **Relational is the whole point:** every other gate here is shaped `for each element: assert P(element)`, and that shape structurally cannot see two elements disagreeing — which is why a 44px input inside a 34px wrapper shipped for weeks, reported twice by a human, with a green suite. ⚠️ **The catalogue GROWS per realm** — Part 0 builds the driver and seeds `shell.json`; each realm registers its own states as it walks them. There is **no PASS 2** (the mockup's numbering ran 1, 3, 4, 5) and PASS 5 (reduced motion) is deliberately not here: every honest assertion about it is about what CSS declares, which a rendered walk is the wrong instrument for.
- **`lib/portalStatePasses.cjs`** — the judgement half, pure and out of the browser so it can be falsified. PASS 1 composite · PASS 3 space · PASS 4 keyboard and modality. ⚠️ **PASS 1 requires the wrapper to be drawn TIGHT around the control** (≤12px taller): an ordinary field inside a panel legitimately has its own border, and the first version reported it on every state — a finding that is wrong three times a run is a finding nobody reads. ⚠️ It selects by ROLE in the composite, never by `[data-bare]`: the mockup's own first version iterated the marker, so removing the marker — the exact regression it exists to catch — left it nothing to iterate and it reported clean.
- ⚠️ **Two driver changes on 2026-08-28, both from a state that could not be reached.** A step may now carry its own **`waitMs`** — the default 160ms between steps is not enough when a step's effect MOUNTS the next step's target, and when it is not enough the following step clicks nothing and only `expect` reports it. And **`type` accepts the nested `{sel, text}` shape its siblings teach** as well as the flat one it originally required: every other two-value step nests (`clickText: {sel, text}`), so a hand-written registry naturally writes `type: {sel, text}`, which the driver read as `step.sel`/`step.text`, found undefined, and typed nothing into nothing. The state then "ran".
- **`portalStates.test.mjs`** (`npm run portal:states:test`) — feeds each pass the real numbers the defect shipped with, then the corrected version of the same shape, and asserts fire-then-silence.
- **`portalClassProps.mjs`** — the ONE list of props that carry a class name (`col`, `metaClass`, `cls`, `accentClass`, `tone`), read by `portal:coverage`, `portal:orphans` and now the reverse sweep. It exists because the first two kept their own copies and drifted: `tone` was added to one and not the other, so `tone: 'live'` counted as covered while the gate that refuses a class with no rule could not see the syntax at all — a no-op class shipped and the number went up. ⚠️ **A prop that names a class goes here, nowhere else**, and it is a module with no side effects on purpose: an earlier version lived inside `portalOrphans.mjs`, so importing the constant RAN that gate and `portalCoverage` printed the other gate's output instead of its own.
- **`lib/chromePath.cjs`** — the ONE list of places a Chrome might be, now shared by `portalContrastRendered.test.js`, `portalGeometry.mjs` and `portalStates.mjs`. Two copies drift, and the drift is silent: a machine where one resolves Chrome and the other does not shows a green suite beside a "no browser" skip with nothing saying they disagree. All three **skip loudly and say they are not a pass** when no browser resolves.

⚠️ **The mockup package's instruments are loaded by `docs/superpowers/mockups/2026-08-23-portal-interactive/assets/shell.js` now.** `.grid.js` and `.peers.js` shipped beside the eight mockup pages and **no page had ever included them**, so `__grid.all()` had never once run on the artifact the portal is measured against. `window.__instruments()` loads them on demand and `?grid` loads them at boot; the loader lives in the one file all eight pages already load, so `door.html` gets it too. `__grid.all()` also carries the **1282x888 viewport contract** in its own output now — a page cannot resize its own window, so every reading states what it was taken at and an off-contract one says so in the reading itself. `buildPortal` copies both files into the harness, so the mockup remains the single source.

## `portalDiff.mjs` — the only instrument that answers the acceptance test (added 2026-08-28 16:0x EDT)

`npm run portal:diff -- --realm season` · `--scroll <px>` · `--json` · `--portal harness` · `--as <discordId>`. Renders the mockup and the **real** portal at 1282×888, subtracts them, and reports the differing regions ranked by area with the element under each side. Writes `local/diff-<realm>/mk-<realm>.png` and `pt-<realm>.png`.

🔴 **Every other gate in this repo is an element scanner and this one is not.** `portal:orphans` asks whether a class has a rule; the reverse sweep asks the inverse; `portalStates` walks states; `__grid` measures boxes. All of them answer *"which elements exist, and are they well-formed"* — so **a page with all the right elements in the wrong arrangement passes every one of them**, which is what shipped from Part 1 of the conformance pass and what Harkirat found in about two seconds with two screenshots. This one enumerates nothing: the mockup is a **program that renders**, not a specification to be read, and where the two renderings disagree IS the work list.

⚠️ **It is never scored and never reaches zero** — real data against a fixture, portal-ahead surfaces, and deliberate divergences all land in it. Every region is **closed or cited** in the Part's difference ledger.

- 🔴 **IT REFUSES TO REPORT IF THE PORTAL SIDE IS THE DOOR**, because its own first run diffed a login page and produced 11.9% across twelve confidently-ranked regions of pure noise. Puppeteer launches a clean profile, so it **mints a PortalSession in dev Mongo** for the run — and refuses to do that against any URI that is not localhost, since a diff tool able to write a session into production is not a diff tool.
- ⚠️ **It scrolls `main`, not the window.** This repo had already written that trap down and the tool walked into it anyway on its first day: `--scroll` was silently inert and reported the top of the page twice.
- ⚠️ **`Buffer.from(...)` around the screenshot is load-bearing.** Recent puppeteer returns a `Uint8Array`, and `Uint8Array.prototype.toString('base64')` ignores its argument and returns comma-joined decimals — a syntactically valid data URL full of garbage, surfacing as an image `onerror` that prints `Event: Event`.
- ⚠️ **Animation is stopped before the shutter**, identically on both sides, or the output moves between runs and nobody trusts it twice.
- 🔴 **STATE ITS COVERAGE WHENEVER YOU STATE ITS RESULT.** It answers **one route, one scroll offset, one width**. It cannot see anything behind an interaction (a drawer, a composer, a hover), anything below the captured 888px, a colour difference under the channel tolerance, or the mobile layout — and Harkirat's original instruction named *every sub-view* at *both* widths. **A clean diff on Track says nothing about Board.** This tool is new and the failure it exists to prevent was over-trusting a green instrument; do not repeat it one level up.
- The 16px cell size and the 6%/24 tolerances were chosen by running it against the **mockup versus itself** (which must report zero) and widening until that stayed empty — a threshold nobody falsified is a threshold nobody should trust.

## `scripts/portalInventory.mjs` — walk BOTH DOMs and compare what the pages are MADE OF (added 2026-08-28)

`npm run portal:inventory -- --realm <r> [--view <tab>]`. Keys every VISIBLE element by its class signature and reports four lists: **ONLY IN THE MOCKUP · ONLY IN THE PORTAL · DIFFERENT WORDS · DIFFERENT STYLE**. It exists because `portal:diff` ranks by AREA, which is the right first question and a poor second one — a 6px control-height difference repeated on forty controls never rises above the noise floor, and a column whose LABEL says something else entirely occupies almost no pixels. The Board's headings read `Live now / Upcoming / Staged / Ended` in the design and `Draft / Staged / Blocked / Ready` in the code for weeks, through every green gate.

🔴 **VISIBLE ONLY, AND THAT ONE LINE IS THE DIFFERENCE BETWEEN AN AUDIT AND A WALL OF NOISE.** The mockup is ONE page carrying every view at once behind `display:none`; the portal is an SPA that renders the active view and nothing else. Comparing raw DOMs reported the Track's ruler, lanes, deadrail and zoomer as *missing from the portal* while looking at the Board — 93 rows, most of them another view's furniture. `getClientRects()` is empty for anything not painted, which is exactly the question being asked. **Proved both ways before it was trusted:** the known case (`section.bcol ×4`) still appears and the Track furniture is gone.

⚠️ **Compare text with the whitespace REMOVED, not collapsed.** The mockup is hand-written HTML with newlines between inline elements and htm emits none, so `textContent` differs on nearly every container for a reason that is not a design difference — 47 rows on the first run, most of them noise, which is the fastest way to make an audit unread.

⚠️ **A difference is not a defect**, and the tool says so in its own footer. Adjudicate every row against `COMPANION.md`, **with dates** — see the conformance plan's §0.5 R2.

## `scripts/portalDiff.mjs` — render both pages and subtract them (added 2026-08-28)

Renders the mockup and the real portal at the same viewport and reports the differing regions, ranked by area, with the element under each side. Every other portal gate here is an element scanner — does this class have a rule, does this rule have an element, do these states walk — and **a page with all the right elements in the wrong arrangement passes all of them**, which is exactly what shipped from Part 1's first attempt. Run it FIRST on any portal surface work. `--selftest` renders the mockup twice and must come back at essentially zero; `--view <tab>` and `--viewport WxH` reach the sub-views and the narrow layout.

🔴 **IT CAPTURED ONE SCREENFUL FOR ITS ENTIRE FIRST DAY.** `page.screenshot` clipped to `{0,0,1282,888}` while Season is **4,378px**, so every reading it produced described the top fifth of the page and a `--scroll` flag nobody used made the ceiling look like a framing choice. Full page is the default now; `--fold` is the opt-in for the old behaviour, and the report prints both sides' heights and the delta. **The falsifier was free: no region in any report ever had `y + h` greater than 888.**

🔴 **A REFUSAL GUARDS THE FLAG'S EFFECT, NOT ITS ARRIVAL — and this bit within an hour of the flag being written.** `--view` refuses if the tab is missing or if clicking it changed nothing, on either side. It cannot refuse a flag that never reached the process. **In zsh an unquoted `$v` does NOT word-split**, so `for v in "--view Board"; do node scripts/portalDiff.mjs --realm season $v; done` passes ONE argument, `--view Board`, which `indexOf('--view')` never matches — three loops ran the default view, printed plausible percentages, and looked like three successful sub-view runs. The report was honest the whole time (`portal:diff — season · Board` versus `portal:diff — season`), and the mistake was reading the percentage line instead of the header. **Read the header line the tool prints, and check the PNG filenames** — a sub-view run writes `mk-season-board.png`, never `mk-season.png`.

⚠️ **It mints a `PortalSession` in dev Mongo for the hardcoded owner** and refuses any URI that is not localhost. Minting for whoever `AdminUser.findOne({})` returns is what it used to do, and that is a scoped test admin holding three realms of six — the tool then reported the rail as a difference, which is a tool measuring its own setup.

## `scripts/cloudflared-dev-config.yml` — the DEV tunnel, on the Mac (added 2026-08-28 12:0x EDT)

Installed to `~/.cloudflared/dioreo-dev.yml`; run with `cloudflared tunnel --config ~/.cloudflared/dioreo-dev.yml run`. Puts `https://dev-portal.dioreo.app` in front of the local dev portal on `127.0.0.1:8787`, so the branch is reviewable from a phone instead of only from the machine it runs on. Tracked for reproducibility and holds no secret.

🔴 **THE HOSTNAME IS ONE LABEL DEEP, AND THE FIRST ATTEMPT PROVED WHY.** `dev.portal.dioreo.app` failed TLS before reaching anything: Cloudflare's Universal SSL certificate covers `dioreo.app` and `*.dioreo.app` and nothing below that — read straight off the live certificate — so a third-level name has no edge certificate at all. A multi-level wildcard needs the paid Advanced Certificate Manager. ⚠️ The abandoned CNAME still exists; `cloudflared` can create a route and has no command to remove one.

⚠️ **Two credentials, two jobs, and confusing them wastes a rotation.** `~/.cloudflared/cert.pem` authenticates `tunnel create` and `tunnel route dns`; the per-tunnel `<uuid>.json` is what `tunnel run` uses. Revoking the first does not stop a running tunnel.

⚠️ **`cloudflared tunnel login` works from another device** — it returns the certificate through Cloudflare's relay rather than a localhost redirect, so the URL can be opened on a phone while the waiting process on the Mac collects it. That is how this one was authorized.

⚠️ **THE EDGE CACHES THIS HOSTNAME UNLESS A RULE SAYS OTHERWISE — one now does (added 2026-08-28 18:2x EDT).** The zone had no `http_request_cache_settings` entrypoint ruleset at all, so a `.js` or `.css` file served through the tunnel picked up Cloudflare's default **four-hour** cache *over* the origin's `no-cache, must-revalidate`, and a rebuilt portal kept serving the previous module graph while `curl` against `127.0.0.1:8787` returned the new bytes. The entrypoint now exists with one rule — `(http.host eq "dev-portal.dioreo.app")` → `set_cache_settings` `{"cache": false}` — created by `PUT`ing to `/zones/<zone>/rulesets/phases/http_request_cache_settings/entrypoint`. 🔴 **That `PUT` body takes `rules` and nothing else**: sending `kind` (or the other fields a GET returns) is rejected with `invalid JSON: unknown field "kind"`. And a `404` with code `10003` on the GET means *no entrypoint exists yet*, **not** that the token lacks access — the `PUT` creates it. ⚠️ The rule is scoped to the dev host only; `dioreo.app` still caches normally, which is what makes it checkable.

## `scripts/cloudflared-config.yml` — hardcode the port, never a template placeholder (fixed 2026-08-22 10:00 EDT)

Installed to `/etc/cloudflared/config.yml` on the VM (see `docs/reference/deployment-and-ops.md`'s "Web admin portal" section for the install path). Shipped with `service: http://127.0.0.1:${PORTAL_PORT}` — a template placeholder nobody ever expanded. **`cloudflared`'s config.yml does no env-var substitution of its own, and nothing in this repo runs `envsubst` on install**, so as written the tunnel would have dialed a literal host named `${PORTAL_PORT}` and 502'd every request the first time it was ever installed. Caught before the systemd unit was ever installed for real (see `docs/reference/portal-launch-checklist.md`). Fixed to a literal `8787`, matching `portal/server.js`'s own default — kept in sync BY HAND with `PORTAL_PORT` in `.env`; if that env var's value ever changes, this file's literal port must change with it, and nothing will warn you if they drift.
